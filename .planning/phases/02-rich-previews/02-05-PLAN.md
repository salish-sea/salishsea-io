---
phase: 02-rich-previews
plan: 05
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - .github/workflows/deploy.yml
autonomous: false
requirements:
  - PREV-01
  - PREV-02
  - PREV-03

user_setup:
  - service: aws-cdk
    why: "Lambda@Edge requires us-east-1 CDK bootstrap before first deploy"
    env_vars: []
    dashboard_config:
      - task: "Bootstrap CDK in us-east-1"
        location: "Terminal: cdk bootstrap aws://648183724555/us-east-1"
  - service: github-actions
    why: "CDK deploy needs SUPABASE_ANON_KEY to set the SSM SecureString parameter"
    env_vars:
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API → anon/public key"
    dashboard_config:
      - task: "Add SUPABASE_ANON_KEY as a GitHub Actions secret"
        location: "GitHub repo → Settings → Environments → production → Add secret"

must_haves:
  truths:
    - "Pasting an occurrence URL into Bluesky or Facebook renders a preview card with title, description, and image"
    - "Pasting the homepage URL renders a minimal preview with just the SalishSea.io title"
    - "Regular browser visits to the site continue working normally (bot-detection gate protects the SPA)"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Updated deploy workflow with CDK deploy step"
  key_links:
    - from: "GitHub Actions deploy workflow"
      to: "CDK deploy"
      via: "cdk deploy --context supabaseAnonKey=$SUPABASE_ANON_KEY"
      pattern: "cdk deploy"
    - from: "CloudFront Distribution"
      to: "Lambda@Edge viewer-request"
      via: "LambdaFunctionAssociation"
      pattern: "facebookexternalhit.*salishsea.io"
---

<objective>
Bootstrap the us-east-1 CDK environment, add the Supabase anon key secret to GitHub Actions, update the deploy workflow to include CDK deploy, trigger deployment, and verify rich previews render correctly in production.

Purpose: Complete PREV-01, PREV-02, PREV-03 — all three requirements are only confirmed by a live deployment test.
Output: Production edge function running on CloudFront; verified preview cards in at least one messaging platform.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-rich-previews/02-CONTEXT.md
@.planning/phases/02-rich-previews/02-RESEARCH.md
@.planning/phases/02-rich-previews/02-VALIDATION.md
@.planning/phases/02-rich-previews/02-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Bootstrap CDK in us-east-1 and add GitHub Actions secret</name>
  <files></files>
  <action>
    Two one-time human actions required before Claude can update the deploy workflow:

    1. Bootstrap CDK in us-east-1 (Lambda@Edge requirement — AWS requires Lambda@Edge functions in us-east-1):
       ```
       cd /Users/rainhead/dev/salishsea-io/infra
       cdk bootstrap aws://648183724555/us-east-1
       ```

    2. Add SUPABASE_ANON_KEY to GitHub Actions:
       - Get the anon key from: https://supabase.com/dashboard/project/grztmjpzamcxlzecmqca/settings/api
       - Go to: GitHub repo → Settings → Environments → production → Add secret
       - Name: SUPABASE_ANON_KEY
       - Value: the anon/public key

    Claude cannot perform either action — CDK bootstrap requires local AWS credentials and terminal access; GitHub Actions secrets require UI interaction.
  </action>
  <verify>
    <automated>MISSING — no automated verification for CDK bootstrap or GitHub secrets; human confirms below</automated>
  </verify>
  <done>CDK bootstrap complete for us-east-1 AND SUPABASE_ANON_KEY secret added to GitHub Actions production environment</done>
</task>

<task type="auto">
  <name>Task 2: Update GitHub Actions deploy workflow for CDK deploy</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
    Read `.github/workflows/deploy.yml` first to understand the existing deploy steps.

    The workflow currently deploys the Vite SPA to S3. Add a CDK deploy step that runs AFTER the S3 sync, deploying the infra stack with the Supabase anon key from the GitHub Actions secret.

    Add a step to the production deploy job (after S3 upload, before CloudFront invalidation if any):
    ```yaml
    - name: Deploy CDK infra (CloudFront + Lambda@Edge)
      working-directory: infra
      run: |
        npm ci
        npm run build
        npx cdk deploy --require-approval never --context supabaseAnonKey=${{ secrets.SUPABASE_ANON_KEY }}
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_DEFAULT_REGION: us-west-2
    ```

    Note: First CDK deploy takes 10-15 minutes because Lambda@Edge replicates across all CloudFront edge locations globally.
  </action>
  <verify>
    <automated>grep -A 5 "cdk deploy" /Users/rainhead/dev/salishsea-io/.github/workflows/deploy.yml</automated>
  </verify>
  <done>`cdk deploy` step exists in deploy.yml with `--context supabaseAnonKey` flag and AWS credentials env vars</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify rich preview cards in production</name>
  <files></files>
  <action>
    After the GitHub Actions deploy completes, Lambda@Edge replication takes up to 15 minutes. Then verify previews using the Facebook Sharing Debugger and at least one messaging platform. Claude cannot access production crawlers — human verification required.
  </action>
  <verify>
    <automated>curl -s -H "User-Agent: facebookexternalhit/1.1" https://salishsea.io/ | grep "og:title"</automated>
  </verify>
  <done>
    - Facebook Sharing Debugger shows og:title="SalishSea.io" for homepage URL
    - Facebook Sharing Debugger shows og:title in "Species · Date" format for an occurrence URL (?o=...)
    - Normal browser visit to https://salishsea.io loads the SPA correctly (not broken)
  </done>
</task>

</tasks>

<verification>
`curl -s -H "User-Agent: facebookexternalhit/1.1" https://salishsea.io/ | grep "og:title"` returns a line containing "SalishSea.io".
`curl -s -H "User-Agent: Mozilla/5.0" https://salishsea.io/` returns the SPA index.html (not the OG HTML).
</verification>

<success_criteria>
- CDK bootstrap complete for us-east-1
- SUPABASE_ANON_KEY secret added to GitHub Actions production environment
- deploy.yml includes `cdk deploy --context supabaseAnonKey=...` step
- Production deployment succeeds (Lambda@Edge attached to CloudFront)
- Bot requests to salishsea.io receive OG-tag HTML with correct og:title
- Regular browser visits to salishsea.io continue loading the SPA normally
- Facebook Sharing Debugger confirms occurrence-specific preview shows species and date in title
</success_criteria>

<output>
After completion, create `.planning/phases/02-rich-previews/02-05-SUMMARY.md`
</output>
