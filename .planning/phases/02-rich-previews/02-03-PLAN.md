---
phase: 02-rich-previews
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/preview.jpg
autonomous: false
requirements:
  - PREV-01
  - PREV-02

must_haves:
  truths:
    - "A 1200x630px branded fallback image exists that will appear when no licensed occurrence photo is available"
  artifacts:
    - path: "src/assets/preview.jpg"
      provides: "Static branded OG fallback image (1200x630px, JPEG)"
  key_links:
    - from: "src/assets/preview.jpg"
      to: "https://salishsea.io/preview.jpg"
      via: "S3 upload path /site/preview.jpg → CloudFront origin /site/"
      pattern: "salishsea.io/preview.jpg"
---

<objective>
Create the static branded fallback image that appears in the og:image tag when an occurrence has no openly-licensed photo. This image is uploaded to S3 at `/site/preview.jpg` and served by CloudFront at `https://salishsea.io/preview.jpg`.

Purpose: PREV-01 and PREV-02 — all shared links show an image; occurrences without licensed photos show the branded image.
Output: `src/assets/preview.jpg` (1200x630px JPEG) committed to the repo and uploaded to S3 as part of the deploy workflow.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-rich-previews/02-CONTEXT.md
@.planning/phases/02-rich-previews/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create branded fallback preview image</name>
  <files>src/assets/preview.jpg</files>
  <action>
    The edge handler hardcodes `https://salishsea.io/preview.jpg` as the og:image fallback. This image needs to be created by the user and uploaded to S3 — Claude cannot generate a branded JPEG.

    Required specs:
    - Dimensions: 1200x630px (1.91:1 aspect ratio — optimal for Facebook, Bluesky, RCS, Twitter/X)
    - Format: JPEG or PNG (save as preview.jpg regardless)
    - Content: "SalishSea.io" wordmark or logo on a branded background (Salish Sea / orca theme)
    - Must look good as a preview card thumbnail — simple, readable at small sizes

    Steps for the user:
    1. Create the image using Figma, Canva, Photoshop, or any design tool
    2. Save it as `src/assets/preview.jpg`
    3. Upload to S3: `aws s3 cp src/assets/preview.jpg s3://salishsea-io/site/preview.jpg`
    4. Verify it's accessible at `https://salishsea.io/preview.jpg` in a browser
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://salishsea.io/preview.jpg</automated>
  </verify>
  <done>Image returns HTTP 200 at https://salishsea.io/preview.jpg AND src/assets/preview.jpg exists in the repository</done>
</task>

</tasks>

<verification>
`curl -s -o /dev/null -w "%{http_code}" https://salishsea.io/preview.jpg` returns 200.
`src/assets/preview.jpg` exists in the repository.
</verification>

<success_criteria>
- Image file committed at `src/assets/preview.jpg`
- Image is 1200x630px (1.91:1 aspect ratio)
- Image is accessible at `https://salishsea.io/preview.jpg` via CloudFront (HTTP 200)
- Image looks appropriate as a branded preview card (simple, readable at small sizes)
</success_criteria>

<output>
After completion, create `.planning/phases/02-rich-previews/02-03-SUMMARY.md`
</output>
