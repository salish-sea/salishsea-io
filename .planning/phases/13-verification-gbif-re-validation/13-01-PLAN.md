---
phase: 13-verification-gbif-re-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dwca/verify-artifact.ts
  - scripts/dwca/verify-artifact.test.ts
  - .planning/phases/13-verification-gbif-re-validation/13-CHECKLIST.md
autonomous: true
requirements: [ATTR-05]
must_haves:
  truths:
    - "A `verify-artifact.ts` script parses dist/dwca/occurrence.txt by column name (header → name→index map from OCCURRENCE_FIELDS) and asserts SC#2/SC#3"
    - "The artifact verifier fails non-zero if any occurrenceID is prefixed 'inaturalist:' or 'happywhale:'"
    - "The artifact verifier confirms institutionCode='SalishSea' and rightsHolder='SalishSea.io' on every data row"
    - "The artifact verifier confirms every datasetName starts with 'SalishSea.io — '"
    - "The artifact verifier confirms eml.xml <title> is the v1.3 archive title"
    - "The 5 prod-DB read-only checklist queries run via `npx supabase db query --linked` and their results are recorded in 13-CHECKLIST.md"
  artifacts:
    - path: "scripts/dwca/verify-artifact.ts"
      provides: "Artifact-level SC#2/SC#3/SC#4 assertions over occurrence.txt + eml.xml"
      exports: ["verifyArtifact", "buildHeaderIndex", "main"]
    - path: "scripts/dwca/verify-artifact.test.ts"
      provides: "Unit tests over fixture TSV/XML strings (no build / no DB needed)"
    - path: ".planning/phases/13-verification-gbif-re-validation/13-CHECKLIST.md"
      provides: "Recorded prod-DB checklist query results (SRC-01, institutionCode, rightsHolder, datasetName, occurrenceID-prefix scan)"
  key_links:
    - from: "scripts/dwca/verify-artifact.ts"
      to: "scripts/dwca/fields.ts"
      via: "import OCCURRENCE_FIELDS to build the column name→index map"
      pattern: "OCCURRENCE_FIELDS"
    - from: "scripts/dwca/verify-artifact.ts"
      to: "dist/dwca/occurrence.txt"
      via: "read file + split header row to resolve columns by name"
      pattern: "occurrence\\.txt"
---

<objective>
Build the artifact-level verification surface for Phase 13: a `scripts/dwca/verify-artifact.ts` CLI that parses the built `dist/dwca/occurrence.txt` and `dist/dwca/eml.xml` and asserts SC#2 (no iNat/HappyWhale occurrenceIDs), SC#3 (institutionCode/rightsHolder/datasetName/recordedBy correctness), and SC#4 (EML v1.3 title). Also run the 5 read-only prod-DB checklist queries and record their results.

Purpose: SC#2/SC#3/SC#4 evidence must come from the built artifact, not a `SELECT` against the view (CONTEXT D-02 + specifics). This plan creates the deterministic, testable verifier that the Wave 2 build run consumes, and captures the DB-side checklist (PITFALLS.md items 3–6, 10) as the read-only invariant baseline.

Output: `verify-artifact.ts` + its unit test (fixtures, no build/DB needed), and `13-CHECKLIST.md` recording the prod-DB query results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/phases/13-verification-gbif-re-validation/13-CONTEXT.md
@.planning/phases/13-verification-gbif-re-validation/13-RESEARCH.md
@scripts/dwca/fields.ts
@scripts/dwca/verify-publish.ts
@scripts/dwca/build.ts
</context>

<artifacts_this_phase_produces>
New symbols created across Phase 13 (source-grounding exclusion list for drift verification):
- `scripts/dwca/verify-artifact.ts` — exports `verifyArtifact`, `buildHeaderIndex`, `main` (THIS PLAN)
- `scripts/dwca/verify-artifact.test.ts` (THIS PLAN)
- `.planning/phases/13-verification-gbif-re-validation/13-CHECKLIST.md` (THIS PLAN)
- `scripts/dwca/validate-gbif.ts` — exports `submitValidation`, `pollValidation`, `assertIndexeable`, `main` (Plan 13-02)
- `scripts/dwca/validate-gbif.test.ts` (Plan 13-02)
- `.planning/phases/13-verification-gbif-re-validation/13-VERIFICATION.md` (Plan 13-03)
- POSSIBLE migration `supabase/migrations/<ts>_maplify_coordinate_uncertainty.sql` — ONLY if the validator output makes the optional D-04 derivation worth shipping (Plan 13-03, conditional)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create verify-artifact.ts — artifact-level SC#2/SC#3/SC#4 assertions</name>
  <files>scripts/dwca/verify-artifact.ts, scripts/dwca/verify-artifact.test.ts</files>
  <read_first>
    - scripts/dwca/fields.ts — OCCURRENCE_FIELDS is the canonical ordered contract; build the column name→index map from it, NEVER hardcode integer indices (Pitfall 6 off-by-one). occurrenceID is index 0, recordedBy 18, institutionCode 19, rightsHolder 20, datasetName 21.
    - scripts/dwca/verify-publish.ts — existing CLI verifier pattern: pure module, `export async function main`, `if (import.meta.url === ...) main().catch(... process.exit(1))`. Mirror this structure (Claude's-discretion D: a NEW script is chosen over extending verify-publish.ts, because verify-publish is HTTP-only and "NEVER ATTACHes / reads disk" per its header — artifact parsing is a distinct concern).
    - scripts/dwca/build.ts — confirms occurrence.txt is tab-delimited, HEADER true, QUOTE '', NULLSTR '' (so empty field = NULL). OUT_DIR = 'dist/dwca'; occurrence.txt path is dist/dwca/occurrence.txt; eml.xml lives inside the zip but build also has the EML string — for the title check, parse the eml.xml entry OR accept a path.
    - .planning/research/PITFALLS.md §"Looks Done But Isn't Checklist" items 4/5/6/10 — the exact assertion targets.
    - .planning/ROADMAP.md Phase 13 SC#2/SC#3/SC#4 wording.
  </read_first>
  <behavior>
    - buildHeaderIndex(headerLine): splits the TSV header on '\t', returns Map<columnName, index>; throws if any name in OCCURRENCE_FIELDS is absent from the header (drift guard).
    - verifyArtifact reads occurrence.txt (path arg, default dist/dwca/occurrence.txt):
      - Test SC#2: zero data rows whose occurrenceID (column by name) matches /^(inaturalist|happywhale):/ — else throw with the first offending occurrenceID and the count.
      - Test SC#3a: every data row's institutionCode === 'SalishSea' — else throw with the count + a sample distinct-value list.
      - Test SC#3b: every data row's rightsHolder === 'SalishSea.io' — else throw with distinct offending values.
      - Test SC#3c: every data row's datasetName starts with 'SalishSea.io — ' — else throw with the count + sample.
      - Test SC#3d (spot-check, non-fatal report): emit to stdout up to 5 sample maplify-prefixed rows showing recordedBy is either a human-name string or empty (NULL) — never an opaque code like 'whalealertoa'. This is reported, asserted only that recordedBy is not equal to any of the known opaque source codes ('whalealertoa','whalealertak','rwsas').
      - Test SC#4: given an eml.xml string/path, assert it contains `<title>SalishSea.io Cetacean Occurrences (v1.3)</title>`.
    - main(): runs verifyArtifact against dist/dwca/occurrence.txt + dist/dwca/eml.xml (extracted from the zip OR a path passed via argv/env); logs OK lines per check; exits non-zero on any thrown assertion.
    - Unit test (verify-artifact.test.ts): drives buildHeaderIndex + verifyArtifact against INLINE fixture strings (a valid 26-col header + a few rows, plus a row with an `inaturalist:` occurrenceID, plus a row with a wrong institutionCode). NO build, NO DB. Tests must run green under `npm test` immediately.
  </behavior>
  <action>
    Create `scripts/dwca/verify-artifact.ts` as a pure-plus-CLI module mirroring verify-publish.ts's shape. Export `buildHeaderIndex(headerLine: string): Map<string, number>`, `verifyArtifact(opts: { occurrencePath?: string; emlXml?: string; emlPath?: string }): Promise<void>` (or sync over already-read content for testability — prefer a pure `assertOccurrenceRows(headerLine, dataLines)` + `assertEmlTitle(xml)` core that `verifyArtifact` wraps with fs reads), and `main()`. Resolve columns BY NAME via `buildHeaderIndex` keyed off `OCCURRENCE_FIELDS` from `./fields.ts` — do NOT hardcode `$20`-style indices (Pitfall 6). Tab-delimited parse: split each line on '\t'; an empty field is NULL (build.ts uses NULLSTR ''). The known opaque-code denylist for the recordedBy spot-check is `['whalealertoa','whalealertak','rwsas']`. The v1.3 title literal is `SalishSea.io Cetacean Occurrences (v1.3)` (matches eml.test.ts mock + dwc.datasets v1.3 bump). Add `if (import.meta.url === \`file://${process.argv[1]}\`)` CLI guard with `process.exit(1)` on throw. Write `verify-artifact.test.ts` with inline fixtures covering: the happy path (all green), an `inaturalist:`-prefixed occurrenceID (SC#2 throws), a wrong institutionCode row (SC#3a throws), a datasetName missing the prefix (SC#3c throws), and an eml string missing the v1.3 title (SC#4 throws).
  </action>
  <verify>
    <automated>npm test -- scripts/dwca/verify-artifact.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/dwca/verify-artifact.ts` exists and exports `buildHeaderIndex`, a row-assertion core, and `main`.
    - Source assertion: the file imports `OCCURRENCE_FIELDS` from `./fields.ts` (`grep -v '^[[:space:]]*//' scripts/dwca/verify-artifact.ts | grep -c "OCCURRENCE_FIELDS"` ≥ 1) and contains NO bare numeric occurrence-column literals like `[19]`/`[20]`/`$20` used for column selection.
    - Behavior assertion: `npm test -- scripts/dwca/verify-artifact.test.ts` is green; the suite includes at least one case where an `inaturalist:` occurrenceID causes a thrown error and one where a wrong `institutionCode` causes a thrown error.
    - Behavior assertion: the v1.3 title literal `SalishSea.io Cetacean Occurrences (v1.3)` appears in the SC#4 assertion (`grep -c "Cetacean Occurrences (v1.3)" scripts/dwca/verify-artifact.ts` ≥ 1).
  </acceptance_criteria>
  <done>verify-artifact.ts exists with name-keyed column resolution and a green fixture-driven test suite; SC#2/SC#3/SC#4 assertions are implemented and proven to throw on bad input.</done>
</task>

<task type="auto">
  <name>Task 2: Run + record the 5 read-only prod-DB checklist queries</name>
  <files>.planning/phases/13-verification-gbif-re-validation/13-CHECKLIST.md</files>
  <read_first>
    - .planning/research/PITFALLS.md §"Looks Done But Isn't Checklist" — items 3 (SRC-01 row-count), 4 (institutionCode), 5 (rightsHolder), 6 (datasetName), 10 (occurrenceID-prefix scan). These are the DB-side checks. Items 1/2/8/9/11/12 are out of scope per 13-RESEARCH.md Pattern 2 (Phase 11 / not-this-phase).
    - .planning/phases/13-verification-gbif-re-validation/13-RESEARCH.md §"Pattern 2 — DB-side query templates" — copy the exact SQL (SRC-01 trio, institutionCode DISTINCT, rightsHolder DISTINCT, datasetName DISTINCT, occurrenceID-prefix COUNT).
    - MEMORY: prod DB is reached read-only via `npx supabase db query --linked` (Management API + keychain token, no DB_PASSWORD), JSON output → pipe to `jq`. This phase is READ-ONLY on the DB — no writes, `comments` immutability is an invariant (do not run any UPDATE).
  </read_first>
  <action>
    Run these five read-only queries against prod via `npx supabase db query --linked` (JSON out; parse with `jq`), and record each query + its exact result in `13-CHECKLIST.md` as a markdown table with a PASS/FAIL column:
    (1) SRC-01 — `SELECT COUNT(*) FROM dwc.occurrences;`, `SELECT COUNT(*) FROM public.observations;`, and `SELECT COUNT(*) FROM maplify.sightings WHERE trusted AND NOT is_test AND number_sighted BETWEEN 1 AND 1000 AND source != 'rwsas';`. PASS iff dwc count ≤ obs + maplify-trusted count.
    (2) institutionCode — `SELECT DISTINCT "institutionCode" FROM dwc.occurrences;`. PASS iff exactly `{'SalishSea'}`.
    (3) rightsHolder — `SELECT DISTINCT "rightsHolder" FROM dwc.occurrences;`. PASS iff exactly `{'SalishSea.io'}`.
    (4) datasetName — `SELECT DISTINCT "datasetName" FROM dwc.occurrences ORDER BY 1;`. PASS iff ≥10 distinct values AND every value begins with `SalishSea.io — `.
    (5) occurrenceID-prefix — `SELECT COUNT(*) FROM dwc.occurrences WHERE "occurrenceID" LIKE 'inaturalist:%' OR "occurrenceID" LIKE 'happywhale:%';`. PASS iff 0.
    Record the actual numbers/value-sets returned (not just PASS/FAIL) so the artifact-level run in Plan 13-03 can be reconciled against the view-level baseline. If any query FAILS, do NOT remediate here — note it as a defect for Plan 13-03 inline remediation (D-06).
  </action>
  <verify>
    <automated>test -f .planning/phases/13-verification-gbif-re-validation/13-CHECKLIST.md && grep -qE "SRC-01|institutionCode|rightsHolder|datasetName|occurrenceID" .planning/phases/13-verification-gbif-re-validation/13-CHECKLIST.md && grep -qiE "PASS|FAIL" .planning/phases/13-verification-gbif-re-validation/13-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `13-CHECKLIST.md` exists and contains all five checklist rows (SRC-01, institutionCode, rightsHolder, datasetName, occurrenceID-prefix), each with the exact query, the actual returned values, and a PASS/FAIL verdict.
    - Behavior assertion: the recorded `SELECT DISTINCT "institutionCode"` result is exactly `{'SalishSea'}` and `rightsHolder` is exactly `{'SalishSea.io'}` (or, if not, a FAIL is explicitly recorded and flagged for 13-03 remediation).
    - Source assertion: no `UPDATE`/`INSERT`/`DELETE`/`ALTER` statement appears in 13-CHECKLIST.md (read-only discipline; `comments` immutability invariant).
  </acceptance_criteria>
  <done>All five DB-side checklist items are recorded with actual prod values and PASS/FAIL verdicts; any FAIL is flagged (not fixed here) for Plan 13-03 inline remediation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → prod DB (read-only) | `npx supabase db query --linked` crosses into prod; must remain read-only |
| built artifact → verifier | occurrence.txt / eml.xml are untrusted file content parsed by verify-artifact.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-RO | Tampering | prod DB checklist queries | mitigate | Read-only SQL only via `--linked`; no UPDATE/INSERT/DELETE/ALTER (asserted by the no-write grep in Task 2); `comments` column never touched |
| T-13-01-PARSE | Tampering | verify-artifact.ts TSV parse | mitigate | Resolve columns by NAME via header map (not positional literals); throw on header drift — a shifted column cannot silently pass a check |
| T-13-01-IDX | Information disclosure | verify-artifact stdout sample rows | accept | Spot-check prints occurrence rows already destined for public GBIF export; no secrets in occurrence.txt |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; all tooling pre-existing (13-RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npm test -- scripts/dwca/verify-artifact.test.ts` green (fixture-driven, no build/DB).
- `npm test` remains green overall (no regressions to fields/eml/meta tests).
- `13-CHECKLIST.md` records all five DB-side checklist results with PASS/FAIL.
</verification>

<success_criteria>
- verify-artifact.ts exists, resolves columns by name from OCCURRENCE_FIELDS, and its unit suite is green.
- SC#2/SC#3/SC#4 assertions are implemented and proven to throw on bad fixture input.
- The five read-only prod-DB checklist queries are recorded with actual values + verdicts; no DB writes occurred.
</success_criteria>

<output>
Create `.planning/phases/13-verification-gbif-re-validation/13-01-SUMMARY.md` when done.
</output>
