---
phase: 13-verification-gbif-re-validation
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - .planning/phases/13-verification-gbif-re-validation/13-VERIFICATION.md
  - scripts/dwca/eml.ts
  - scripts/dwca/eml.test.ts
  - supabase/migrations
autonomous: false
requirements: [ATTR-05]
user_setup:
  - service: gbif
    why: "Validator run needs GBIF.org Basic-auth credentials (D-01)"
    env_vars:
      - name: GBIF_USER
        source: "GBIF.org account username"
      - name: GBIF_PASS
        source: "GBIF.org account password"
  - service: supabase
    why: "Fresh local build (D-02) connects DuckDB to prod DB; an OPTIONAL coordinateUncertaintyInMeters migration would require a schema push"
    env_vars:
      - name: SUPABASE_DB_URL
        source: "Prod Postgres DSN via the IPv4 session pooler (postgres.<ref>@aws-1-us-west-1.pooler.supabase.com:5432/postgres); assemble from DB_PASSWORD per project memory — never commit"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Only if the optional coordinateUncertaintyInMeters migration ships — needed for non-TTY `supabase db push`"
must_haves:
  truths:
    - "A fresh local archive is built with `npm run build:dwca` against the prod DB (D-02), producing dist/dwca/salishsea-occurrences-v1.zip + occurrence.txt + eml.xml"
    - "verify-artifact.ts runs green against the fresh build: SC#2 (no iNat/HappyWhale occurrenceIDs), SC#3 (institutionCode/rightsHolder/datasetName), SC#4 (EML v1.3 title)"
    - "validate-gbif.ts (or the manual-upload fallback) returns indexeable=true with zero RESOURCE_INTEGRITY/RESOURCE_STRUCTURE issues (SC#1)"
    - "The GBIF validator's actual warning set is captured and reconciled against the EML-contact and coordinateUncertainty decisions (D-03/D-04)"
    - "13-VERIFICATION.md records the local-build ↔ nightly-archive equivalence (identical pipeline) and all SC#1–SC#4 evidence"
    - "npm test is green before AND after any inline remediation, with the full field-contract (26 fields) intact"
  artifacts:
    - path: ".planning/phases/13-verification-gbif-re-validation/13-VERIFICATION.md"
      provides: "End-to-end SC#1–SC#4 evidence: build provenance, artifact-verifier output, GBIF validator result, warning reconciliation, local↔nightly equivalence note"
    - path: "scripts/dwca/eml.ts"
      provides: "EML contact — enriched ONLY if the validator flags RESOURCE_CONTACTS_MISSING_OR_INCOMPLETE (D-03, conditional)"
  key_links:
    - from: "npm run build:dwca"
      to: "scripts/dwca/verify-artifact.ts"
      via: "verifier parses the freshly-built dist/dwca/occurrence.txt"
      pattern: "occurrence\\.txt"
    - from: "scripts/dwca/validate-gbif.ts"
      to: "GBIF validator API"
      via: "submit the freshly-built zip + assert indexeable"
      pattern: "salishsea-occurrences-v1\\.zip"
---

<objective>
Run the end-to-end Phase 13 verification: build a fresh local archive against prod (D-02), run the artifact verifier (Plan 13-01) and the GBIF validator (Plan 13-02), reconcile the validator's actual warnings, and inline-remediate ONLY what the validator surfaces (D-06): EML contact enrichment if `RESOURCE_CONTACTS_MISSING_OR_INCOMPLETE` fires (D-03), and the OPTIONAL Maplify `coordinateUncertaintyInMeters` honest-derivation if (and only if) the result makes it worthwhile (D-04). Record everything in 13-VERIFICATION.md. Closing this plan green resolves ATTR-05 and the overarching v1.3 attribution milestone todo.

D-05 disposition (already satisfied — no task needed): D-05 asks for `coordinateUncertaintyInMeters` as a gated field-contract change (fields.ts → migration → meta.xml → round-trip tests, `npm test` green before validation). Per 13-RESEARCH Pitfall 1, Phase 12 ALREADY shipped this — the field sits at index 14 in `fields.ts`, the dwc views emit it, `fields.test.ts` expects 26 fields, and `npm test` is green. The ONLY residual D-05/D-04 work is the OPTIONAL Maplify VALUE derivation (Task 3, gated on validator output) — NOT a fresh field-contract change. The `npm test`-green-before-validation guarantee D-05 requires is enforced here: Task 1 builds only after the existing 26-field suite is green, and Task 3 keeps it green after any remediation.

Purpose: SC#1–SC#4 must be proven against the built artifact, not the views — and remediation is decided by real validator output, not speculatively (13-RESEARCH Open Questions 1 & 2). This is the phase gate.

Output: 13-VERIFICATION.md (evidence) + any validator-driven remediation (EML contact and/or optional Maplify coordinateUncertainty migration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/phases/13-verification-gbif-re-validation/13-CONTEXT.md
@.planning/phases/13-verification-gbif-re-validation/13-RESEARCH.md
@.planning/phases/12-dwc-view-rebuild/12-CONTEXT.md
@scripts/dwca/build.ts
@scripts/dwca/eml.ts
@scripts/dwca/eml.test.ts
@scripts/dwca/guard.ts
@supabase/migrations/20260617203900_dwc_schema.sql
</context>

<artifacts_this_phase_produces>
See Plan 13-01's `<artifacts_this_phase_produces>` for the full phase list. This plan creates:
- `.planning/phases/13-verification-gbif-re-validation/13-VERIFICATION.md`
- CONDITIONAL: an EML contact sub-element in `scripts/dwca/eml.ts` (+ `eml.test.ts` assertion) — only if the validator flags RESOURCE_CONTACTS_MISSING_OR_INCOMPLETE
- CONDITIONAL: `supabase/migrations/<ts>_maplify_coordinate_uncertainty.sql` — only if the user opts into the optional D-04 Maplify derivation after seeing validator output
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Fresh local build + artifact verifier + GBIF validator run, recorded in 13-VERIFICATION.md</name>
  <files>.planning/phases/13-verification-gbif-re-validation/13-VERIFICATION.md</files>
  <read_first>
    - scripts/dwca/build.ts — the build pipeline. Step 1 requires `SUPABASE_DB_URL`; Step 6/7 run `assertFieldAlignment` (F-02) so a 26-col view↔array mismatch fails before COPY; outputs land in dist/dwca/ (occurrence.txt, eml.xml inside the zip, salishsea-occurrences-v1.zip, .parquet). build.ts Step 15.5 PG refs are pgdb-qualified (static guard f716d9a) — do not regress.
    - scripts/dwca/verify-artifact.ts (created in 13-01) — the SC#2/SC#3/SC#4 artifact assertions to run against the fresh build.
    - scripts/dwca/validate-gbif.ts (created in 13-02) — the SC#1 submit/poll/assert client; needs GBIF_USER/GBIF_PASS.
    - .planning/phases/13-verification-gbif-re-validation/13-RESEARCH.md §"Pattern 5: Build Mechanics" (SUPABASE_DB_URL via pooler) and §"Common Pitfalls 3/4" (GBIF auth + offline-fallback).
    - MEMORY: prod DSN via IPv4 session pooler; never commit the DSN; pushing `main` also runs `supabase db push` in CI.
  </read_first>
  <action>
    0. Confirm `npm test` is green first (the 26-field contract / D-05 precondition) before building.
    1. Set `SUPABASE_DB_URL` to the prod DSN (IPv4 session pooler per project memory; assembled from DB_PASSWORD; never echoed/committed) and run `npm run build:dwca`. Confirm the F-02 alignment guard passed (26-col view↔OCCURRENCE_FIELDS parity) and dist/dwca/{salishsea-occurrences-v1.zip, occurrence.txt, .parquet} exist. Extract eml.xml from the zip (or capture it) for the SC#4 check.
    2. Run the artifact verifier against the fresh build: `node scripts/dwca/verify-artifact.ts` (pointed at dist/dwca/occurrence.txt + the extracted eml.xml). Record PASS/FAIL for SC#2, SC#3a/b/c, SC#3d spot-check sample, SC#4.
    3. Run the GBIF validator: with GBIF_USER/GBIF_PASS set, run `node scripts/dwca/validate-gbif.ts` against the fresh zip. Capture the full result: `indexeable`, the blocking-category check (must be zero RESOURCE_INTEGRITY/RESOURCE_STRUCTURE), and the complete non-blocking warning list (esp. whether RESOURCE_CONTACTS_MISSING_OR_INCOMPLETE and any COORDINATE_UNCERTAINTY_* warning appear). If the API is unreachable after retries, use the manual fallback (upload the zip at gbif.org/tools/data-validator) and transcribe the result + screenshot reference.
    4. Reconcile the artifact-level results against the Plan 13-01 DB-side checklist values (13-CHECKLIST.md) — note any divergence between view and artifact (there should be none; flag if there is).
    5. Write `13-VERIFICATION.md`: build provenance (commit SHA, build timestamp, row count), the explicit local-build ↔ nightly-archive equivalence statement (identical `scripts/dwca/build.ts` pipeline from prod DB + committed code, per CONTEXT D-02 planner note), the SC#1–SC#4 evidence table, and the captured GBIF warning set with a per-warning disposition (blocking → must-fix in Task 2/3; non-blocking → accept or conditionally fix). If any SC FAILS, record it as a defect to remediate inline (D-06) before the phase closes.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-verification-gbif-re-validation/13-VERIFICATION.md && grep -qiE "indexeable" .planning/phases/13-verification-gbif-re-validation/13-VERIFICATION.md && grep -qE "SC#1|SC#2|SC#3|SC#4" .planning/phases/13-verification-gbif-re-validation/13-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:dwca` completed and `dist/dwca/salishsea-occurrences-v1.zip` + `dist/dwca/occurrence.txt` exist (the F-02 26-col alignment guard passed).
    - Behavior assertion: the artifact verifier output is recorded with SC#2=PASS (zero iNat/HappyWhale occurrenceIDs in occurrence.txt), SC#3 PASS (institutionCode='SalishSea', rightsHolder='SalishSea.io', every datasetName prefixed 'SalishSea.io — '), SC#4 PASS (EML title is the v1.3 string).
    - Behavior assertion: 13-VERIFICATION.md records the GBIF result with `indexeable` value and an explicit statement that zero RESOURCE_INTEGRITY/RESOURCE_STRUCTURE issues were present (SC#1), plus the full non-blocking warning list with dispositions.
    - Source assertion: 13-VERIFICATION.md contains the local-build↔nightly-archive equivalence note (identical pipeline) so the ROADMAP "nightly-regenerated archive" wording is provably satisfied.
  </acceptance_criteria>
  <done>A fresh archive is built and validated; SC#1–SC#4 evidence (artifact verifier + GBIF result + reconciliation + equivalence note) is recorded in 13-VERIFICATION.md; any failing SC is flagged for inline remediation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: GBIF validator result review + remediation decision (D-03/D-04/D-06)</name>
  <files>.planning/phases/13-verification-gbif-re-validation/13-VERIFICATION.md</files>
  <what-built>
    A fresh DwC-A archive was built from prod and submitted to the GBIF validator (Task 1). 13-VERIFICATION.md now holds the `indexeable` verdict, the blocking-category check (SC#1), and the complete non-blocking warning list — including whether `RESOURCE_CONTACTS_MISSING_OR_INCOMPLETE` (D-03 driver) and any `COORDINATE_UNCERTAINTY_*` warning (D-04 driver) actually fired.
  </what-built>
  <action>
    This is a human-verify checkpoint — no automation; the operator reviews the recorded validator result and chooses the remediation set for Task 3. Present the `indexeable` verdict, the SC#1 blocking-category check, and the non-blocking warning list from 13-VERIFICATION.md, then pause for the operator's decision per the resume-signal. Do NOT apply any code/migration change in this task.
  </action>
  <how-to-verify>
    1. Open `.planning/phases/13-verification-gbif-re-validation/13-VERIFICATION.md`.
    2. Confirm SC#1: `indexeable: true` AND zero RESOURCE_INTEGRITY / RESOURCE_STRUCTURE issues. This is the hard gate — if `indexeable` is false or any structural/integrity issue is present, this is a blocking defect that MUST be remediated inline before phase close (D-06).
    3. Review the non-blocking warning list and decide remediation:
       - If `RESOURCE_CONTACTS_MISSING_OR_INCOMPLETE` is present → approve the D-03 EML contact enrichment in Task 3 (note which sub-element the validator named, e.g. positionName).
       - If absent → the contact already meets GBIF minimums; skip the EML edit (per 13-RESEARCH: the current contact has name+org+email and may not trigger the warning at all).
       - For any `coordinateUncertaintyInMeters` warning: D-04 is honest-derivation-only and the optional Maplify derivation is DEFERRED by default (CONTEXT deferred list). Decide whether to (a) accept the warning (NULL is honest; warning is non-blocking) — the default — or (b) opt into the optional Maplify decimal-place derivation migration in Task 3 (requires a prod trailing-zero census first per Pitfall 2).
    4. Record your decisions (which remediations to apply, which to defer/accept) for Task 3.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the SC#1 gate state and selects the remediation set (none | eml-contact | maplify-coord | both) per the resume-signal.</human-check>
  </verify>
  <resume-signal>Type "approved — apply: [none | eml-contact | maplify-coord | both]" or describe blocking issues to fix first</resume-signal>
  <done>The operator has reviewed SC#1 and recorded a remediation decision for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Conditional inline remediation per the Task 2 decision (EML contact and/or optional Maplify coordinateUncertainty)</name>
  <files>scripts/dwca/eml.ts, scripts/dwca/eml.test.ts, supabase/migrations</files>
  <read_first>
    - scripts/dwca/eml.ts — the `<contact>` block (givenName/surName hardcoded, organizationName 'SalishSea.io', electronicMailAddress from d.contact_email). The EML edit, if any, adds ONE sub-element (e.g. `<positionName>Data Manager</positionName>`) per the validator's named gap — xmlEsc applies to free text; a hardcoded literal needs no new escaping. Placement per GBIF EML 2.1.1 sequence.
    - scripts/dwca/eml.test.ts — existing contact assertions (givenName 'Peter', surName 'Abrahamsen', org, email). Add an assertion for the new sub-element if one is added.
    - supabase/migrations/20260617203900_dwc_schema.sql — the dwc view definitions. The OPTIONAL D-04 derivation changes `dwc._maplify_occurrences` ONLY: replace `NULL::integer AS "coordinateUncertaintyInMeters"` with the decimal-place→meters CASE (13-RESEARCH Pattern 4 Option B). This is a NEW migration file, never an edit to the shipped one. fields.ts already has the field at index 14 (Phase 12 / D-05 already shipped) — do NOT touch fields.ts/meta.xml/fields.test.ts (Pitfall 1: the field-contract change is already done).
    - .planning/phases/13-verification-gbif-re-validation/13-RESEARCH.md §"Pattern 4" (incl. Pitfall 2 trailing-zero census + the `::numeric(15,7)` mitigation) and §"Pattern 3" (EML contact).
    - MEMORY: pushing `main` runs `supabase db push` in CI; a local pre-prod `supabase db push` is still required so verification reflects the live DB.
  </read_first>
  <action>
    Apply ONLY the remediations approved in Task 2. If "none", make no code/migration change — record in 13-VERIFICATION.md that no remediation was required and proceed to re-verify.

    IF eml-contact: add the single EML contact sub-element the validator named (e.g. `<positionName>Data Manager</positionName>`) to the `<contact>` block in eml.ts, placed per the GBIF EML 2.1.1 contact sequence; add a matching assertion to eml.test.ts; run `npm test -- scripts/dwca/eml.test.ts` green. No migration; metadata-only (D-03).

    IF maplify-coord: FIRST run the prod trailing-zero census (read-only): `npx supabase db query --linked` with `SELECT gis.ST_Y(s.location::gis.geometry)::text AS lat_txt, COUNT(*) FROM maplify.sightings s WHERE s.trusted GROUP BY 1 ORDER BY 2 DESC LIMIT 20;` and record it in 13-VERIFICATION.md to confirm decimal places are reliably detectable (Pitfall 2). THEN create a NEW migration `supabase/migrations/<ts>_maplify_coordinate_uncertainty.sql` that `CREATE OR REPLACE`s `dwc._maplify_occurrences` (and re-creates the `dwc.occurrences` UNION if Postgres requires it for type parity) replacing `NULL::integer AS "coordinateUncertaintyInMeters"` with the decimal-place CASE: ≤2 dp → 1111, ≤4 dp → 11, else NULL — using a precision-preserving cast (`::numeric(15,7)::text`) so trailing zeros aren't stripped. Keep all OTHER 26 columns byte-identical to the current view (do not alter the field contract). This is a `[BLOCKING]` migration: after writing it, run a LOCAL `supabase db push` (non-TTY: ensure SUPABASE_ACCESS_TOKEN; otherwise pause for the operator) so the prod-targeted verification reflects the live DB, then re-run the build + validator. `npm test` (incl. build-queries.test.ts pgdb-qualification + fields.test.ts 26-field) MUST stay green.

    AFTER any remediation: re-run `npm run build:dwca`, re-run verify-artifact.ts, and re-run validate-gbif.ts; update 13-VERIFICATION.md with the post-remediation SC#1–SC#4 evidence. Confirm the chosen warning(s) cleared (or, for D-04 NULL rows, that the warning only partially cleared as accepted). Finally, mark the v1.3 attribution milestone todo (`2026-06-17-model-embedded-dataset-attributions-as-first-class-sources.md`) resolvable per STATE.md once all four SCs are green.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `npm test` is green AFTER remediation, including fields.test.ts (`OCCURRENCE_FIELDS.length === 26`), build-queries.test.ts (pgdb-qualified refs), and — if the EML edit was applied — the new contact sub-element assertion in eml.test.ts.
    - Source assertion (IF eml-contact applied): the named sub-element appears in eml.ts inside the `<contact>` block AND has a matching `expect(...).toContain(...)` in eml.test.ts; fields.ts is unchanged.
    - Source assertion (IF maplify-coord applied): a NEW `supabase/migrations/*_maplify_coordinate_uncertainty.sql` exists that `CREATE OR REPLACE`s `dwc._maplify_occurrences` with the decimal-place CASE; the shipped `20260617203900_dwc_schema.sql` and `fields.ts`/`meta-xml.ts`/`fields.test.ts` are NOT modified (Pitfall 1); the migration is pushed locally before re-verification.
    - Behavior assertion: post-remediation 13-VERIFICATION.md shows SC#1 still PASS (indexeable=true, zero blocking issues) and the targeted warning's disposition (cleared / partially cleared / accepted) is recorded.
    - IF "none": 13-VERIFICATION.md explicitly records that no remediation was required and all four SCs are green on the un-remediated archive.
  </acceptance_criteria>
  <done>The approved remediations (if any) are applied with npm test green; the archive is re-built and re-validated; 13-VERIFICATION.md reflects the final SC#1–SC#4 state; the v1.3 attribution milestone todo is marked resolvable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → prod DB (read-only) | `npm run build:dwca` ATTACHes prod via SUPABASE_DB_URL; read-only |
| local → GBIF API (authenticated) | validator run sends GBIF_USER/GBIF_PASS over HTTPS |
| migration → prod DB (write) | ONLY if the optional coordinateUncertainty migration ships — `CREATE OR REPLACE VIEW` + `supabase db push` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-DSN | Information disclosure | SUPABASE_DB_URL during build | mitigate | build.ts maskDsn scrubs the DSN from all error paths; never echo the DSN; never commit it (project memory pattern) |
| T-13-03-CRED | Information disclosure | GBIF_USER/GBIF_PASS during validator run | mitigate | env-sourced; validate-gbif.ts never logs the Authorization header (enforced in 13-02) |
| T-13-03-VIEWONLY | Tampering | optional coordinateUncertainty migration | mitigate | View-only `CREATE OR REPLACE` (no source-table write, `comments` untouched); preserves SRC-01-by-construction (exactly two UNION branches, no third source); local `supabase db push` + re-run guard before trusting prod |
| T-13-03-FIELDLOCK | Tampering | field-contract surface | mitigate | Pitfall 1: do NOT touch fields.ts/meta.xml/fields.test.ts (field already at index 14); `npm test` 26-field gate is the guardrail |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (13-RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npm run build:dwca` succeeds (F-02 26-col alignment guard passes).
- verify-artifact.ts green against the fresh build (SC#2/SC#3/SC#4).
- validate-gbif.ts (or manual fallback) shows indexeable=true, zero RESOURCE_INTEGRITY/RESOURCE_STRUCTURE issues (SC#1).
- `npm test` green before and after any remediation (26-field contract intact, pgdb refs intact).
- 13-VERIFICATION.md records SC#1–SC#4 evidence + the local↔nightly equivalence note + warning dispositions.
</verification>

<success_criteria>
- The corrected archive passes the GBIF validator with no blocking/structural errors (SC#1 / ATTR-05).
- occurrence.txt has zero iNat/HappyWhale occurrenceIDs (SC#2); Maplify rows carry correct attribution in the artifact (SC#3); EML lists orgs as associatedParty with the v1.3 title (SC#4).
- Any validator-driven remediation is applied inline with npm test green; deferred/accepted warnings are documented.
- The overarching v1.3 attribution milestone todo is marked resolvable.
</success_criteria>

<output>
Create `.planning/phases/13-verification-gbif-re-validation/13-03-SUMMARY.md` when done.
</output>
