---
phase: 11-resolution-backfill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-resolution-backfill/maplify_census.tsv
autonomous: false
requirements: [RESOLVE-03, RESOLVE-02]
must_haves:
  truths:
    - "A read-only prod SELECT DISTINCT census of every Maplify bracket tag, trailing attribution, and source code (with row counts) is committed as an artifact (D-08)"
    - "The census ran read-only against prod — no INSERT/UPDATE/DELETE was issued, and maplify.sightings.comments was not mutated (SC#2)"
    - "A human has confirmed the expansion + collection-slug mapping for every census bracket tag, including the 11 Phase-9 acronym stubs (D-09/D-10), before any collection_rule seed row is written"
    - "Each census row is annotated with the target collection slug it will map to, or explicitly marked stay-NULL (empty/[NULL] brackets per D-09)"
  artifacts:
    - path: ".planning/phases/11-resolution-backfill/maplify_census.tsv"
      provides: "Authoritative prod census: match_kind, match_value, count, + human-confirmed target collection slug per row"
      min_lines: 10
  key_links:
    - from: ".planning/phases/11-resolution-backfill/maplify_census.tsv"
      to: "maplify.collection_rule seed (plan 11-03)"
      via: "human-confirmed slug column feeds the seed rows"
      pattern: "collection|slug"
---

<objective>
Run the full prod `SELECT DISTINCT` census (read-only) of Maplify bracket tags, trailing "Trusted Observer" attributions, and structured `source` codes, commit the raw output as an artifact, and obtain HUMAN confirmation of every acronym expansion + target collection slug BEFORE any `collection_rule` seed row is written (D-08, D-09, D-10). This is the keystone gate: STATE.md says "do not skip" the census, and the 11 acronym expansions (PSWS, MCW, CWW, WSSJI, HIWS, SBW, WA, SSCH, SA, PSWW, Bremerton) are LOW-confidence community knowledge that must come from a human, never automated expansion.

Purpose: The census is the authoritative starting point for the dictionary (RESOLVE-02) and the source data for the diff-gate (RESOLVE-03). Seeding rules from un-verified acronym guesses would misattribute collections in the GBIF export.
Output: `.planning/phases/11-resolution-backfill/maplify_census.tsv` with a human-confirmed target-collection-slug column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-resolution-backfill/11-CONTEXT.md
@.planning/phases/11-resolution-backfill/11-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Created by THIS plan:
- File `.planning/phases/11-resolution-backfill/maplify_census.tsv` — committed prod census with human-confirmed slug mapping per row.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run read-only prod census and commit the raw artifact</name>
  <files>.planning/phases/11-resolution-backfill/maplify_census.tsv</files>
  <read_first>
    - .planning/phases/11-resolution-backfill/11-RESEARCH.md ("Production Census: How to Run Safely" — the exact SELECT DISTINCT query, the read-only guarantee, the inline DSN assembly)
    - Auto-memory: Supabase DSN assembled inline from DB_PASSWORD + SUPABASE_PROJECT_ID (project ref grztmjpzamcxlzecmqca) — do NOT add a SUPABASE_DB_URL secret
    - .planning/v1.3-EXECUTIVE-SUMMARY.md §3 (the prior Maplify bracket-tag + attribution census — sanity-check the new counts against it)
  </read_first>
  <action>
    Assemble the prod DSN INLINE per memory: `postgresql://postgres:${DB_PASSWORD}@db.grztmjpzamcxlzecmqca.supabase.co:5432/postgres` (obtain DB_PASSWORD from the operator / GitHub Actions production environment; do NOT persist a new secret). Run the RESEARCH "Production Census" query — a single read-only `SELECT match_kind, match_value, count(*) ... FROM maplify.sightings GROUP BY 1,2 ORDER BY 3 DESC` that classifies each row as `bracket` (regex `^\[([^\]]+)\]`), `attribution` (`Trusted Observer`), or `source-only`. Use ONLY SELECT — no DDL/DML. If `DB_PASSWORD` is not available in the environment, STOP and surface a blocking note to the operator (this is why the plan is non-autonomous). Pipe the result to `.planning/phases/11-resolution-backfill/maplify_census.tsv` in TSV form (psql `--tuples-only --field-separator=$'\t' --no-align` or `\copy ... TO`). Add a header comment block at the top of the file recording: the date, the read-only guarantee, the exact query used, and a `target_collection_slug` column (left blank in this task; filled in Task 2). Do NOT connect through the connection pooler — use direct port 5432.
  </action>
  <verify>
    <automated>test -s .planning/phases/11-resolution-backfill/maplify_census.tsv &amp;&amp; grep -qiE "bracket|attribution|source" .planning/phases/11-resolution-backfill/maplify_census.tsv &amp;&amp; echo CENSUS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/11-resolution-backfill/maplify_census.tsv` exists and is non-empty.
    - File contains at least the three match_kind classifications observed in prod (`bracket`, `attribution`, and/or `source-only`) with per-row counts.
    - File header documents the read-only query and the date; no UPDATE/INSERT/DELETE/ALTER text appears anywhere in the committed query block (grep: `grep -ciE 'update|insert|delete|alter' <file>` against the query block returns 0).
    - Census tag counts are consistent in shape with v1.3-EXECUTIVE-SUMMARY §3 (operator confirms in checkpoint Task 2 if counts drifted).
  </acceptance_criteria>
  <done>Read-only prod census committed as a TSV artifact with a documented query and a blank target_collection_slug column ready for human confirmation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Human confirms acronym expansions + target collection slug for every census row</name>
  <files>.planning/phases/11-resolution-backfill/maplify_census.tsv</files>
  <read_first>
    - .planning/phases/11-resolution-backfill/maplify_census.tsv (the census from Task 1)
    - .planning/phases/11-resolution-backfill/11-RESEARCH.md ("D-10: Acronym Expansion Research" table — the LOW/MEDIUM-confidence proposed expansions for PSWS, MCW, CWW, WSSJI, HIWS, SBW, WA, SSCH, SA, PSWW, Bremerton)
    - supabase/migrations/20260619184037_reference_tables.sql lines 109-135 (the 21 existing collection slugs the census rows must map onto — bracket tags map to acronym-stub slugs psws/mcw/cww/wssji/hiws/sbw/wa/ssch/sa/psww/bremerton-fb or named slugs orca-network/cascadia/tmmc/etc.; source codes map per D-11)
  </read_first>
  <what-built>
    Task 1 produced the read-only prod census TSV. This checkpoint records the human-confirmed mapping from each census `match_value` (bracket tag / attribution / source code) to a target `public.collections` slug, OR an explicit "stay NULL" for empty/[NULL] brackets (D-09). The researcher's acronym expansions are LOW-confidence community knowledge and MUST be confirmed by a human before plan 11-03 writes any collection_rule seed row.
  </what-built>
  <action>
    This is a checkpoint:human-verify task — execution pauses for the operator. The executor presents the census artifact and the proposed acronym expansions, then waits. The substantive instructions are in <what-built>, <how-to-verify>, and <resume-signal>. Do NOT write any collection_rule seed row (plan 11-03) until the operator approves every row's target_collection_slug. No automated work happens in this task beyond surfacing the artifact for review.
  </action>
  <verify>
    <human-check>Operator confirms every census row has a target_collection_slug (or STAY_NULL) and all 11 acronym expansions, then types "approved".</human-check>
  </verify>
  <how-to-verify>
    1. Open `.planning/phases/11-resolution-backfill/maplify_census.tsv`.
    2. For EACH bracket-tag row, confirm the acronym expansion and write the target collection slug in the `target_collection_slug` column (e.g. `PSWS -> psws`, `Orca Network -> orca-network`, `Bremerton FB group -> bremerton-fb`). For any tag naming a real channel that lacks a stub (D-09: one-offs), note the slug to create.
    3. For EACH empty / `[NULL]` bracket row, mark `target_collection_slug = STAY_NULL` (D-09 — these stay NULL; SC#1 regex tightened to `^\[[^\]]+\]`).
    4. For EACH trailing-attribution row, set the slug to the collection/org only — NEVER a contributor (D-13 / SC#3).
    5. For EACH structured `source` code row (whalealertoa, cascadiaWebMap, farallon, ...), set the curated target slug per D-11, or mark STAY_NULL if no sensible collection.
    6. Confirm the per-row counts look right against v1.3-EXECUTIVE-SUMMARY §3.
    7. Commit the annotated TSV.
  </how-to-verify>
  <resume-signal>Type "approved" once every census row has a target_collection_slug (or STAY_NULL) and all 11 acronym expansions are human-confirmed. Describe any tag that needs a new collection slug created in plan 11-03.</resume-signal>
  <acceptance_criteria>
    - Every bracket-tag and source-code row in maplify_census.tsv has a non-blank `target_collection_slug` value OR an explicit `STAY_NULL`.
    - All 11 acronym stubs (psws, mcw, cww, wssji, hiws, sbw, wa, ssch, sa, psww, bremerton-fb) have a human-confirmed expansion recorded.
    - No attribution row maps to a contributor (D-13 / SC#3) — all attribution rows map to a collection/org slug or STAY_NULL.
    - Operator typed "approved".
  </acceptance_criteria>
  <done>Census artifact is fully annotated with human-confirmed target collection slugs; acronym expansions confirmed; ready to drive the collection_rule seed in plan 11-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local psql -> PROD DB | A direct connection to the production Supabase DB. Must be read-only; any accidental write mutates live data. |
| prod row text -> census output | `comments` text is read and aggregated; it is never modified (SC#2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | prod census query | mitigate | Query is SELECT-only; acceptance gate greps the committed query for update/insert/delete/alter and requires 0 matches. No comments mutation (SC#2). |
| T-11-02-02 | Information Disclosure | inline DSN with DB_PASSWORD | mitigate | DSN assembled inline at runtime from DB_PASSWORD; never committed to the repo or persisted as a new secret (memory: no SUPABASE_DB_URL). The committed artifact contains only aggregated tag/count data, no credentials. |
| T-11-02-03 | Spoofing/Repudiation | wrong acronym expansion -> misattributed collection | mitigate | blocking-human checkpoint requires explicit human confirmation of every expansion before any rule is seeded; never auto-approvable. |
| T-11-SC | Tampering | npm installs | mitigate | No packages installed this plan. |
</threat_model>

<verification>
- Census artifact committed and non-empty; query provably read-only.
- Human checkpoint approved with every row annotated.
</verification>

<success_criteria>
- Read-only prod census committed (RESOLVE-03 census precondition; D-08).
- Human-confirmed target collection slug per row, with empty brackets marked STAY_NULL (D-09) and attribution rows mapping to collection/org only (D-13/SC#3).
- `comments` never mutated during census (SC#2).
</success_criteria>

<output>
Create `.planning/phases/11-resolution-backfill/11-02-SUMMARY.md` when done.
</output>
