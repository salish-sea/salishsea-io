---
phase: 11-resolution-backfill
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - supabase/migrations/20260620000100_resolution_backfill.sql
  - supabase/migrations/20260620000200_resolution_ingest.sql
  - supabase/snippets/11_resolution_assertions.sql
autonomous: false
requirements: [RESOLVE-03, RESOLVE-04]
must_haves:
  truths:
    - "A one-time idempotent backfill UPDATE populates collection_id on iNat/native/HappyWhale existing rows by slug join, guarded by WHERE collection_id IS NULL (D-07)"
    - "A one-time idempotent backfill UPDATE populates maplify.sightings.collection_id via maplify.resolve_collection(comments, source), guarded by WHERE collection_id IS NULL; comments untouched (D-02/D-07/D-12)"
    - "A one-time idempotent backfill mints + links iNat contributor_id from inaturalist.observations.username via inaturalist.mint_contributor, guarded by WHERE contributor_id IS NULL (D-15)"
    - "maplify.update_sightings INSERT is rewritten to an explicit column list (all fetch_date_range columns + maplify.resolve_collection(comments, source) AS collection_id), fixing the Phase-10-broken wildcard (D-01/D-02)"
    - "inaturalist.upsert_observation_page MERGE WHEN NOT MATCHED INSERT adds contributor_id = inaturalist.mint_contributor(v.username); WHEN MATCHED UPDATE does NOT touch contributor_id/collection_id (D-16, RESEARCH Pitfall 6)"
    - "maplify.sightings.contributor_id and the comments column are never written (D-12/D-13/SC#2/SC#3)"
    - "The completed schema is pushed to the database via supabase db push before verification (no false-positive from config-derived types)"
  artifacts:
    - path: "supabase/migrations/20260620000100_resolution_backfill.sql"
      provides: "Idempotent one-time backfill UPDATEs for all four providers (collection_id) + iNat contributor_id"
      contains: "WHERE collection_id IS NULL"
    - path: "supabase/migrations/20260620000200_resolution_ingest.sql"
      provides: "Edited maplify.update_sightings (explicit columns) + edited inaturalist.upsert_observation_page (contributor mint in MERGE)"
      contains: "maplify.resolve_collection"
  key_links:
    - from: "maplify.update_sightings INSERT"
      to: "maplify.resolve_collection"
      via: "explicit column list, collection_id = resolve_collection(comments, source)"
      pattern: "maplify\\.resolve_collection\\(comments, source\\)"
    - from: "inaturalist.upsert_observation_page MERGE INSERT"
      to: "inaturalist.mint_contributor"
      via: "contributor_id = inaturalist.mint_contributor(v.username)"
      pattern: "inaturalist\\.mint_contributor\\(v\\.username\\)"
    - from: "backfill UPDATE maplify.sightings"
      to: "maplify.resolve_collection"
      via: "SET collection_id = maplify.resolve_collection(comments, source) WHERE collection_id IS NULL"
      pattern: "SET collection_id = maplify\\.resolve_collection"
---

<objective>
Land the one-time idempotent backfill UPDATEs (RESOLVE-03) and the ongoing-ingest function edits (RESOLVE-04), then push the completed schema to the database and run the full assertion suite + the prod diff-gate. This plan mutates PROD row state and edits two LIVE DB-side ingest functions, so it carries the mandatory `[BLOCKING]` schema-push task and is non-autonomous.

Two migrations (ordered AFTER plan 11-03's schema migration so resolve_collection + the seed already exist — RESEARCH Pitfall 3):
- `20260620000100_resolution_backfill.sql` — idempotent one-time UPDATEs (collection_id for all four tables; iNat contributor_id), all guarded by `WHERE ... IS NULL` (D-07); a no-op on local reset where prod rows are absent (acceptable for a data migration).
- `20260620000200_resolution_ingest.sql` — rewrite `maplify.update_sightings` to an explicit column list calling `maplify.resolve_collection` (fixes the Phase-10-broken `SELECT sightings.*` wildcard, D-01/D-02) and extend `inaturalist.upsert_observation_page`'s MERGE to mint + link `contributor_id` (D-16).

Purpose: RESOLVE-03 (one-time backfill across all four providers, comments preserved) and RESOLVE-04 (ongoing exact-match resolution at ingest). Because the project auto-deploys on push to main and the Maplify cron runs every 5 min, the ingest edit must be tested locally before the push and all migrations land in one deploy.
Output: two migration files + the extended assertion snippet; schema pushed; assertions + prod diff-gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-resolution-backfill/11-CONTEXT.md
@.planning/phases/11-resolution-backfill/11-RESEARCH.md
@.planning/phases/11-resolution-backfill/11-PATTERNS.md
@supabase/migrations/20250904165159_fetch_data.sql
@supabase/migrations/20250914232212_cron.sql
@supabase/migrations/20260619203013_source_table_fk_columns.sql
</context>

<artifacts_this_phase_produces>
Created/edited by THIS plan:
- Migration `supabase/migrations/20260620000100_resolution_backfill.sql` — one-time idempotent backfill UPDATEs.
- Migration `supabase/migrations/20260620000200_resolution_ingest.sql` — edited `maplify.update_sightings` (CREATE OR REPLACE) + edited `inaturalist.upsert_observation_page` (CREATE OR REPLACE).
- Extended `supabase/snippets/11_resolution_assertions.sql` — adds the update_sightings synthetic round-trip (SC#5) and the MERGE-contributor structural assertion.
- Applied prod row state (via push + cron): collection_id populated on existing rows (all four tables); iNat contributor_id minted + linked.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: One-time idempotent backfill UPDATEs (collection_id all four tables + iNat contributor_id)</name>
  <files>supabase/migrations/20260620000100_resolution_backfill.sql</files>
  <read_first>
    - supabase/migrations/20260619203013_source_table_fk_columns.sql lines 46-50 (the idempotent slug-join UPDATE shape with `WHERE collection_id IS NULL` guard — the exact backfill template)
    - supabase/migrations/20260620000000_resolution_schema.sql (plan 11-03 output: maplify.resolve_collection, inaturalist.mint_contributor, the seeded collection_rule, the inat_login column — must already exist; this migration depends on them)
    - supabase/migrations/20250914232212_cron.sql lines 33-74 (confirms iNat username source is `o.user->>'login'` stored into inaturalist.observations.username — the backfill reads the existing username column)
    - .planning/phases/11-resolution-backfill/11-CONTEXT.md (D-07 idempotent guard; D-12 comments immutable; D-13 Maplify contributor stays NULL; D-14 native contributor unchanged, HW contributor deferred; D-15 iNat contributor populated)
    - .planning/phases/11-resolution-backfill/11-RESEARCH.md (Pattern 1 backfill; Runtime State Inventory — last-10-days Maplify rows get wiped+reinserted by cron, but historical rows need this UPDATE)
  </read_first>
  <action>
    Create `supabase/migrations/20260620000100_resolution_backfill.sql` with a header citing RESOLVE-03 and D-07 (idempotent), D-12 (comments untouched), D-15 (iNat contributor). All statements idempotent via `WHERE ... IS NULL`. Sections:
    (1) collection_id slug-join backfill for the three single-collection tables (mirror the Phase-10 template): `UPDATE inaturalist.observations SET collection_id = c.id FROM public.collections c WHERE c.slug='inaturalist' AND collection_id IS NULL;` and likewise public.observations -> 'salishsea-direct', happywhale.encounters -> 'happywhale'.
    (2) Maplify collection_id backfill: `UPDATE maplify.sightings SET collection_id = maplify.resolve_collection(comments, source) WHERE collection_id IS NULL;` — reads comments, NEVER writes it (D-12/SC#2). resolve_collection returns NULL for unmatched, so unmatched rows stay NULL (D-09/RESOLVE-04).
    (3) iNat contributor_id backfill (D-15): `UPDATE inaturalist.observations SET contributor_id = inaturalist.mint_contributor(username) WHERE contributor_id IS NULL AND username IS NOT NULL;` — mint_contributor is idempotent (ON CONFLICT). This mints distinct public.contributors rows keyed by inat_login and links them; jmaughn(iNat) stays unlinked from James Maughn(native) (cross-provider unification deferred).
    Do NOT write maplify.sightings.contributor_id (stays NULL, D-13/SC#3). Do NOT touch native contributor_id (already 100% populated, D-14). Do NOT touch HappyWhale contributor_id (deferred, D-14). Do NOT write maplify.sightings.comments anywhere (D-12). Add a comment noting this migration is a no-op on local `supabase db reset` because prod's historical rows are not present locally (D-07) — acceptable for a data migration.
  </action>
  <verify>
    <automated>supabase db reset &amp;&amp; psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -v ON_ERROR_STOP=1 -c "SELECT 1;" &amp;&amp; echo BACKFILL_MIGRATION_APPLIES</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` applies the migration with exit 0.
    - Every UPDATE is guarded by an IS NULL clause: `grep -c "IS NULL" supabase/migrations/20260620000100_resolution_backfill.sql` >= 5 (3 collection_id slug-join + 1 maplify resolver + 1 iNat contributor).
    - The Maplify collection_id UPDATE uses the resolver: `grep -c "SET collection_id = maplify.resolve_collection(comments, source)" supabase/migrations/20260620000100_resolution_backfill.sql` >= 1.
    - The iNat contributor UPDATE uses the mint helper: `grep -c "inaturalist.mint_contributor(username)" supabase/migrations/20260620000100_resolution_backfill.sql` >= 1.
    - No write to comments and no maplify contributor_id write: `grep -niE "set[^;]*comments|maplify.sightings[^;]*set[^;]*contributor_id" supabase/migrations/20260620000100_resolution_backfill.sql` returns nothing.
    - No hardcoded collection/contributor ids: `grep -nE "collection_id = [0-9]+|contributor_id = [0-9]+" supabase/migrations/20260620000100_resolution_backfill.sql` returns nothing.
  </acceptance_criteria>
  <done>Idempotent backfill migration applies cleanly; collection_id resolved for all four tables (Maplify via resolver, others by slug join); iNat contributor minted/linked; comments + Maplify contributor_id + native/HW contributor untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Edit live ingest functions — update_sightings explicit columns + iNat MERGE contributor mint + extend assertions</name>
  <files>supabase/migrations/20260620000200_resolution_ingest.sql, supabase/snippets/11_resolution_assertions.sql</files>
  <read_first>
    - supabase/migrations/20250904165159_fetch_data.sql lines 125-199 (the EXACT current maplify.update_sightings body with the broken `SELECT sightings.*` wildcard, AND the maplify.fetch_date_range RETURNS TABLE column list — the authoritative source for the explicit INSERT column list; count the columns from this RETURNS TABLE, do not trust a remembered count)
    - supabase/migrations/20250914232212_cron.sql lines 24-103 (the LIVE inaturalist.upsert_observation_page MERGE — the version to edit; username is `v.username` derived from `o.user->>'login'`; the WHEN NOT MATCHED INSERT column list at lines 73-74; the WHEN MATCHED UPDATE at lines 63-72)
    - supabase/migrations/20260620000000_resolution_schema.sql (plan 11-03: maplify.resolve_collection + inaturalist.mint_contributor must exist before these edits reference them)
    - .planning/phases/11-resolution-backfill/11-PATTERNS.md (Pattern E edited update_sightings; Pattern F MERGE contributor wiring)
    - .planning/phases/11-resolution-backfill/11-RESEARCH.md (Critical SQL section; Pitfall 1 wildcard; Pitfall 4 live-prod-ingest risk; Pitfall 6 do NOT add contributor_id to the MATCHED UPDATE)
  </read_first>
  <action>
    Create `supabase/migrations/20260620000200_resolution_ingest.sql` with a header citing RESOLVE-04, D-01/D-02 (Maplify ingest edit), D-16 (iNat MERGE edit), and noting this is the deliberate departure from Phase-10 D-14 ("don't touch ingest RPCs"). Two edits, both `CREATE OR REPLACE FUNCTION`:
    (A) `maplify.update_sightings` (CREATE OR REPLACE, keep the exact signature `(start_date date = current_date, end_date date = current_date) RETURNS void LANGUAGE SQL VOLATILE BEGIN ATOMIC`): keep the DELETE line verbatim; replace the wildcard `INSERT INTO maplify.sightings SELECT sightings.*` with an EXPLICIT column list. The INSERT column list AND the SELECT list must each name every column returned by `maplify.fetch_date_range` (read the RETURNS TABLE definition and list them in declared order) PLUS `collection_id`. The SELECT computes `collection_id` as `maplify.resolve_collection(comments, source)`. Leave `provider_id` OUT of the column list (Phase-10 DEFAULT fires) and leave `contributor_id` + `source_url` OUT (default NULL). CRITICAL: the INSERT column count MUST equal the SELECT expression count, or Postgres errors "INSERT has more target columns than expressions" (Pitfall 1). Keep the bbox/fetch_date_range FROM clause unchanged.
    (B) `inaturalist.upsert_observation_page` (CREATE OR REPLACE, copy the LIVE cron.sql MERGE body verbatim) with ONE change: in the `WHEN NOT MATCHED BY TARGET THEN INSERT` clause, append `contributor_id` to the column list and `inaturalist.mint_contributor(v.username)` to the VALUES list. Do NOT add contributor_id (or collection_id) to the `WHEN MATCHED ... THEN UPDATE SET` clause (Pitfall 6 — existing rows keep their backfilled values; the MERGE UPDATE must not overwrite). collection_id stays absent from the INSERT so its DEFAULT fires (D-05). Keep the second MERGE (observation_photos) and the ensure_taxa call unchanged.
    Then EXTEND `supabase/snippets/11_resolution_assertions.sql`: add an SC#5 synthetic round-trip block — run `maplify.update_sightings(current_date-3, current_date)` (it hits the live HTTP fetch locally; if the fetch is unavailable in CI, instead assert structurally) and assert `pg_get_functiondef('maplify.update_sightings'::regprocedure)` contains `maplify.resolve_collection(comments, source)`; add an assertion that `pg_get_functiondef('inaturalist.upsert_observation_page(jsonb)'::regprocedure)` contains `inaturalist.mint_contributor(v.username)` and that the MATCHED UPDATE clause does NOT contain `contributor_id` (Pitfall 6). Keep the closing `\echo`.
  </action>
  <verify>
    <automated>supabase db reset &amp;&amp; psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -v ON_ERROR_STOP=1 -f supabase/snippets/11_resolution_assertions.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` applies both migrations with exit 0; the snippet exits 0.
    - update_sightings is now explicit-column (no wildcard): `grep -c "SELECT sightings.\\*" supabase/migrations/20260620000200_resolution_ingest.sql` == 0; `grep -c "maplify.resolve_collection(comments, source)" supabase/migrations/20260620000200_resolution_ingest.sql` >= 1.
    - The INSERT into maplify.sightings names collection_id and omits provider_id/contributor_id/source_url (verify by reading the column list).
    - iNat MERGE INSERT wires the mint: `grep -c "inaturalist.mint_contributor(v.username)" supabase/migrations/20260620000200_resolution_ingest.sql` >= 1.
    - Contributor mint is NOT in the MATCHED UPDATE clause (Pitfall 6): the `WHEN MATCHED` ... `UPDATE SET` block contains no `contributor_id` (verify by reading; the snippet asserts this via pg_get_functiondef).
    - `pg_get_functiondef('maplify.update_sightings'::regprocedure)` contains `resolve_collection` (asserted in the snippet).
    - No write to maplify.sightings.comments anywhere in this file (D-12/SC#2): `grep -niE "set[^;]*comments" supabase/migrations/20260620000200_resolution_ingest.sql` returns nothing.
  </acceptance_criteria>
  <done>update_sightings rewritten to explicit columns calling resolve_collection (wildcard gone); iNat MERGE INSERT mints+links contributor_id without touching the MATCHED UPDATE; assertion snippet extended for SC#5 + MERGE structure; both migrations apply cleanly locally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Push completed schema to the database, then verify backfill + prod diff-gate</name>
  <files>supabase/snippets/11_resolution_assertions.sql</files>
  <read_first>
    - .planning/STATE.md (deploy auto-runs on push to main; .github/workflows/deploy.yml)
    - Auto-memory: pushes to main auto-deploy to prod; Supabase DSN assembled inline from DB_PASSWORD + SUPABASE_PROJECT_ID (no SUPABASE_DB_URL secret); orcasound AWS profile is for S3 only, not the DB
    - .planning/phases/11-resolution-backfill/11-RESEARCH.md (Pitfall 4 live-prod ingest risk; "Migration ordering constraint" — schema (a) before backfill (b) before ingest (c), all in one deploy; the prod diff-gate query)
    - .planning/phases/11-resolution-backfill/11-PATTERNS.md (the commented PROD-ONLY diff-gate block to run here)
  </read_first>
  <what-built>
    Tasks 1-2 (this plan) + plan 11-03 produced three migrations (schema -> backfill -> ingest) and the assertion snippet, all green on local `supabase db reset`. This BLOCKING task applies the completed schema to the database and verifies the prod outcome. The schema push is mandatory: build/type checks pass WITHOUT it because Supabase types come from config not the live DB, so skipping the push would yield a false-positive verification. Because the project auto-deploys on push to main and the Maplify cron runs every 5 min, all three migrations must land in ONE deploy after local testing (Pitfall 4).
  </what-built>
  <action>
    This is a [BLOCKING] checkpoint:human-verify task wrapping the mandatory schema push. The executor first confirms all three migrations are green on local `supabase db reset` and the assertion snippet exits 0, then runs `supabase db push` to apply the schema to the database (non-TTY: assemble the inline DSN from DB_PASSWORD + SUPABASE_PROJECT_ID per memory; never add a SUPABASE_DB_URL secret). Pushing to main triggers the production deploy — the operator confirms required GitHub Actions vars/secrets are in place before pushing. After the push, run the read-only prod diff-gate + SC#1/SC#2/SC#3 checks in <how-to-verify> and pause for operator approval. The push is mandatory: build/type checks pass without it (types come from config, not the live DB), so skipping it yields a false-positive verification.
  </action>
  <verify>
    <human-check>Operator confirms `supabase db push` succeeded, the prod diff-gate returns 0 uncovered tags, SC#1=0, SC#3=0, recent cron rows show resolved collection_id, then types "approved".</human-check>
  </verify>
  <how-to-verify>
    1. Confirm all three migrations apply cleanly on a fresh local `supabase db reset` and the snippet exits 0 (Tasks 1-2 + plan 11-03 already green).
    2. Apply the schema to the database: run `supabase db push` (non-TTY: ensure `SUPABASE_ACCESS_TOKEN` / inline DSN env is set per memory — assemble from DB_PASSWORD + SUPABASE_PROJECT_ID; do NOT add a new SUPABASE_DB_URL secret). If `supabase db push` requires interactive confirmation that cannot be suppressed, the operator runs it manually. NOTE: pushing to main triggers the production deploy (.github/workflows/deploy.yml) — operator confirms required GH Actions vars/secrets are in place before pushing (memory).
    3. Run the PROD diff-gate read-only (the D-08 assertion, uncommented for prod): `psql "$PROD_DSN" -c "SELECT COUNT(*) FROM maplify.sightings s WHERE s.comments ~ '^\[[^\]]+\]' AND NOT EXISTS (SELECT 1 FROM maplify.collection_rule r WHERE r.match_kind='bracket' AND r.match_value=(regexp_match(s.comments,'^\[([^\]]+)\]'))[1])"` -> expect 0 uncovered bracket tags.
    4. Run SC#1 against prod (read-only): `psql "$PROD_DSN" -c "SELECT COUNT(*) FROM maplify.sightings WHERE comments ~ '^\[[^\]]+\]' AND collection_id IS NULL"` -> expect 0 (D-09: non-empty brackets resolved; empty brackets excluded by the tightened regex).
    5. Run SC#3 against prod (read-only): `SELECT COUNT(*) FROM maplify.sightings WHERE contributor_id IS NOT NULL AND comments ~ 'Trusted Observer'` -> expect 0 (D-13: attributions never yield a contributor).
    6. Verify SC#2 structurally: no migration UPDATEs maplify.sightings.comments (already enforced by Task 1/2 acceptance greps); bracket tags still present in the column.
    7. Spot-check ongoing ingest: `SELECT created_at, collection_id FROM maplify.sightings ORDER BY created_at DESC LIMIT 5` -> recent cron-inserted rows show non-NULL collection_id where the source carries a seeded bracket tag (Pitfall 4 warning-sign check).
  </how-to-verify>
  <resume-signal>Type "approved" once the schema is pushed, the prod diff-gate returns 0 uncovered tags, SC#1 (bracket rows NULL) returns 0, SC#3 (attribution contributor) returns 0, and recent cron rows show resolved collection_id. Report any uncovered tag (it indicates a census/seed gap to close in 11-03).</resume-signal>
  <acceptance_criteria>
    - `supabase db push` applied the three migrations to the database (operator confirms success).
    - Prod diff-gate returns 0 uncovered bracket tags (D-08 / RESOLVE-03).
    - SC#1: `COUNT(*) FROM maplify.sightings WHERE comments ~ '^\[[^\]]+\]' AND collection_id IS NULL` == 0.
    - SC#3: `COUNT(*) FROM maplify.sightings WHERE contributor_id IS NOT NULL AND comments ~ 'Trusted Observer'` == 0.
    - SC#2: bracket tags + trailing attributions still present verbatim in comments; no comments UPDATE in any migration.
    - Recent (cron-inserted) Maplify rows show non-NULL collection_id for seeded-tag sources (ongoing ingest verified).
    - Operator typed "approved".
  </acceptance_criteria>
  <done>Schema pushed to the database; one-time backfill verified complete against prod (diff-gate 0, SC#1 0, SC#3 0); ongoing ingest resolves collection_id; comments preserved verbatim (SC#2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration on push to main -> PROD rows | The backfill + ingest migrations auto-apply to production on deploy; they mutate live row state and replace live ingest functions. |
| cron/postgres role -> public.contributors (RLS) | The edited iNat MERGE mints contributors via the SECURITY DEFINER helper from the cron context. |
| upstream comment text -> resolve_collection at ingest | Each new Maplify row's comments are matched against the dictionary at INSERT time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | accidental maplify.sightings.comments mutation | mitigate | NO UPDATE on comments in any migration (acceptance greps enforce); the backfill reads comments only; structural SC#2 type-assert in the snippet. Bracket tags/attributions are the immutable audit trail (D-12). |
| T-11-04-02 | Tampering | non-idempotent re-run overwrites resolved FKs | mitigate | Every backfill UPDATE guarded by `WHERE ... IS NULL`; resolve_collection returns NULL for unmatched so re-runs add only newly-coverable rows; ON CONFLICT on contributor mint. |
| T-11-04-03 | Denial of Service | buggy update_sightings breaks live ingest for up to 5 min | mitigate | Test the edited function on local `supabase db reset` before push (Pitfall 4); explicit column count verified == SELECT expression count (Pitfall 1); all three migrations land in one deploy after local green. |
| T-11-04-04 | Elevation of Privilege | iNat MERGE mint runs RLS-protected INSERT from cron | mitigate | Mint goes through inaturalist.mint_contributor (SECURITY DEFINER, SET search_path='', schema-qualified) created in plan 11-03; never DISABLE RLS. |
| T-11-04-05 | Tampering | contributor_id overwritten on MERGE UPDATE | mitigate | contributor_id added ONLY to WHEN NOT MATCHED INSERT, never to WHEN MATCHED UPDATE (Pitfall 6); snippet asserts the UPDATE clause has no contributor_id. |
| T-11-04-06 | Information Disclosure | inline prod DSN with DB_PASSWORD during push/diff-gate | mitigate | DSN assembled inline at runtime; never committed or persisted as a new secret (memory). Diff-gate/SC queries are read-only SELECT. |
| T-11-SC | Tampering | npm installs | mitigate | No packages installed this plan (no new npm deps in phase). |
</threat_model>

<verification>
- Local: `supabase db reset` applies all three migrations; `psql ... -f supabase/snippets/11_resolution_assertions.sql` exits 0; `npm test` still green.
- Push: `supabase db push` applies the schema to the database (BLOCKING — required to avoid false-positive verification).
- Prod (read-only): diff-gate 0 uncovered tags; SC#1 0 bracket-NULL rows; SC#3 0 attribution-contributor rows; recent cron rows resolved.
</verification>

<success_criteria>
- RESOLVE-03: one-time idempotent backfill populated collection_id across all four providers (Maplify via resolver, others via slug join) and iNat contributor_id; comments preserved verbatim (SC#2); census-driven diff-gate returns 0 uncovered tags.
- RESOLVE-04: ongoing ingest resolves collection_id at INSERT (Maplify via resolve_collection in update_sightings; iNat/native/HW via DEFAULT); iNat contributor minted at ingest in the MERGE; unmatched tags -> NULL.
- SC#1 (with D-09 tightened regex), SC#2, SC#3, SC#5 satisfied against prod; SC#4 satisfied by plan 11-01 + the DEFAULTs/backfill here.
- Schema pushed to the database (no false-positive verification).
</success_criteria>

<output>
Create `.planning/phases/11-resolution-backfill/11-04-SUMMARY.md` when done.
</output>
