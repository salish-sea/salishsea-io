---
phase: 11-resolution-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ingest/resolve-provider.ts
  - scripts/ingest/resolve-provider.test.ts
autonomous: true
requirements: [RESOLVE-01]
must_haves:
  truths:
    - "resolveProvider() returns {provider:'inaturalist', collection:'inaturalist'} for an inaturalist.org observation URL"
    - "resolveProvider() returns the native {provider:'direct', collection:'salishsea-direct'} mapping for a salishsea.io observation URL"
    - "resolveProvider() returns null for an unrecognized URL or empty/invalid input"
    - "The module is a pure function with no I/O, no DB access, and no external npm imports (RESOLVE-01 / SC#4)"
  artifacts:
    - path: "scripts/ingest/resolve-provider.ts"
      provides: "Pure URL-pattern resolver: sourceUrl -> {provider, collection} | null"
      exports: ["resolveProvider", "ProviderResolution"]
      min_lines: 25
    - path: "scripts/ingest/resolve-provider.test.ts"
      provides: "vitest unit tests: one per known URL pattern + null cases"
      min_lines: 20
  key_links:
    - from: "scripts/ingest/resolve-provider.test.ts"
      to: "scripts/ingest/resolve-provider.ts"
      via: "import { resolveProvider }"
      pattern: "import .*resolve-provider"
---

<objective>
Build the pure TypeScript URL-pattern resolver `scripts/ingest/resolve-provider.ts` (RESOLVE-01 / SC#4 / D-06): a side-effect-free function mapping a `source_url` to `{ provider, collection }` slugs (or `null`). It serves two roles — (a) the one-time iNat/native URL backfill consumed in plan 11-04, and (b) the future-FB / mixed-source extension point. Per D-06 it is NOT on the Maplify resolution path (that is DB-side, plan 11-03) and NOT the ongoing mechanism for single-collection tables (that is the column DEFAULT, plan 11-03). This plan is the Wave 0 scaffold for the RESOLVE-01 test file plus its implementation.

Purpose: SC#4 requires this pure resolver to exist; it is the only TypeScript deliverable in the phase. Resolution everywhere else is DB-side because ingest is entirely pg_cron SQL.
Output: `scripts/ingest/resolve-provider.ts` + `scripts/ingest/resolve-provider.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-resolution-backfill/11-CONTEXT.md
@.planning/phases/11-resolution-backfill/11-RESEARCH.md
@.planning/phases/11-resolution-backfill/11-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Created by THIS plan:
- File `scripts/ingest/resolve-provider.ts` — exports `resolveProvider(sourceUrl: string)` and type `ProviderResolution`.
- File `scripts/ingest/resolve-provider.test.ts` — vitest suite.

(Other phase-level artifacts — `maplify.collection_rule`, `maplify.resolve_collection`, `inaturalist.mint_contributor`, `public.contributors.inat_login`, collection_id DEFAULTs, migration files, the assertion snippet — are produced by plans 11-02/11-03/11-04 and listed there.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure URL-pattern resolver + vitest suite (Wave 0 scaffold + impl)</name>
  <files>scripts/ingest/resolve-provider.ts, scripts/ingest/resolve-provider.test.ts</files>
  <read_first>
    - scripts/dwca/fields.ts (analog: file-header doc-comment convention, typed named exports, no default export, no I/O — lines 1-26)
    - scripts/dwca/fields.test.ts (analog: vitest import line `import { describe, test, expect } from 'vitest'`, test structure, no vi.mock)
    - .planning/phases/11-resolution-backfill/11-PATTERNS.md (Pattern Assignments: resolve-provider.ts + resolve-provider.test.ts sections — the exact adapted module/test shape)
    - supabase/migrations/20260619184037_reference_tables.sql lines 109-135 (canonical collection slugs the resolver must emit: `inaturalist`, `salishsea-direct`; provider slugs from the providers seed: `inaturalist`, `direct`)
  </read_first>
  <behavior>
    - resolveProvider('https://www.inaturalist.org/observations/12345') === { provider: 'inaturalist', collection: 'inaturalist' }
    - resolveProvider('https://inaturalist.org/observations/9') === { provider: 'inaturalist', collection: 'inaturalist' } (host with and without www)
    - A native salishsea.io observation URL (whatever form `public.observations.url` takes — read the column comment / sample) === { provider: 'direct', collection: 'salishsea-direct' }
    - resolveProvider('https://example.com/foo') === null
    - resolveProvider('') === null and resolveProvider('not a url') === null (no throw on invalid input)
  </behavior>
  <action>
    Create directory `scripts/ingest/`. Write `resolve-provider.ts` as a pure module mirroring `scripts/dwca/fields.ts` conventions: a file-header doc-comment citing "Phase 11 Plan 01" and D-06, a named type export `ProviderResolution = { readonly provider: string; readonly collection: string } | null`, and a named function export `resolveProvider(sourceUrl: string): ProviderResolution`. Implement with the built-in `URL` constructor wrapped in try/catch (return null on parse failure) plus host/path matching — no external npm imports (RESEARCH: "no new npm packages"). Emit provider/collection SLUGS that exactly match the Phase 9 seed: iNaturalist host -> `{ provider: 'inaturalist', collection: 'inaturalist' }`; native salishsea.io host -> `{ provider: 'direct', collection: 'salishsea-direct' }`. Unknown host -> `null`. Per D-06 do NOT add Maplify URL patterns (Maplify has no source_url today; its resolution is DB-side). Write `resolve-provider.test.ts` importing `{ resolveProvider }` with one test per known pattern plus the null/invalid cases above; no mocks (pure function). Cite RESOLVE-01 and D-06 in the file header. Keep the slug strings as inline literals matching the DB seed exactly — these are the join contract.
  </action>
  <verify>
    <automated>npm test -- resolve-provider</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- resolve-provider` exits 0 with all tests passing.
    - `grep -n "export function resolveProvider" scripts/ingest/resolve-provider.ts` returns a match.
    - `grep -n "export type ProviderResolution" scripts/ingest/resolve-provider.ts` returns a match.
    - `grep -cE "import .*resolve-provider" scripts/ingest/resolve-provider.test.ts` >= 1.
    - `grep -c "from 'vitest'" scripts/ingest/resolve-provider.test.ts` >= 1.
    - No new dependency lines added to package.json (resolver uses only built-in `URL`/regex): `git diff --name-only` does not include package.json or package-lock.json.
    - The slug literals `'inaturalist'` and `'salishsea-direct'` appear in resolve-provider.ts (join contract with Phase 9 seed).
  </acceptance_criteria>
  <done>Pure resolver exists with passing vitest suite; emits Phase-9 slugs; returns null on unknown/invalid input; no external deps; D-06 / RESOLVE-01 cited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest URL string -> resolver | `source_url` values originate from upstream provider records; the resolver parses them. Pure-function, no eval, no I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | `resolveProvider` URL parsing | mitigate | Parse with built-in `URL` in try/catch; never `eval`; return `null` on malformed input. No DB write from this module. |
| T-11-01-02 | Denial of Service | regex on attacker-influenced URL | accept | Matching uses anchored host checks / built-in URL parser, not catastrophic-backtracking regex; input length bounded by upstream column widths. Low value, offline batch context. |
| T-11-SC | Tampering | npm installs | mitigate | No new packages installed this plan (RESEARCH Package Legitimacy Audit: none). slopcheck N/A. |
</threat_model>

<verification>
- `npm test -- resolve-provider` green.
- `npm test` (full suite) still green — the new module must not break existing dwca tests.
</verification>

<success_criteria>
- `scripts/ingest/resolve-provider.ts` is a pure function exporting `resolveProvider` + `ProviderResolution`, emitting Phase-9 slugs, returning null on unknown/invalid input (SC#4 partial — the URL resolver exists as a pure function).
- vitest suite passes; no new npm dependency introduced.
</success_criteria>

<output>
Create `.planning/phases/11-resolution-backfill/11-01-SUMMARY.md` when done.
</output>
