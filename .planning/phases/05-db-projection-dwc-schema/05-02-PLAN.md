---
phase: 05-db-projection-dwc-schema
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - supabase/migrations/20260617203900_dwc_schema.sql
autonomous: true
requirements:
  - ALIGN-01
  - ALIGN-02
  - ALIGN-04
  - ALIGN-05
  - ALIGN-06
must_haves:
  truths:
    - "`dwc._native_occurrences` returns one row per `public.observations` row, joined to its contributor and to `dwc.taxa_classification`."
    - "Every row carries the four GBIF-required terms non-null: `occurrenceID` (= `'salishsea:' || o.id::text`), `basisOfRecord` (= `'HumanObservation'`), `scientificName` (from `tc.scientific_name`), `eventDate` (full ISO-8601 UTC timestamp)."
    - "`decimalLatitude` is `ST_Y` and `decimalLongitude` is `ST_X` (PostGIS axis convention, RESEARCH §"PostGIS Axis Convention")."
    - "`coordinateUncertaintyInMeters` is omitted (NULL) when `o.accuracy` is NULL or 0 — never emits 0 (D-04 / ALIGN-04)."
    - "`license` is the canonical CC-BY-NC 4.0 `/legalcode` URI (per D-20 / §1.1) for every native row."
    - "Branch view UNION-ALL-compatible: column order, names, and types match plan 05-03's Maplify branch exactly."
  artifacts:
    - path: supabase/migrations/20260617203900_dwc_schema.sql
      provides: "Appends CREATE VIEW dwc._native_occurrences."
      contains: "CREATE VIEW dwc._native_occurrences"
  key_links:
    - from: dwc._native_occurrences
      to: dwc.taxa_classification
      via: "JOIN tc ON tc.taxon_id = o.taxon_id"
      pattern: "JOIN dwc.taxa_classification"
    - from: dwc._native_occurrences
      to: public.contributors
      via: "JOIN c ON c.id = o.contributor_id"
      pattern: "JOIN public.contributors"
---

<objective>
Append `CREATE VIEW dwc._native_occurrences` to the Phase 5 migration. This branch projects `public.observations` joined to `public.contributors`, `dwc.taxa_classification`, and the single-row dataset constants (inlined here; the actual `dwc.datasets` view is added in plan 05-04) into DwC-aligned columns per 04-POLICY §3.1 and the RESEARCH column-by-column table.

Purpose: One inspectable per-source projection that the union view in 05-04 consumes. The leading-underscore name signals "internal" — Phase 6 reads `dwc.occurrences` (the union), never the branches.

Output: Same migration file, now containing `dwc.taxa_classification` (plan 05-01) plus `dwc._native_occurrences`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-db-projection-dwc-schema/05-CONTEXT.md
@.planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md
@.planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md
@.planning/phases/04-rights-data-model-policy-gate/04-POLICY.md
@supabase/migrations/20250903172708_initial_schema.sql
@supabase/migrations/20250915171505_sighting_policies.sql
@supabase/migrations/20260204013006_sightings_uses_contributors.sql
@supabase/migrations/20250924160210_detect_individuals.sql
@.planning/phases/05-db-projection-dwc-schema/05-01-PLAN.md
</context>

<critical_notes>
**Source table is `public.observations`, NOT `public.sightings`.** 04-POLICY §3.1 still uses the old name; the on-disk schema renamed it on 2025-09-15 (`20250915171505_sighting_policies.sql:35`). FK column is `observation_id` (not `sighting_id`). RESEARCH §"CRITICAL Schema Discrepancies" Discrepancy 1.

**Both branch views must emit identical column count, order, names, and types.** `CREATE VIEW dwc.occurrences AS … UNION ALL …` will raise at view creation if either differs (Pitfall 4). This plan establishes the canonical column order; plan 05-03 mirrors it exactly.

**`extract_identifiers` returns NULL (not `'{}'`) on no match.** For `dynamicProperties` we WANT the NULL to propagate so `jsonb_strip_nulls` drops the key — do NOT wrap in `COALESCE(..., '{}')`. Pitfall 6 in RESEARCH.

**Casts for type discipline:** `eventDate` MUST be cast to `text` explicitly (not `timestamptz`) — see Pitfall 4 and RESEARCH §"ISO-8601 eventDate Formatting" "Single-column type discipline". `dynamicProperties` MUST be cast `::text` (not left as `jsonb`).
</critical_notes>

<interface_contract>
**Branch view column contract (canonical, mirrored by plan 05-03):**

The view emits columns in this exact order, all double-quoted to preserve case for `UNION ALL` and PostgREST/DuckDB consumers:

| # | Column (quoted) | Type | Native expression |
|---|-----------------|------|-------------------|
| 1 | `"occurrenceID"` | text | `'salishsea:' \|\| o.id::text` |
| 2 | `"basisOfRecord"` | text | `'HumanObservation'::text` |
| 3 | `"eventDate"` | text | `to_char(o.observed_at AT TIME ZONE 'UTC', 'YYYY-MM-DD"T"HH24:MI:SS"Z"')` |
| 4 | `"scientificName"` | text | `tc.scientific_name` |
| 5 | `"taxonRank"` | text | `tc.taxon_rank` |
| 6 | `"kingdom"` | text | `tc.kingdom` |
| 7 | `"phylum"` | text | `tc.phylum` |
| 8 | `"class"` | text | `tc.class` |
| 9 | `"order"` | text | `tc.order_` |
| 10 | `"family"` | text | `tc.family` |
| 11 | `"genus"` | text | `tc.genus` |
| 12 | `"decimalLatitude"` | double precision | `gis.ST_Y(o.subject_location::gis.geometry)` |
| 13 | `"decimalLongitude"` | double precision | `gis.ST_X(o.subject_location::gis.geometry)` |
| 14 | `"geodeticDatum"` | text | `'WGS84'::text` |
| 15 | `"coordinateUncertaintyInMeters"` | integer | `NULLIF(o.accuracy, 0)` |
| 16 | `"individualCount"` | integer | `o.count::integer` (smallint widened) |
| 17 | `"occurrenceStatus"` | text | `'present'::text` |
| 18 | `"occurrenceRemarks"` | text | `NULLIF(TRIM(regexp_replace(o.body, '<[^>]+>', '', 'g')), '')` |
| 19 | `"recordedBy"` | text | `c.name` |
| 20 | `"rightsHolder"` | text | `c.name` |
| 21 | `"datasetName"` | text | `'SalishSea.io Cetacean Occurrences (v1.2)'::text` (dataset title; matches dwc.datasets row added in plan 05-04) |
| 22 | `"datasetID"` | text | `'https://salishsea.io/datasets/occurrences-v1'::text` |
| 23 | `"license"` | text | `'https://creativecommons.org/licenses/by-nc/4.0/legalcode'::text` |
| 24 | `"dynamicProperties"` | text | jsonb expression cast `::text`, see Task 2 |
| 25 | `"informationWithheld"` | text | `NULL::text` (Maplify mirrors) |

Rationale: identical types enable `UNION ALL`; identical names enable DuckDB COPY in Phase 6 to map column → DwC term by header.
</interface_contract>

<tasks>

<task type="auto">
  <name>Task 1: Append CREATE VIEW dwc._native_occurrences with scalar columns and joins</name>
  <files>supabase/migrations/20260617203900_dwc_schema.sql</files>
  <read_first>
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"DwC Column Specification" — the full column table.
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"PostGIS Axis Convention" — `ST_Y` for lat, `ST_X` for lon, `::gis.geometry` cast required.
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"ISO-8601 eventDate Formatting" — `to_char(o.observed_at AT TIME ZONE 'UTC', 'YYYY-MM-DD"T"HH24:MI:SS"Z"')`.
    - .planning/phases/04-rights-data-model-policy-gate/04-POLICY.md §3.1 (native gap table, column-by-column) and §2.1 (D-09, `recordedBy` = `rightsHolder` = `contributors.name`) and §1.1 (D-20, native license URI).
    - supabase/migrations/20260204013006_sightings_uses_contributors.sql — `public.observations.contributor_id INTEGER REFERENCES contributors(id)`.
    - supabase/migrations/20250903172708_initial_schema.sql — columns on `public.observations`: `id uuid`, `observed_at timestamptz NOT NULL`, `body varchar(2000)`, `count smallint CHECK (count IS NULL OR count > 0)`, `direction public.travel_direction`, `taxon_id INTEGER REFERENCES inaturalist.taxa(id)`, `subject_location gis.geography(Point,4326)`, `accuracy INTEGER`.
    - The interface_contract block above for column ordering and exact expressions.
  </read_first>
  <action>
    Append a single statement `CREATE VIEW dwc._native_occurrences AS SELECT … FROM public.observations o JOIN public.contributors c ON c.id = o.contributor_id JOIN dwc.taxa_classification tc ON tc.taxon_id = o.taxon_id;` to `supabase/migrations/20260617203900_dwc_schema.sql`.

    Emit every column listed in `<interface_contract>` above, in that exact order, each double-quoted to preserve case. Specifically:
    - `"occurrenceID"` is `'salishsea:' || o.id::text` (ALIGN-06: stable, deterministic, source-prefixed).
    - `"basisOfRecord"` is the literal `'HumanObservation'::text` (POLICY §3.1).
    - `"eventDate"` uses `to_char(o.observed_at AT TIME ZONE 'UTC', 'YYYY-MM-DD"T"HH24:MI:SS"Z"')` — text type, Z-suffixed UTC, T delimiter. Do NOT use the bare `::text` cast on a `timestamptz` (Pitfall: yields `2024-03-15 14:30:00+00`, which strict ISO-8601 parsers reject).
    - `"scientificName"`, `"taxonRank"`, `"kingdom"`, `"phylum"`, `"class"`, `"order"` (alias `tc.order_`), `"family"`, `"genus"` from `dwc.taxa_classification` (joined as `tc`).
    - `"decimalLatitude"` = `gis.ST_Y(o.subject_location::gis.geometry)`; `"decimalLongitude"` = `gis.ST_X(o.subject_location::gis.geometry)`. Always cast `::gis.geometry` first (per existing `public.occurrences` pattern). Axis-swap is the single most failure-prone DwC mistake — Verification §10(c) tests a Salish Sea point.
    - `"geodeticDatum"` = `'WGS84'::text` (POLICY §3.1, ALIGN-04 constant).
    - `"coordinateUncertaintyInMeters"` = `NULLIF(o.accuracy, 0)` — explicit `0` → NULL collapse per POLICY §3.1 "Omit when NULL — never 0" and ALIGN-04.
    - `"individualCount"` = `o.count::integer` (widen `smallint` to `integer` for `UNION ALL` parity with Maplify's `number_sighted integer`; per Pitfall 4 ensure type parity by explicit cast). The `count CHECK (count > 0)` constraint is already in the source; no fabrication needed (D-13).
    - `"occurrenceStatus"` = `'present'::text` (D-12, constant per POLICY §3.4).
    - `"occurrenceRemarks"` = `NULLIF(TRIM(regexp_replace(o.body, '<[^>]+>', '', 'g')), '')` — HTML-strip then NULL-collapse empty strings, per POLICY §3.1 "Strip HTML".
    - `"recordedBy"` = `c.name` and `"rightsHolder"` = `c.name` (per D-09, POLICY §2.1).
    - `"datasetName"` = the literal `'SalishSea.io Cetacean Occurrences (v1.2)'::text` (matches the single row in `dwc.datasets` added in plan 05-04 — must stay in sync; if plan 05-04 picks a different title, update here).
    - `"datasetID"` = the literal `'https://salishsea.io/datasets/occurrences-v1'::text` (D-17, POLICY §6.3 — slug per CONTEXT M-discretion).
    - `"license"` = `'https://creativecommons.org/licenses/by-nc/4.0/legalcode'::text` per D-20 / POLICY §1.1.
    - `"informationWithheld"` = `NULL::text` (POLICY §2.4 leaves this optional; do not emit static text in v1.2).

    For `"dynamicProperties"` in this task, emit a placeholder `NULL::text AS "dynamicProperties"`. Task 2 replaces this with the actual `jsonb_strip_nulls(jsonb_build_object(...))::text` expression. Splitting the column-skeleton task from the JSON-assembly task keeps the diff reviewable.

    End with `COMMENT ON VIEW dwc._native_occurrences IS 'Encodes 04-POLICY §3.1 (native gap table). Internal — Phase 6 reads dwc.occurrences (the UNION), never this branch directly.';`
  </action>
  <verify>
    <automated>grep -q 'CREATE VIEW dwc._native_occurrences' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "'salishsea:' || o.id::text" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "gis.ST_Y(o.subject_location" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "gis.ST_X(o.subject_location" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "NULLIF(o.accuracy, 0)" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "by-nc/4.0/legalcode" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q 'JOIN public.contributors c' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q 'JOIN dwc.taxa_classification tc' supabase/migrations/20260617203900_dwc_schema.sql</automated>
  </verify>
  <done>
    The view definition exists with all 25 columns in the contracted order. Lat/lon use the correct PostGIS axes (`ST_Y` = lat, `ST_X` = lon). `coordinateUncertaintyInMeters` uses `NULLIF(o.accuracy, 0)`. `eventDate` uses the Z-suffixed `to_char` format. The CC-BY-NC `/legalcode` license URI is emitted as a constant. Joins reference `public.observations`, `public.contributors`, and `dwc.taxa_classification` (NOT `public.sightings`/`sighting_photos`). `dynamicProperties` is a placeholder `NULL::text`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace dynamicProperties placeholder with jsonb_strip_nulls expression (native key set)</name>
  <files>supabase/migrations/20260617203900_dwc_schema.sql</files>
  <read_first>
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"`dynamicProperties` JSON Construction" → "Native branch" — the canonical `jsonb_strip_nulls(jsonb_build_object(...))::text` pattern.
    - .planning/phases/04-rights-data-model-policy-gate/04-POLICY.md §2.3 — the key definition table; native rows carry only `travelDirection` and `unvalidatedIdentifiers`; Maplify-only keys (`aggregatorSource`, `aggregatorChain`, `countIsMinimum`) must NOT appear here.
    - supabase/migrations/20250924160210_detect_individuals.sql — `public.extract_identifiers(text) returns varchar[]` returns NULL (not empty array) on no match.
    - supabase/migrations/20250903172708_initial_schema.sql — `public.travel_direction` enum + `public.extract_travel_direction(body text)` (NOT used here — that's for Maplify; native uses `o.direction::text` directly).
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"Anti-Patterns to Avoid" — "Calling `extract_identifiers` without `COALESCE`": for `dynamicProperties` we want the OPPOSITE — let NULL propagate so `jsonb_strip_nulls` drops the key.
  </read_first>
  <action>
    Edit `supabase/migrations/20260617203900_dwc_schema.sql` to replace the `NULL::text AS "dynamicProperties"` placeholder from Task 1 with this expression:

    `NULLIF(jsonb_strip_nulls(jsonb_build_object('travelDirection', o.direction::text, 'unvalidatedIdentifiers', NULLIF(public.extract_identifiers(o.body), ARRAY[]::varchar[])))::text, '{}'::text) AS "dynamicProperties"`

    Concrete encoding requirements:
    - Cast `o.direction` (a `public.travel_direction` enum) to `text` — `jsonb_build_object` needs a value Postgres can encode as a JSON scalar.
    - For `unvalidatedIdentifiers`, call `public.extract_identifiers(o.body)` (varchar[]). Per Pitfall 6 / Anti-Patterns: do NOT wrap in `COALESCE(..., '{}')` — we WANT NULL to propagate so `jsonb_strip_nulls` drops the key entirely. The outer `NULLIF(..., ARRAY[]::varchar[])` is belt-and-suspenders against a future regex change that returns an empty array instead of NULL.
    - `jsonb_strip_nulls` removes any key whose value is `null`.
    - `::text` cast converts the resulting `jsonb` to text — Phase 6 treats `dynamicProperties` as opaque text (POLICY §5.4) and CSV COPY emits text.
    - Outer `NULLIF(..., '{}'::text)` collapses an entirely-empty object to NULL, so rows with no dynamic properties at all carry NULL instead of literal `{}`.

    Do NOT add `aggregatorSource`, `aggregatorChain`, or `countIsMinimum` — those are Maplify-branch keys (POLICY §2.3). `countIsMinimum` is also a no-op for v1.2 (D-14, HappyWhale out of scope).

    The view's `COMMENT ON VIEW` from Task 1 remains. The diff for this task should be confined to the single column expression.
  </action>
  <verify>
    <automated>grep -q "jsonb_strip_nulls(jsonb_build_object('travelDirection', o.direction" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "public.extract_identifiers(o.body)" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "AS \"dynamicProperties\"" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; ! grep -q "aggregatorSource.*o.direction" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; ! grep -q "aggregatorChain.*o.body" supabase/migrations/20260617203900_dwc_schema.sql</automated>
  </verify>
  <done>
    `dwc._native_occurrences.dynamicProperties` is the JSON-strip-nulls expression with exactly two keys (`travelDirection` from `o.direction::text` and `unvalidatedIdentifiers` from `public.extract_identifiers(o.body)`), `::text` cast, and outer `NULLIF(..., '{}')`. No Maplify-only keys (`aggregatorSource`/`aggregatorChain`/`countIsMinimum`) leak into this branch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `public.contributors` → `dwc.*` | `contributors.name` is contributor identity, intentionally exposed as `rightsHolder` AND `recordedBy` per D-09 (POLICY §2.1). T-05-02 (informational). |
| `inaturalist.taxa` → `dwc._native_occurrences` (via `dwc.taxa_classification`) | Read-only join through helper view; same risk surface as plan 05-01's T-05-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information disclosure | `recordedBy` / `rightsHolder` = `contributors.name` exposes contributor identity | accept | Intentional per D-09 / POLICY §2.1; contributor consent basis is D-08. Future "anonymize" requests must respect D-09 first. |
| T-05-04 | Tampering | Recursive CTE in `dwc.taxa_classification` (covered in plan 05-01) reached through this branch's JOIN | mitigate | Plan 05-01's fully-qualified references protect this branch transitively; no additional work here. |
</threat_model>

<verification>
Plan-level checks (after both tasks committed):
- `grep -c 'CREATE VIEW dwc._native_occurrences' supabase/migrations/20260617203900_dwc_schema.sql` returns exactly 1.
- `grep -v '^--' supabase/migrations/20260617203900_dwc_schema.sql | grep -c 'public.sightings\b'` returns 0 — no reference to the old (pre-rename) name.
- `grep -v '^--' supabase/migrations/20260617203900_dwc_schema.sql | grep -c '"decimalLatitude".*ST_Y'` returns at least 1 (axis correctness asserted at the file level).
- `grep -v '^--' supabase/migrations/20260617203900_dwc_schema.sql | grep -c '"decimalLongitude".*ST_X'` returns at least 1.
- Full assertion suite runs in plan 05-04; this plan stops at file authorship.
</verification>

<success_criteria>
- `CREATE VIEW dwc._native_occurrences` exists with the canonical 25-column interface contract above.
- All four GBIF-required terms are non-null by construction (`occurrenceID`, `basisOfRecord`, `scientificName`, `eventDate`) — ALIGN-02.
- Lat/lon correctness: `decimalLatitude` = `ST_Y`, `decimalLongitude` = `ST_X` (ALIGN-04).
- `geodeticDatum` is constant `'WGS84'`; `coordinateUncertaintyInMeters` uses `NULLIF(o.accuracy, 0)` (ALIGN-04).
- `eventDate` is Z-suffixed full-precision UTC text (ALIGN-05).
- `occurrenceID` is `'salishsea:' || o.id::text` (ALIGN-06).
- `license` is the CC-BY-NC `/legalcode` URI (D-20, POLICY §1.1).
- `dynamicProperties` is a `jsonb_strip_nulls`-wrapped expression with native-only keys (no Maplify keys leak in).
- View body uses `public.observations` / `public.contributors` (NOT `public.sightings`).
</success_criteria>

<output>
Create `.planning/phases/05-db-projection-dwc-schema/05-02-SUMMARY.md`: column-by-column table for `dwc._native_occurrences` (mapping DwC term → SQL expression), explicit confirmation that Discrepancy 1 (rename) is honored, note that `dynamicProperties` carries exactly two native-only keys, and the interface contract being mirrored in plan 05-03.
</output>
