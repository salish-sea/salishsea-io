---
phase: 05-db-projection-dwc-schema
plan: 04
type: execute
wave: 4
depends_on:
  - 05-02
  - 05-03
files_modified:
  - supabase/migrations/20260617203900_dwc_schema.sql
  - supabase/snippets/05_dwc_assertions.sql
  - .planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md
autonomous: true
requirements:
  - ALIGN-01
  - ALIGN-02
  - ALIGN-03
  - ALIGN-04
  - ALIGN-05
  - ALIGN-06
must_haves:
  truths:
    - "`SELECT * FROM dwc.occurrences` returns the UNION ALL of `dwc._native_occurrences` and `dwc._maplify_occurrences` — no other sources; iNaturalist + HappyWhale are absent by construction (ALIGN-01)."
    - "`dwc.datasets` returns exactly one row with `dataset_id = 'https://salishsea.io/datasets/occurrences-v1'`, `intellectual_rights = 'https://creativecommons.org/licenses/by-nc/4.0/legalcode'`, `contact_email = 'rainhead@gmail.com'` (M-03, M-04, D-15..D-18)."
    - "`dwc.multimedia` returns one row per non-`'none'`, non-NULL-license `public.observation_photos` row, joined to its observation's contributor for `creator`/`rightsHolder` (POLICY §3.3 + §1.2 converter)."
    - "`anon` and `authenticated` hold `SELECT` on every relation in schema `dwc` (Pitfall 5)."
    - "After `supabase db reset`, `psql -v ON_ERROR_STOP=1 -f supabase/snippets/05_dwc_assertions.sql` exits 0 — every ALIGN-01..06 + M-05 + D-15/16 + D-19 + D-20 assertion passes."
  artifacts:
    - path: supabase/migrations/20260617203900_dwc_schema.sql
      provides: "Final migration with all six views + grants — schema (plan 05-01), branches (plans 05-02/03), union + datasets + multimedia + final grants (this plan)."
      contains: "CREATE VIEW dwc.occurrences"
    - path: supabase/snippets/05_dwc_assertions.sql
      provides: "psql assertion harness; exit code 0 = all ALIGN-01..06 + M-05 + D-15/16 + D-19/D-20 contracts hold."
      contains: "\\set ON_ERROR_STOP on"
  key_links:
    - from: dwc.occurrences
      to: "dwc._native_occurrences + dwc._maplify_occurrences"
      via: "SELECT * FROM _native UNION ALL SELECT * FROM _maplify"
      pattern: "UNION ALL"
    - from: dwc.multimedia
      to: dwc.occurrences
      via: "coreId join — 'salishsea:' || op.observation_id::text matches occurrenceID"
      pattern: "salishsea:"
    - from: dwc.occurrences.datasetID
      to: dwc.datasets.dataset_id
      via: "join key (both = 'https://salishsea.io/datasets/occurrences-v1' in v1.2)"
      pattern: "occurrences-v1"
---

<objective>
Close the Phase 5 migration: append `dwc.occurrences` (the UNION), `dwc.datasets` (view over a single VALUES row per M-03), `dwc.multimedia` (per RESEARCH §"Multimedia Extension"), and the final blanket `GRANT SELECT`. Then apply the migration to the local DB via `supabase db reset` and run the assertion suite at `supabase/snippets/05_dwc_assertions.sql` (created here) to prove every ALIGN-01..06 + M-05 + D-15..D-20 truth holds.

Purpose: This is the integration plan — the moment everything compiles together (UNION ALL forces type parity between branches) and the assertion harness proves the contract end-to-end.

Output: A fully-encoded migration that applies cleanly via `supabase db reset`, an assertion script that exits 0, and an updated `05-VALIDATION.md` with task IDs filled in and `nyquist_compliant: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-db-projection-dwc-schema/05-CONTEXT.md
@.planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md
@.planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md
@.planning/phases/04-rights-data-model-policy-gate/04-POLICY.md
@.planning/phases/04-rights-data-model-policy-gate/04-SUMMARY.md
@supabase/migrations/20250903172708_initial_schema.sql
@supabase/migrations/20250915171505_sighting_policies.sql
@.planning/phases/05-db-projection-dwc-schema/05-01-PLAN.md
@.planning/phases/05-db-projection-dwc-schema/05-02-PLAN.md
@.planning/phases/05-db-projection-dwc-schema/05-03-PLAN.md
</context>

<critical_notes>
**Discrepancy 2 reminder.** `dwc.multimedia`'s license CASE encodes a NULL arm that is currently unreachable on `public.observation_photos.license_code` (still `varchar(20) NOT NULL`). Per RESEARCH Option 1 (recommended), encode the CASE faithfully to POLICY §1.2 with an inline SQL comment marking the NULL arm as forward-compat. The `WHERE` clause then excludes both `'none'` (D-19 terminal) and `IS NULL` (D-19 non-terminal) so v1.2 behavior matches POLICY §1.4.

**`dwc.multimedia` is native-only.** Maplify photos lack a license column on `maplify.sightings.photo_url` (POLICY §1.4); they are excluded entirely. The DWCA-03 readiness assertion (anti-join check) is satisfied because every `dwc.multimedia.coreId` (= `'salishsea:' || op.observation_id::text`) corresponds to a native row in `dwc.occurrences`.

**Push command:** `supabase db reset` is the required `[BLOCKING]` step between writing the migration and running assertions. Use `supabase db reset` (not `supabase migration up`) per `<schema_push_requirement>` "Recommended for this phase" — assertions need a deterministic data shape from the seed.

**Do not regenerate `database.types.ts`.** RESEARCH Open Question 3: `dwc.*` is for Phase 7's DuckDB consumer, not the app runtime. Adding TS types here is churn the frontend doesn't use.

**Do not modify `supabase/config.toml`.** `dwc` stays out of `api.schemas` and `extra_search_path` (Pitfall 5).
</critical_notes>

<tasks>

<task type="auto">
  <name>Task 1: Append dwc.occurrences (UNION ALL), dwc.datasets (VALUES view), dwc.multimedia, and final grants</name>
  <files>supabase/migrations/20260617203900_dwc_schema.sql</files>
  <read_first>
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"Recommended File Layout" (sequence 7–10).
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"Pattern 1: View-as-export-contract" (the literal `dwc.occurrences = SELECT * FROM _native UNION ALL SELECT * FROM _maplify;`).
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"Single-row dataset `VALUES`-backed view (M-03)" — the full 19-column VALUES literal.
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"Multimedia Extension Column Set" and §"`dwc.multimedia` skeleton" (the full CREATE VIEW).
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"`license_code` CASE Pattern (Per-Photo Conversion)" — the CC-URI map including the D-19 commented branches.
    - .planning/phases/04-rights-data-model-policy-gate/04-POLICY.md §1.2 (per-photo converter), §1.4 (license-less exclusion), §3.3 (multimedia gap table), §6 (D-15..D-18 dataset model), §6.7 (Phase 5 vs Phase 6 ownership).
    - supabase/migrations/20250903172708_initial_schema.sql — `public.observation_photos (id, observation_id uuid, seq smallint, href varchar, license_code varchar(20) NOT NULL)`; `public.observations.contributor_id INTEGER REFERENCES contributors(id)`.
    - The Discrepancy 2 callout in the migration file's header (added in plan 05-01) — confirms the NULL arm is intentional.
  </read_first>
  <action>
    Append four more statements to `supabase/migrations/20260617203900_dwc_schema.sql`, in this order:

    **(A) `CREATE VIEW dwc.occurrences AS SELECT * FROM dwc._native_occurrences UNION ALL SELECT * FROM dwc._maplify_occurrences;`** — the union view (M-02). If the branch views drift in column count, name, or type, Postgres raises here at view-creation; that's the desired loud-failure semantic per RESEARCH "Pattern 1". Add `COMMENT ON VIEW dwc.occurrences IS 'Encodes 04-POLICY §3.1 (native) and §3.2 (Maplify) via M-02 UNION ALL of dwc._native_occurrences and dwc._maplify_occurrences. Read by Phase 6 (DuckDB ATTACH + COPY to CSV/GeoParquet).';`

    **(B) `CREATE VIEW dwc.datasets AS SELECT * FROM (VALUES (...)) AS d (...);`** — single-row view over a 19-column VALUES literal per RESEARCH §"Single-row dataset VALUES-backed view (M-03)". Concrete values:
    - `dataset_id` = `'https://salishsea.io/datasets/occurrences-v1'::text` (D-17 — slug per CONTEXT M-discretion).
    - `parent_dataset_id` = `NULL::text` (no parent in v1.2; D-16).
    - `title` = `'SalishSea.io Cetacean Occurrences (v1.2)'::text` — must match exactly the constant used in `dwc._native_occurrences."datasetName"` so the native rows display the correct dataset title.
    - `abstract` = a one-sentence placeholder per POLICY §6.7 "Phase 6 authors": `'Native and Maplify/Whale Alert cetacean sighting records from the Salish Sea region. Authored from observation tables in the SalishSea.io database, expressed as DarwinCore-aligned columns.'::text` (Phase 6 may refine in a follow-up migration).
    - `pub_date` = `CURRENT_DATE::text` (per RESEARCH; Phase 6 may overwrite with a constant date later).
    - `language` = `'en'::text`.
    - `intellectual_rights` = `'https://creativecommons.org/licenses/by-nc/4.0/legalcode'::text` (matches native record-level `license` per §6.6).
    - `creator_name` = `'SalishSea.io'::text` (D-18 organizational publisher).
    - `creator_email` = `'rainhead@gmail.com'::text` (M-04).
    - `creator_role` = `'originator'::text`.
    - `metadata_provider_name` = `'SalishSea.io'::text`.
    - `metadata_provider_email` = `'rainhead@gmail.com'::text` (M-04).
    - `contact_name` = `'Peter Abrahamsen'::text` (D-18 individual contact).
    - `contact_email` = `'rainhead@gmail.com'::text` (M-04).
    - `contact_role` = `'pointOfContact'::text`.
    - `geographic_coverage` = `NULL::text` (Phase 6 authors — POLICY §6.7).
    - `temporal_coverage` = `NULL::text` (Phase 6 computes at generation time — POLICY §6.5).
    - `taxonomic_coverage` = `'Cetacea (Order)'::text` (POLICY §6.5 "stated").
    - `methods` = `NULL::text` (Phase 6 authors — POLICY §6.7).
    Add `COMMENT ON VIEW dwc.datasets IS 'M-03 single-row dataset reification (D-15..D-18). Phase 6 reads this for EML. Future per-constituent rows added by extension migration (POLICY §6.2 sized for this).';`

    **(C) `CREATE VIEW dwc.multimedia AS …`** — per RESEARCH §"`dwc.multimedia` skeleton":
    - `FROM public.observation_photos op JOIN public.observations o ON o.id = op.observation_id JOIN public.contributors c ON c.id = o.contributor_id`.
    - Columns in this order, all double-quoted: `"coreId"` (= `'salishsea:' || op.observation_id::text` — matches `dwc.occurrences."occurrenceID"` for the join key DWCA-03 requires), `"type"` (= `'StillImage'::text`), `"identifier"` (= `op.href`), `"license"` (the CASE — see below), `"rightsHolder"` (= `c.name`), `"creator"` (= `c.name`).
    - `"license"` CASE: enumerate the seven CC enum members per POLICY §1.2 mapping to canonical `/legalcode` URIs (`cc0` → `publicdomain/zero/1.0/legalcode`, `cc-by` → `licenses/by/4.0/legalcode`, etc.). Add the explicit D-19 branches: `WHEN 'none' THEN NULL` (terminal, "no license") and `ELSE NULL` (catches `IS NULL`, "unknown / unclassified"). Immediately before the `WHEN 'none'` arm, embed a SQL comment: `/* D-19 / POLICY §1.2: NULL ('unknown') and 'none' ('no license') are semantically distinct; both excluded in v1.2 by the WHERE clause. NULL arm currently unreachable on public.observation_photos.license_code (NOT NULL constraint preserved); encoded for forward-compat with a future DROP NOT NULL — see Discrepancy 2 in 05-RESEARCH. */`
    - `WHERE op.license_code IS NOT NULL AND op.license_code <> 'none'` — POLICY §1.4 exclusion (both v1.2 cases). Per RESEARCH "Filter placement note", apply the filter in the view body, not inside the CASE.
    - `ORDER BY op.observation_id, op.seq` — RESEARCH note: Phase 6's COPY preserves this; do not expose `seq` as an output column (not a DwC term).
    - Add `COMMENT ON VIEW dwc.multimedia IS 'GBIF Simple Multimedia extension; encodes 04-POLICY §3.3 + §1.2 (license CASE) + §1.4 (none/NULL exclusion). Native photos only; Maplify photo_url is license-less per POLICY §1.4 and excluded. Phase 6 reads this for the Multimedia extension CSV.';`

    **(D) `GRANT SELECT ON ALL TABLES IN SCHEMA dwc TO anon, authenticated;`** — Pitfall 5 + RESEARCH §"Recommended File Layout" step 9. ("ALL TABLES" includes views.)

    Place statements in this order so dependencies resolve cleanly: branches are already authored (plans 05-02, 05-03); `dwc.datasets` is independent; `dwc.occurrences` depends on both branches; `dwc.multimedia` depends only on `public.*` tables (not on `dwc._native_occurrences`); final GRANT comes after every view exists.

    Do NOT add `dwc` to `supabase/config.toml:api.schemas` or `extra_search_path`. Do NOT regenerate `database.types.ts`.
  </action>
  <verify>
    <automated>grep -q 'CREATE VIEW dwc.occurrences AS SELECT \* FROM dwc._native_occurrences' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q 'UNION ALL' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q 'CREATE VIEW dwc.datasets' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q 'occurrences-v1' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "'rainhead@gmail.com'::text" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q 'CREATE VIEW dwc.multimedia' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "WHEN 'none' THEN NULL" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "license_code IS NOT NULL AND op.license_code &lt;&gt; 'none'" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q 'GRANT SELECT ON ALL TABLES IN SCHEMA dwc TO anon, authenticated' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; git diff --quiet supabase/config.toml</automated>
  </verify>
  <done>
    All four statements appended in the right order with the right values. `dwc.datasets` has exactly one row carrying the M-04 email and the M-03 slug. `dwc.multimedia` encodes the license CASE with the commented D-19 NULL arm and the WHERE-clause exclusion. Final blanket `GRANT SELECT` covers every view. `supabase/config.toml` is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author supabase/snippets/05_dwc_assertions.sql (assertion harness)</name>
  <files>supabase/snippets/05_dwc_assertions.sql</files>
  <read_first>
    - .planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md §"Per-Task Verification Map" (full assertion table).
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"Local Verification Harness" (the `\set ON_ERROR_STOP on` + `DO $$ ... RAISE EXCEPTION ... $$` skeleton).
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"Phase Requirements → Test Map" (alternative assertion phrasings for ALIGN-01..06, M-05, POLICY §1.4, DWCA-03 readiness, dataset wiring, license URIs).
  </read_first>
  <action>
    Create `supabase/snippets/05_dwc_assertions.sql` (the directory exists; the file is new).

    Header: `\set ON_ERROR_STOP on` followed by `\echo === Phase 5 DwC projection verification ===`.

    Emit one `DO $$ DECLARE n INTEGER; BEGIN SELECT … INTO n FROM …; IF (condition) THEN RAISE EXCEPTION '<REQ-ID> FAIL: …', n; END IF; END $$;` block per row in VALIDATION.md §"Per-Task Verification Map". Each block must:
    - Start with `\echo &lt;REQ-ID&gt;: &lt;short description&gt;` so the failing assertion is identifiable from psql stdout.
    - Use the exact assertion SQL from VALIDATION.md (each row has a copy-pasteable `SELECT …`).
    - Raise with a message that includes the requirement/policy reference and the failing count or sample.

    The minimum required assertion set (each maps to a VALIDATION.md row):
    - **ALIGN-01 (source filter):** prefixes from `dwc.occurrences` are exactly the set `{salishsea, maplify}` — query `SELECT array_agg(DISTINCT split_part("occurrenceID",':',1) ORDER BY 1) INTO arr FROM dwc.occurrences` and assert `arr = ARRAY['maplify','salishsea']::text[]` (or empty set is acceptable if local seed has zero rows of either source; if so, raise a warning notice but do not fail — clarify intent: "ALIGN-01 SKIP: dwc.occurrences is empty in local DB; assertion deferred to first data fixture").
    - **ALIGN-02 (required terms):** `SELECT COUNT(*) FROM dwc.occurrences WHERE "occurrenceID" IS NULL OR "basisOfRecord" IS NULL OR "scientificName" IS NULL OR "eventDate" IS NULL` — expect 0.
    - **ALIGN-03 (no fabricated binomial):** `SELECT COUNT(*) FROM dwc.occurrences WHERE "taxonRank" IN ('family','subfamily','superfamily','order','class','phylum','kingdom') AND "genus" IS NOT NULL` — expect 0.
    - **ALIGN-03 (taxonRank populated):** `SELECT COUNT(*) FROM dwc.occurrences WHERE "taxonRank" IS NULL` — expect 0.
    - **ALIGN-04 (lat/lon range):** `SELECT COUNT(*) FROM dwc.occurrences WHERE "decimalLatitude" NOT BETWEEN -90 AND 90 OR "decimalLongitude" NOT BETWEEN -180 AND 180` — expect 0.
    - **ALIGN-04 (axis sanity — Salish Sea point):** only run when `dwc.occurrences` has rows. If empty, `\echo ALIGN-04 axis-sanity SKIP: no rows`. If non-empty, `SELECT ABS("decimalLatitude" - 48.5) + ABS("decimalLongitude" + 123.0) INTO d FROM dwc.occurrences ORDER BY d LIMIT 1;` and assert the nearest point lands within `47 ≤ lat ≤ 50` AND `-125 ≤ lon ≤ -122`.
    - **ALIGN-04 (uncertainty ≠ 0):** `SELECT COUNT(*) FROM dwc.occurrences WHERE "coordinateUncertaintyInMeters" = 0` — expect 0.
    - **ALIGN-04 (geodeticDatum constant):** `SELECT COUNT(DISTINCT "geodeticDatum") FROM dwc.occurrences` — expect ≤ 1 (treat 0 as pass: empty table is fine).
    - **ALIGN-05 (Maplify date-only):** `SELECT COUNT(*) FROM dwc.occurrences WHERE "occurrenceID" LIKE 'maplify:%' AND "eventDate" LIKE '%T%'` — expect 0.
    - **ALIGN-05 (Native has time):** `SELECT COUNT(*) FROM dwc.occurrences WHERE "occurrenceID" LIKE 'salishsea:%' AND "eventDate" NOT LIKE '%T%'` — expect 0.
    - **ALIGN-06 (occurrenceID unique):** `SELECT COUNT(*) FROM (SELECT "occurrenceID" FROM dwc.occurrences GROUP BY 1 HAVING COUNT(*) > 1) dup` — expect 0.
    - **M-05 contract (genus NULL for family rank):** `SELECT COUNT(*) FROM dwc.taxa_classification tc JOIN inaturalist.taxa t ON t.id = tc.taxon_id WHERE t.rank IN ('family','subfamily','superfamily','order','class','phylum','kingdom') AND tc.genus IS NOT NULL` — expect 0.
    - **M-05 contract (one row per taxon):** assert `(SELECT COUNT(*) FROM dwc.taxa_classification) = (SELECT COUNT(*) FROM inaturalist.taxa)`.
    - **POLICY §1.4 (multimedia excludes none/NULL):** `SELECT COUNT(*) FROM dwc.multimedia WHERE "license" IS NULL` — expect 0.
    - **DWCA-03 readiness (multimedia coreId in occurrences):** `SELECT COUNT(*) FROM dwc.multimedia m LEFT JOIN dwc.occurrences o ON o."occurrenceID" = m."coreId" WHERE o."occurrenceID" IS NULL` — expect 0.
    - **D-15/D-16 wiring (datasetID joins datasets):** `SELECT COUNT(*) FROM dwc.occurrences o LEFT JOIN dwc.datasets d ON d.dataset_id = o."datasetID" WHERE d.dataset_id IS NULL` — expect 0.
    - **D-20 / §1.1 (license URIs):** `SELECT array_agg(DISTINCT "license" ORDER BY 1) INTO arr FROM dwc.occurrences` — assert (when non-empty) it is a subset of `ARRAY['https://creativecommons.org/licenses/by-nc/4.0/legalcode','https://creativecommons.org/licenses/by/4.0/legalcode']::text[]`.

    Empty-table handling: where an assertion would trivially pass on an empty `dwc.occurrences` (e.g., a NULL-check on no rows), `\echo` a `SKIP` line rather than `RAISE EXCEPTION`. Where empty IS a problem (e.g., `dwc.taxa_classification` empty when `inaturalist.taxa` has rows), the M-05 count-match assertion catches it.

    End with `\echo === All assertions passed ===`.

    Each `RAISE EXCEPTION` message should start with the requirement ID followed by `FAIL:` and include the failing count or value (per RESEARCH skeleton).
  </action>
  <verify>
    <automated>test -f supabase/snippets/05_dwc_assertions.sql &amp;&amp; grep -q '^\\set ON_ERROR_STOP on' supabase/snippets/05_dwc_assertions.sql &amp;&amp; grep -q -E "ALIGN-0[123456]" supabase/snippets/05_dwc_assertions.sql &amp;&amp; grep -q 'taxa_classification' supabase/snippets/05_dwc_assertions.sql &amp;&amp; grep -q 'dwc.multimedia' supabase/snippets/05_dwc_assertions.sql &amp;&amp; grep -q 'occurrenceID.*LIKE' supabase/snippets/05_dwc_assertions.sql &amp;&amp; grep -q 'RAISE EXCEPTION' supabase/snippets/05_dwc_assertions.sql</automated>
  </verify>
  <done>
    `supabase/snippets/05_dwc_assertions.sql` exists, starts with `\set ON_ERROR_STOP on`, contains at least one `RAISE EXCEPTION` block per ALIGN-01..06 row in VALIDATION.md, plus the M-05, POLICY §1.4, DWCA-03 readiness, dataset wiring, and D-20 license-URI assertions, with empty-table SKIP handling on assertions that would trivially pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push migration to local DB via supabase db reset</name>
  <files>(no files modified — local DB only)</files>
  <read_first>
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"Schema Push Command for Local Dev" — choice of `supabase db reset` over `migration up`.
    - The full migration file `supabase/migrations/20260617203900_dwc_schema.sql` (review one more time before pushing).
    - `~/.claude/projects/-Users-rainhead-dev-salishsea-io/memory/MEMORY.md` — confirms `supabase db reset` is safe locally.
  </read_first>
  <action>
    Run `supabase db reset` from the repo root. This:
    1. Drops the local Postgres database on port 54322.
    2. Re-runs every migration in `supabase/migrations/` (including the new `20260617203900_dwc_schema.sql`).
    3. Runs `supabase/seed.sql` (per `config.toml:db.seed.enabled = true`).

    Expected outcome: the command exits 0. If it fails:
    - `column count mismatch` on `dwc.occurrences` → the two branch views drifted; check Task 1 (B) added all 25 columns in the correct order in both branches (revisit plan 05-02 and 05-03's interface_contract).
    - `type mismatch` on `dwc.occurrences` → likely `individualCount` (smallint vs integer) or `eventDate` (timestamptz vs text). Re-cast in the offending branch and re-run.
    - `relation "inaturalist.taxa" does not exist` → the initial schema didn't apply; check upstream migration files for ordering.

    Do NOT run `supabase migration up`; the assertion suite needs the deterministic data shape from a clean reset.

    Do NOT regenerate `database.types.ts` (RESEARCH Open Question 3).

    On success, immediately confirm view existence with `psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -c '\dv dwc.*'` — expect 6 entries: `dwc.taxa_classification`, `dwc._native_occurrences`, `dwc._maplify_occurrences`, `dwc.occurrences`, `dwc.datasets`, `dwc.multimedia`.
  </action>
  <verify>
    <automated>psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -t -A -c "SELECT array_agg(table_name ORDER BY table_name) FROM information_schema.views WHERE table_schema = 'dwc'" | grep -q "_maplify_occurrences" &amp;&amp; psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -t -A -c "SELECT array_agg(table_name ORDER BY table_name) FROM information_schema.views WHERE table_schema = 'dwc'" | grep -q "_native_occurrences" &amp;&amp; psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -t -A -c "SELECT COUNT(*) FROM information_schema.views WHERE table_schema = 'dwc'" | grep -q "^6$"</automated>
  </verify>
  <done>
    `supabase db reset` exits 0; the local DB has all six views in schema `dwc` (`taxa_classification`, `_native_occurrences`, `_maplify_occurrences`, `occurrences`, `datasets`, `multimedia`).
  </done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Run assertion suite; require psql exit 0</name>
  <files>(no files modified — assertions only)</files>
  <read_first>
    - .planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md (the validation contract).
    - `supabase/snippets/05_dwc_assertions.sql` (the script just created).
  </read_first>
  <action>
    Run: `psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -v ON_ERROR_STOP=1 -f supabase/snippets/05_dwc_assertions.sql`.

    Expected: every `\echo` line prints, "All assertions passed" prints last, exit code 0.

    On failure: the `RAISE EXCEPTION` message identifies the failing requirement ID and count. Fix the projection in `supabase/migrations/20260617203900_dwc_schema.sql`, re-run `supabase db reset`, re-run the assertion script. Repeat until exit 0.

    Common failure modes:
    - **ALIGN-02 FAIL** (required term NULL): a contributor row has no `name`, breaking `recordedBy`/`rightsHolder`. Per POLICY §2.1: "When the contributor display name is absent (NULL), both fields are omitted from the record." That language permits NULL for `recordedBy`/`rightsHolder` — but `scientificName` NULL means `dwc.taxa_classification` returned no row for that `taxon_id`, which is the bug. Investigate the recursive walk (plan 05-01).
    - **ALIGN-03 FAIL** (fabricated binomial): the explicit-rank-list CASE in `dwc.taxa_classification.genus` (plan 05-01 Task 2) missed a rank. Add the missing rank or audit the CASE.
    - **ALIGN-04 FAIL** (axis sanity): the lat/lon are swapped — `ST_Y` and `ST_X` were exchanged. Audit both branches.
    - **ALIGN-05 FAIL** (T in maplify eventDate): the date-only cast was replaced with the native `to_char` formatter, or the branch order in UNION ALL was swapped.

    On success, the phase's SQL contract is proven against local data. The Maplify QA pass (POLICY §4.5) and GBIF validator pass (DWCA-05) are out of Phase 5 scope.
  </action>
  <verify>
    <automated>psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -v ON_ERROR_STOP=1 -f supabase/snippets/05_dwc_assertions.sql</automated>
  </verify>
  <done>
    psql exits 0; the assertion suite's final `\echo === All assertions passed ===` prints.
  </done>
</task>

<task type="auto">
  <name>Task 5: Finalize 05-VALIDATION.md — fill task IDs, flip nyquist_compliant</name>
  <files>.planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md (existing scaffold; "TBD" in the Plan and Task ID columns).
    - The four plan files 05-01 through 05-04 (this one) — for the actual plan and task IDs.
  </read_first>
  <action>
    Edit `05-VALIDATION.md`:

    (1) In `## Per-Task Verification Map`, replace every `TBD` `Plan` cell with the plan number that establishes the assertion's premise:
      - The Wave-0 `supabase/snippets/05_dwc_assertions.sql` row → Plan `05-04`, Task `2`.
      - ALIGN-01..06 + ALIGN-04 (uncertainty/datum) + ALIGN-05 (date precision) + ALIGN-06 (uniqueness) rows → Plan `05-04`, Task `4` (the run that validates them all). These assertions are written in plan 05-04 task 2 but exercised in task 4.
      - M-05 rows (taxa_classification family/genus, one row per taxon) → Plan `05-04`, Task `4` (exercised by the harness; written in plan 05-01 task 2).
      - POLICY §1.4 (dwc.multimedia excludes none/NULL) → Plan `05-04`, Task `4`.
      - DWCA-03 readiness + dataset wiring + D-20 license rows → Plan `05-04`, Task `4`.

    (2) After Task 4 has exited 0 (confirmed in the previous task), flip the frontmatter `nyquist_compliant: false` → `nyquist_compliant: true` and `wave_0_complete: false` → `wave_0_complete: true`.

    (3) In `## Validation Sign-Off`, check each box and update `**Approval:**` to read `auto-approved by plan 05-04 task 4 — psql exit 0 on YYYY-MM-DD` (use today's date).
  </action>
  <verify>
    <automated>! grep -q "^| TBD" .planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md &amp;&amp; grep -q "nyquist_compliant: true" .planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md &amp;&amp; grep -q "wave_0_complete: true" .planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md</automated>
  </verify>
  <done>
    No `TBD` rows remain in the Per-Task Verification Map. `nyquist_compliant: true` and `wave_0_complete: true` set in frontmatter. Sign-off section updated.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
For downstream review and drift detection, the complete list of new symbols/files this phase introduces:

**New schema:** `dwc`.

**New views in schema `dwc`:**
- `dwc.taxa_classification` (plan 05-01 task 2)
- `dwc._native_occurrences` (plan 05-02 tasks 1–2)
- `dwc._maplify_occurrences` (plan 05-03 tasks 2–3)
- `dwc.occurrences` (this plan, task 1A)
- `dwc.datasets` (this plan, task 1B)
- `dwc.multimedia` (this plan, task 1C)

**New files:**
- `supabase/migrations/20260617203900_dwc_schema.sql` (plans 05-01..05-04 all append; final file)
- `supabase/snippets/05_dwc_assertions.sql` (plan 05-04 task 2)

**Files modified (not new):**
- `.planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md` (plan 05-04 task 5: task-ID fill-in + nyquist_compliant flip)

**Files NOT modified:**
- `supabase/config.toml` (intentionally untouched — `dwc` not added to `api.schemas` or `extra_search_path`).
- `database.types.ts` (not regenerated; RESEARCH Open Question 3).
- `supabase/seed.sql` (M-03 keeps dataset metadata in the migration, not seed).
- Any other migration file in `supabase/migrations/` (Phase 5 is additive).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `public.observation_photos` → `dwc.multimedia` | Native photos only; `WHERE license_code IS NOT NULL AND <> 'none'` excludes license-less rows per POLICY §1.4. |
| `dwc.occurrences` UNION ALL boundary | Postgres enforces column count/type parity at view creation; no runtime check needed. |
| Local DB only | `dwc` schema not exposed via PostgREST. Production deploy must NOT add `dwc` to `api.schemas` either. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | `dwc.multimedia` and `dwc.occurrences` published via PostgREST | mitigate | `supabase/config.toml:api.schemas` unchanged; verification grep in Task 1 confirms. Production deploy must apply the same migration without amending `api.schemas`. |
| T-05-02 | Information disclosure | `c.name` in `dwc.multimedia.creator`/`rightsHolder` (inherits from observation) | accept | Same intentional D-09 exposure as plan 05-02; no new surface. |
| T-05-04 | Tampering | recursive CTE in `dwc.taxa_classification` reached through `dwc.occurrences` | mitigate | Plan 05-01's fully-qualified references protect transitively. |
</threat_model>

<verification>
End-to-end verification (after all five tasks complete):
- `supabase db reset` exits 0.
- `psql … -v ON_ERROR_STOP=1 -f supabase/snippets/05_dwc_assertions.sql` exits 0.
- All six views exist in schema `dwc` per Task 3's `\dv dwc.*` check.
- `git diff supabase/config.toml` is empty.
- `git diff database.types.ts` is empty (RESEARCH Open Question 3).
- `05-VALIDATION.md` has `nyquist_compliant: true` and zero `TBD` rows.

Manual-only verifications (out of Phase 5 scope per VALIDATION.md):
- `SELECT DISTINCT source FROM maplify.sightings` audit against production (handled in plan 05-03 task 1).
- GBIF DwC-A validator pass (Phase 6 / DWCA-05).
- Maplify QA pass (POLICY §4.5; out-of-band review, gates Phase 8 link only).
</verification>

<success_criteria>
- All five tasks complete.
- All six `dwc.*` views exist and are queryable by `anon` / `authenticated`.
- Assertion script exits 0 → every ALIGN-01..06 + M-05 + D-15/16 + D-19 + D-20 contract holds.
- `05-VALIDATION.md` frontmatter shows `nyquist_compliant: true`.
- `supabase/config.toml` untouched; `database.types.ts` untouched.
</success_criteria>

<output>
Create `.planning/phases/05-db-projection-dwc-schema/05-04-SUMMARY.md`. Capture:
- Final migration file size + view count (6).
- Assertion suite count (one block per VALIDATION row) and the exit-0 confirmation.
- The `dwc.datasets` single-row dataset_id and contact_email actually committed (M-03/M-04 evidence).
- Any failures encountered during db reset / assertion runs and how they were fixed.
- A one-line note that Phase 6 reads `dwc.occurrences` + `dwc.multimedia` + `dwc.datasets` via DuckDB ATTACH and never re-decides a column (POLICY §6.7).
</output>
