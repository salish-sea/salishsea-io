---
phase: 09-reference-table-foundation
verified: 2026-06-19T00:00:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 0
deferred:
  - truth: "export emits recordedByID when an ORCID is present (CONTRIB-02 export clause)"
    addressed_in: "Phase 12"
    evidence: "Phase 12 goal: 'dwc.occurrences view emits 26 columns with correct attribution'; ATTR-01 requires recordedBy from contributor. The CONTEXT.md explicitly states: 'recordedByID emission from ORCID is a Phase 12 export concern.'"
---

# Phase 9: Reference Table Foundation — Verification Report

**Phase Goal:** The provenance graph's reference tables (`providers`, `organizations`, `collections`) exist, are seeded with canonical data, and are readable by all consumers — unblocking every FK addition and backfill that follows. Plus a nullable `orcid` column on the existing `public.contributors`.
**Verified:** 2026-06-19
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `SELECT * FROM public.providers` returns exactly 4 rows (direct, maplify, inaturalist, happywhale); anon SELECT > 0 | VERIFIED | DB query: `providers_count=4`, `provider_slugs=direct, happywhale, inaturalist, maplify`; anon SC-1 block passes in assertion gate |
| 2 | `SELECT * FROM public.organizations` returns parent institutions; every row has non-null `url`; anon SELECT > 0 | VERIFIED | DB query: `org_null_url=0`, `org_slugs=cascadia, mbari, orca-network, orcasound, tmmc`; assertion gate SC-2 exits 0 |
| 3 | `SELECT * FROM public.collections` returns ~15+ canonical collections; correct `kind` enum; `aggregator_ingest` absent; anon SELECT > 0 | VERIFIED | DB query: `collections_count=21`, `named_collections_with_kind=10`, `stub_collections_null_kind=11`, `aggregator_ingest_in_enum=0`; `collection_kind_values=facebook_group, research_dataset, acoustic_feed, detector, direct_app`; assertion gate SC-3 exits 0 |
| 4 | `public.contributors` has nullable `orcid` column | VERIFIED | DB query: `orcid_is_nullable=YES`; information_schema introspection confirmed in assertion gate SC-4 |
| 5 | Per-provider contributor model intact; no `provider_id` on `public.contributors`; no cross-provider merge | VERIFIED | DB query: `provider_id_on_contributors=0`; SC-5 structural check passes |
| 6 | anon/authenticated cannot INSERT/UPDATE/DELETE on any reference table (RLS write-closed) | VERIFIED | Assertion gate T-09-01: INSERT raises `insufficient_privilege` on all 3 tables; UPDATE/DELETE affect 0 rows on all 3 tables (WR-01 addressed); DB confirms `write_policies_count=0` |
| 7 | `aggregator_ingest` is absent from `public.collection_kind` enum by construction | VERIFIED | DB query: `aggregator_ingest_in_enum=0`; failed-cast assertion in SC-3 raises `invalid_text_representation` as expected |
| 8 | `providers` and `collections` (and `organizations`) carry IDENTITY PK + UNIQUE slug; seed is idempotent via ON CONFLICT (slug) | VERIFIED | Migration uses `INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY` + `UNIQUE` + `ON CONFLICT (slug) DO NOTHING` (grep confirmed); migration applies cleanly twice from reset |
| 9 | Collections include exactly one baseline collection per non-aggregator provider (iNaturalist, HappyWhale, salishsea-direct) per D-03 | VERIFIED | DB query confirms: `inaturalist/direct_app`, `happywhale/direct_app`, `salishsea-direct/direct_app` all present with non-null kind |
| 10 | `supabase db reset` applies the migration cleanly; assertion snippet exits 0 | VERIFIED | `supabase db reset` exit 0 (all 32 migrations including `20260619184037_reference_tables.sql` applied); `psql -f 09_reference_assertions.sql` exits 0 with `=== All Phase 9 assertions passed ===` |

**Score:** 10/10 truths verified

### Deferred Items

Items not yet met but explicitly addressed in later milestone phases.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | export emits `recordedByID` when an ORCID is present (CONTRIB-02 export clause) | Phase 12 | Phase 12 requirements include ATTR-01 (`recordedBy` from contributor); CONTEXT.md D-11 states "recordedByID emission from ORCID is a Phase 12 export concern" |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/20260619184037_reference_tables.sql` | collection_kind enum + 3 tables + RLS + orcid column + seed | VERIFIED | 148 lines; contains `CREATE TYPE public.collection_kind`, `CREATE TABLE public.providers/organizations/collections`, `ENABLE ROW LEVEL SECURITY`, `CREATE POLICY ... FOR SELECT ... USING (true)` (x3), `ON CONFLICT (slug) DO NOTHING` (x3), `ADD COLUMN IF NOT EXISTS orcid TEXT`, `REFERENCES public.organizations`, no `GRANT SELECT`, no `aggregator_ingest` in non-comment lines |
| `supabase/snippets/09_reference_assertions.sql` | SC-1..SC-5 + RLS write-closed check; min 120 lines | VERIFIED | 311 lines; covers all 5 SCs plus T-09-01 with INSERT/UPDATE/DELETE checks on all 3 tables; `SET ROLE anon` appears 4+ times; `aggregator_ingest` in failed-cast block; `is_nullable` check for SC-4; `insufficient_privilege` exception handler present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `20260619184037_reference_tables.sql` | `public.collections.organization_id` | `REFERENCES public.organizations(id)` FK, seeded via slug subquery | VERIFIED | Line 93: `organization_id INTEGER REFERENCES public.organizations(id)`; slug subquery pattern used; DB confirms `org_backed_null_fk=0` (FK resolved for all 5 org-backed collections) |
| `20260619184037_reference_tables.sql` | anon, authenticated SELECT policy | `ENABLE ROW LEVEL SECURITY + CREATE POLICY ... FOR SELECT USING (true)` | VERIFIED | DB confirms: `rls_enabled_providers/organizations/collections=true`; `select_policies_count=3`; `write_policies_count=0` |
| `09_reference_assertions.sql` | public.providers / organizations / collections / contributors | `SET ROLE anon` smoke SELECT + information_schema introspection + failed-cast enum check | VERIFIED | Assertion gate exits 0; `SET ROLE anon` used 4 times; WR-02 FK-resolution check present; WR-01 UPDATE/DELETE row-count checks present |

### Data-Flow Trace (Level 4)

Not applicable — this is a pure SQL migration phase with no app code or components that render dynamic data. The "data flow" is the seed rows being readable via PostgREST; verified by the `SET ROLE anon; SELECT COUNT(*)` blocks in the assertion gate.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| providers has exactly 4 rows with correct slugs | `psql ... -c "SELECT count(*), string_agg(slug,...) FROM public.providers"` | count=4, slugs=direct,happywhale,inaturalist,maplify | PASS |
| organizations has 5 rows, all with non-null url | `psql ... -c "SELECT count(*), count(*) FILTER (WHERE url IS NULL) FROM public.organizations"` | count=5, null_url=0 | PASS |
| collections has 21 rows (10 named + 11 stubs) | `psql ... -c "SELECT count(*), count(*) FILTER (WHERE kind IS NOT NULL) FROM public.collections"` | total=21, named=10 | PASS |
| aggregator_ingest absent from enum | `psql ... -c "SELECT count(*) FROM pg_enum WHERE enumlabel='aggregator_ingest' AND enumtypid='public.collection_kind'::regtype"` | 0 | PASS |
| orcid column is nullable on contributors | `psql ... -c "SELECT is_nullable FROM information_schema.columns WHERE ... column_name='orcid'"` | YES | PASS |
| RLS write-closed on all 3 tables | assertion gate T-09-01 | INSERT rejected, UPDATE/DELETE affect 0 rows | PASS |
| FK subqueries resolved (no NULL org_id on org-backed collections) | `psql ... -c "SELECT count(*) FROM public.collections WHERE slug IN ('orca-network',...) AND organization_id IS NULL"` | 0 | PASS |
| FK index on collections.organization_id | `psql pg_indexes` query | count=1 | PASS |
| assertion gate | `psql -v ON_ERROR_STOP=1 -f 09_reference_assertions.sql` | exit 0, `=== All Phase 9 assertions passed ===` | PASS |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| `supabase db reset` + `09_reference_assertions.sql` | `supabase db reset && psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -v ON_ERROR_STOP=1 -f supabase/snippets/09_reference_assertions.sql` | exit 0, all 32 migrations applied, `=== All Phase 9 assertions passed ===` | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| PROV-01 | 09-01-PLAN.md | `providers` table exists, seeded with 4 ingest providers, with SELECT grants | SATISFIED | 4 rows confirmed in DB; anon SELECT passes in SC-1 |
| ORG-01 | 09-01-PLAN.md | `organizations` table exists with name, url, rights-holder text; seeded with parent institutions | SATISFIED | 5 rows, all with non-null url and non-null rights_holder_text confirmed in DB; anon SELECT passes in SC-2 |
| COLL-01 | 09-01-PLAN.md | `collections` table with `kind` enum (no `aggregator_ingest`), nullable `organization_id` FK, ~15 canonical collections | SATISFIED | 21 rows (10 named + 11 stubs), enum has 5 values excluding `aggregator_ingest`, FK confirmed; anon SELECT passes in SC-3 |
| CONTRIB-01 | 09-01-PLAN.md | Contributors modeled on existing `public.contributors`, per-provider, no cross-provider merge | SATISFIED | `provider_id` column absent from contributors; table structure unchanged; SC-5 passes |
| CONTRIB-02 | 09-01-PLAN.md | `public.contributors` has nullable `orcid` column (export emission deferred to Phase 12) | SATISFIED (column half); DEFERRED (export half → Phase 12) | `orcid` column confirmed nullable via information_schema; `recordedByID` emission is explicitly a Phase 12 concern per D-11 and CONTEXT.md |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | — | — | No debt markers (TBD/FIXME/XXX), no stubs, no placeholder returns found in either artifact |

### Code-Review Warning Resolution

The REVIEW.md identified 3 warnings and 3 info items. Verified disposition:

| Finding | REVIEW Status | Disposition in Committed Code |
|---------|--------------|-------------------------------|
| WR-01: Write-closed assertion proved only INSERT | Warning | ADDRESSED — snippet now includes UPDATE/DELETE row-count checks via `GET DIAGNOSTICS affected = ROW_COUNT` loop on all 3 tables; T-09-01 block exercises INSERT (raises), UPDATE (0 rows), DELETE (0 rows) for providers, organizations, and collections |
| WR-02: No assertion that FK subqueries resolved | Warning | ADDRESSED — snippet includes two SC-3 blocks: one asserting 0 org-backed collections with NULL organization_id, another checking no dangling FK values |
| WR-03: Row-count guard too loose (`< 10` for 21-row seed) | Warning | PARTIALLY ADDRESSED — guard tightened to `< 20` (not exact `= 21`), which is a meaningful improvement that would catch losing all 11 stubs or all 10 named collections. Losing exactly 1-9 rows would still pass. This is a minor residual gap in assertion tightness, not a blocker — the seed produces exactly 21 rows confirmed by live DB query |
| IN-01: Stub NULL-kind invariant only half-checked | Info | ADDRESSED — snippet now asserts `count(*) WHERE kind IS NULL >= 10` (the stub side) |
| IN-02: SC-2 doesn't verify org count or rights_holder_text | Info | ADDRESSED — SC-2 now includes `count(*) >= 5` check and `rights_holder_text IS NULL` zero-row check |
| IN-03: SC-5 structural-only | Info | Acknowledged as intentional; `provider_id` absence check is the meaningful structural assertion; NOTICE for row count is informational only |

### Human Verification Required

None. This is a pure SQL migration phase. All success criteria are observable via database queries and have been verified directly against the live local Supabase instance.

### Gaps Summary

No gaps found. All 10 must-have truths are VERIFIED by direct database evidence. The phase goal is fully achieved: three reference tables exist in the live database with canonical seed data, RLS SELECT policies permitting anon access, write-closedness confirmed for all three verbs on all three tables, the `orcid` nullable column exists on `public.contributors`, and the assertion gate exits 0 from a clean `supabase db reset`.

The one partial item in CONTRIB-02 (the export emission of `recordedByID`) is deliberately deferred to Phase 12 and is documented as such in CONTEXT.md D-11; it does not affect Phase 9 goal achievement.

---

_Verified: 2026-06-19_
_Verifier: Claude (gsd-verifier)_
