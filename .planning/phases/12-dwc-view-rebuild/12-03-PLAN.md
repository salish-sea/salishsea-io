---
phase: 12-dwc-view-rebuild
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - scripts/dwca/eml.ts
  - scripts/dwca/eml.test.ts
  - scripts/dwca/build.ts
  - scripts/dwca/build.test.ts
autonomous: true
requirements: [ATTR-04, ATTR-03]
must_haves:
  truths:
    - "The EML emits a <associatedParty role=contentProvider> element for each upstream org represented in the export"
    - "Only orgs with exported rows are credited (data-driven, not all 5 seeded orgs)"
    - "Upstream orgs appear as associatedParty, never as institutionCode"
    - "Org name and URL are XML-escaped in the EML"
    - "EML <title> reflects v1.3"
    - "The build pipeline passes the associated-parties list into buildEml"
    - "The nightly row-count guard reads the rebuilt (trusted-only) view unchanged"
    - "npm test passes green"
  artifacts:
    - path: "scripts/dwca/eml.ts"
      provides: "AssociatedParty interface, EmlInput.associatedParties, <associatedParty> rendering"
      exports: ["AssociatedParty", "EmlInput", "buildEml"]
      contains: "associatedParty"
    - path: "scripts/dwca/eml.test.ts"
      provides: "associatedParty presence/placement/escaping/empty tests; v1.3 title"
      contains: "associatedParty"
    - path: "scripts/dwca/build.ts"
      provides: "Build-time associated-parties query + pass-through to buildEml"
      contains: "associatedParties"
  key_links:
    - from: "scripts/dwca/build.ts (Step 15.5 query)"
      to: "public.organizations via collections (trusted Maplify + native)"
      via: "DuckDB runAndReadAll + getRowObjects"
      pattern: "associatedParties"
    - from: "scripts/dwca/build.ts"
      to: "scripts/dwca/eml.ts buildEml"
      via: "buildEml({ datasets, temporalCoverage, associatedParties })"
      pattern: "buildEml\\("
    - from: "scripts/dwca/eml.ts buildEml"
      to: "EML <dataset> document"
      via: "<associatedParty> block before <pubDate>, xmlEsc on name/url"
      pattern: "<associatedParty>"
---

<objective>
Surface upstream organizations in the archive's EML as structured
`<associatedParty>` elements (ATTR-04) — never as `institutionCode`. Add an
`AssociatedParty` interface and `associatedParties` field to `EmlInput`; render
each party as `<associatedParty><organizationName/><onlineUrl/><role>contentProvider
</role></associatedParty>` (D-07/D-09) with `xmlEsc` on all values, placed in the
GBIF EML 2.1.1 schema-correct position (after `<metadataProvider>`, before
`<pubDate>`). Feed the list from a build-time query in `build.ts` that selects
DISTINCT org name/url across only the collections with exported rows (D-08 —
data-driven, not all 5 seeded orgs; trusted-only Maplify + native, mirroring the
view filters). Bump the EML test's mock title to v1.3. The nightly row-count
guard (`guard.ts`) needs no code change — it reads the rebuilt trusted-only view
automatically (ATTR-03 runtime guard) — confirm this with a no-change check.

Purpose: ATTR-04 (upstream orgs as associatedParty) and the ATTR-03 runtime
guard. This depends on the LIVE rebuilt 26-column view from Plan 02 because the
build-time associated-parties query mirrors the trusted-row filter.
Output: Extended EML builder, the build-time party query, updated tests, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dwc-view-rebuild/12-CONTEXT.md
@.planning/phases/12-dwc-view-rebuild/12-RESEARCH.md
@.planning/phases/12-dwc-view-rebuild/12-PATTERNS.md
@.planning/phases/12-dwc-view-rebuild/12-VALIDATION.md
@.planning/phases/12-dwc-view-rebuild/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add associatedParty to the EML builder (eml.ts + eml.test.ts)</name>
  <files>scripts/dwca/eml.ts, scripts/dwca/eml.test.ts</files>
  <read_first>
    - scripts/dwca/eml.ts (EmlInput interface ~lines 75-81; buildEml; xmlEsc ~lines 91-98; the <dataset> document — title/creator/metadataProvider/pubDate ordering at lines 150-163; methodsPara2 hardcoded org prose at lines 134-142)
    - scripts/dwca/eml.test.ts (mockInput ~lines 48-51; mockDatasets v1.2 title ~line 26 + the <title> assertion ~line 78; the "required elements present" toContain tests)
    - .planning/phases/12-dwc-view-rebuild/12-RESEARCH.md (§Pattern 6 EML associatedParty structure + AssociatedParty interface; Pitfall 7 placement; §Pattern 7 v1.3 bump locations)
    - .planning/phases/12-dwc-view-rebuild/12-PATTERNS.md (the exact eml.ts AssociatedParty interface, EmlInput extension, associatedPartyXml block, eml.test.ts test cases)
    - .planning/phases/12-dwc-view-rebuild/12-CONTEXT.md (D-07 structured associatedParty; D-08 only-represented orgs; D-09 role=contentProvider; institutionCode is NEVER the org credit)
  </read_first>
  <behavior>
    - buildEml(mockInput) contains one <associatedParty> block per input party, each with <organizationName>, <onlineUrl>, <role>contentProvider</role>
    - The first <associatedParty> appears AFTER <metadataProvider> and BEFORE <pubDate> (GBIF EML 2.1.1 sequence; also before <coverage>)
    - buildEml({...mockInput, associatedParties: []}) contains NO <associatedParty> element
    - An org name 'Org & Co' renders as <organizationName>Org &amp; Co</organizationName> (xmlEsc); a URL with '&' is escaped
    - The EML <title> contains 'v1.3'
  </behavior>
  <action>
    In `eml.ts`: add the `AssociatedParty` interface (`{ readonly name: string;
    readonly url: string; readonly role: 'contentProvider' }`) and add
    `readonly associatedParties: readonly AssociatedParty[]` to `EmlInput`. Build
    an `associatedPartyXml` string by mapping `input.associatedParties` to
    `<associatedParty><organizationName>${xmlEsc(p.name)}</organizationName>
    <onlineUrl>${xmlEsc(p.url)}</onlineUrl><role>contentProvider</role>
    </associatedParty>` and interpolate it between the `<metadataProvider>` block
    and `<pubDate>` (GBIF EML 2.1.1 sequence: title, creator*, metadataProvider*,
    associatedParty*, pubDate — Pitfall 7). When `associatedParties` is empty, the
    mapped string is empty (no element emitted). Leave `methodsPara2` (the
    hardcoded "Orca Network and Cascadia Research Collective" prose) UNCHANGED —
    it remains factually accurate (RESEARCH §Pattern 6).

    In `eml.test.ts`: add `associatedParties` to `mockInput` (Orca Network +
    Cascadia Research Collective, role contentProvider); change the mock title and
    the `<title>` assertion from `(v1.2)` to `(v1.3)`; add tests for
    presence-per-party, placement (associatedParty index < pubDate index AND <
    coverage index), empty-list (no `<associatedParty>`), and xmlEsc of an
    org name/url containing `&`.
  </action>
  <verify>
    <automated>npm test -- --run scripts/dwca/eml.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/dwca/eml.ts` exports `AssociatedParty` and `EmlInput.associatedParties`; `buildEml` renders `<associatedParty>` with `<role>contentProvider</role>`
    - `npm test -- --run scripts/dwca/eml.test.ts` exits 0
    - A test asserts `<associatedParty>` appears before `<pubDate>` (and before `<coverage>`)
    - A test asserts empty `associatedParties` produces no `<associatedParty>` element
    - A test asserts `xmlEsc` is applied (e.g. `Org &amp; Co`)
    - The EML title assertion checks `v1.3`
  </acceptance_criteria>
  <done>buildEml renders data-driven, XML-escaped associatedParty elements in the schema-correct position; eml.test.ts passes green with the v1.3 title.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the build-time associated-parties query into build.ts</name>
  <files>scripts/dwca/build.ts, scripts/dwca/build.test.ts</files>
  <read_first>
    - scripts/dwca/build.ts (Step 15 temporal coverage; Step 16 datasets read at lines 335-344 — the runAndReadAll + getRowObjects pattern; Step 17 buildEml call at line 348; TAB_COLLAPSE_COLS lines 60-66)
    - scripts/dwca/build.test.ts (how build steps are tested/mocked — confirm whether the new query needs a test stub)
    - scripts/dwca/eml.ts (buildEml signature after Task 1 — now requires associatedParties)
    - .planning/phases/12-dwc-view-rebuild/12-RESEARCH.md (§Pattern 6 — the build-time party query SQL, sorted ORDER BY name; Open Question Q2 build-time vs view; Q3 alphabetical sort)
    - .planning/phases/12-dwc-view-rebuild/12-PATTERNS.md (the exact Step 15.5 query block + Step 17 update)
    - .planning/phases/12-dwc-view-rebuild/12-CONTEXT.md (D-08 only-represented orgs; trusted-only Maplify)
  </read_first>
  <action>
    In `build.ts`, add a Step 15.5 between temporal coverage (Step 15) and the
    datasets read (Step 16): run a DuckDB `runAndReadAll` query selecting DISTINCT
    `org.name, org.url` from `maplify.sightings s JOIN public.collections c ON
    c.id = s.collection_id JOIN public.organizations org ON org.id =
    c.organization_id` filtered `WHERE s.trusted = TRUE AND NOT s.is_test AND
    s.number_sighted BETWEEN 1 AND 1000 AND s.source != 'rwsas'` UNION the same
    join over `public.observations o`, `ORDER BY name` (D-08 data-driven, trusted
    filter mirrors the view; alphabetical for determinism per Q3). Map rows to
    `{ name, url, role: 'contentProvider' as const }` via `getRowObjects()`
    (mirrors the Step 16 datasets pattern). Pass `associatedParties` into the
    Step 17 `buildEml({ datasets: datasetsRow, temporalCoverage, associatedParties })`
    call. `TAB_COLLAPSE_COLS` needs no change (institutionCode is a constant with
    no tabs; datasetName is already in the set).

    If `build.test.ts` mocks the DB query sequence, add the corresponding stub for
    the new Step 15.5 query so the test suite stays green; otherwise no test edit
    is needed.
  </action>
  <verify>
    <automated>npm test -- --run scripts/dwca/build.test.ts &amp;&amp; grep -q "associatedParties" scripts/dwca/build.ts &amp;&amp; grep -q "org.organization_id\\|organizations org" scripts/dwca/build.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/dwca/build.ts` contains a build-time query joining `public.organizations` via `public.collections` with `WHERE s.trusted = TRUE` (and the native UNION branch), `ORDER BY name`
    - `buildEml(...)` in build.ts is called with `associatedParties`
    - `npm test -- --run scripts/dwca/build.test.ts` exits 0
    - The query mirrors the view's Maplify filters (trusted, not is_test, number_sighted 1-1000, source != 'rwsas')
  </acceptance_criteria>
  <done>build.ts queries the represented orgs (trusted-only) and passes the list to buildEml; build.test.ts passes green.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the row-count guard reads the rebuilt view (no-change verification) + full suite</name>
  <files>scripts/dwca/guard.ts, scripts/dwca/guard.test.ts</files>
  <read_first>
    - scripts/dwca/guard.ts (ROW_FLOOR env-overridable default 1000 at line 41; the `SELECT COUNT(*) FROM pgdb.dwc.occurrences` at line 104)
    - scripts/dwca/guard.test.ts (DuckDB-mocked tests; confirm no hardcoded row count)
    - .planning/phases/12-dwc-view-rebuild/12-RESEARCH.md (§Pattern 8 — guard needs no code change; trusted-only shrinks count but stays > 1000; Assumption A1)
    - .planning/phases/12-dwc-view-rebuild/12-PATTERNS.md (guard.ts no-change confirmation)
  </read_first>
  <action>
    Verify (no code change expected) that `guard.ts` already enforces the ATTR-03
    runtime row-count gate by querying `COUNT(*) FROM pgdb.dwc.occurrences` against
    the rebuilt trusted-only view, with the env-overridable `ROW_FLOOR=1000` floor
    (RESEARCH §Pattern 8 / Assumption A1: trusted-only Maplify stays well above
    1,000). Do NOT change the floor unless the Plan-02 prod application reveals the
    trusted count dropped below 1,000 (it will not, given ~6,800 prod rows). Run
    the full `npm test` suite to confirm cross-file parity (fields ↔ meta-xml ↔
    eml ↔ build ↔ guard) is coherent end-to-end (D-10 single-PR gate). If A1 is
    falsified at prod-apply time, document a `ROW_FLOOR` override rather than
    editing the constant.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/dwca/guard.ts` queries `COUNT(*) FROM pgdb.dwc.occurrences` (reads the rebuilt view) — confirmed, no code change required
    - `npm test -- --run` (full suite) exits 0 — fields, meta-xml, eml, build, guard, assertions all green together
    - If trusted-only count < 1000 surfaced at Plan-02 prod apply, a `ROW_FLOOR` env override is documented (not a constant edit)
  </acceptance_criteria>
  <done>The guard reads the rebuilt view unchanged; the full test suite is green, confirming the 26-column lockstep holds across all dwca files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public.organizations name/url → EML XML | Org-supplied strings rendered into the public EML document |
| dwc.occurrences row count → guard | Runtime gate that must fail if excluded sources inflate the export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-XML | Tampering | associatedParty org name/url in eml.ts | mitigate | `xmlEsc()` applied to both `p.name` and `p.url` before interpolation (extends the existing T-06-03-XML mitigation); a test asserts `&` → `&amp;`. |
| T-12-03-CREDIT | Information Disclosure | associated-parties build query | mitigate | Query is data-driven over only collections with exported rows (D-08); upstream orgs surface as `associatedParty` with role `contentProvider`, never as `institutionCode` (which stays the constant 'SalishSea'). No org with zero in-scope rows is credited. |
| T-12-03-GATE | Information Disclosure | row-count guard | mitigate | `guard.ts` enforces the ATTR-03 runtime gate over the rebuilt trusted-only `dwc.occurrences`; SRC-01 (iNat/HappyWhale) is excluded by the 2-branch UNION construction (Plan 02), so the gate confirms the export does not exceed the native + trusted-Maplify baseline. |
| T-12-03-SC | Tampering | npm installs | mitigate | No new packages this phase (RESEARCH §Standard Stack); no Package Legitimacy Gate needed. |
</threat_model>

<verification>
- `npm test -- --run` (full suite) passes green.
- EML emits XML-escaped `<associatedParty role=contentProvider>` for represented orgs only, before `<pubDate>`/`<coverage>`; title is v1.3.
- build.ts feeds the data-driven trusted-only party list into buildEml.
- guard.ts reads the rebuilt view unchanged (ATTR-03 runtime gate).
</verification>

<success_criteria>
- EML lists upstream organizations as `associatedParty` elements (never institutionCode); only orgs represented in the export are credited.
- Org name/URL XML-escaped; EML title reflects v1.3.
- The nightly guard enforces the SRC-01 row-count gate over the rebuilt trusted-only view.
- Full `npm test` green — the 26-column lockstep is coherent across all dwca files.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (exclude from drift verification):
- `AssociatedParty` interface in `scripts/dwca/eml.ts`
- `EmlInput.associatedParties` field
- The `<associatedParty>` rendering block + `associatedPartyXml` in `buildEml`
- The Step 15.5 build-time associated-parties query in `scripts/dwca/build.ts`
- The `associatedParties` argument threaded into the `buildEml(...)` call
- New `associatedParty` tests in `scripts/dwca/eml.test.ts` (presence/placement/empty/escaping) + v1.3 title update
- The `build.test.ts` Step 15.5 query stub (if mocking requires it)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-dwc-view-rebuild/12-03-SUMMARY.md` when done
</output>
