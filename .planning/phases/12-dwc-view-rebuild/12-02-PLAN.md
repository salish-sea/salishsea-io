---
phase: 12-dwc-view-rebuild
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - supabase/migrations/20260621000000_dwc_view_rebuild.sql
  - scripts/dwca/fields.ts
  - scripts/dwca/fields.test.ts
  - scripts/dwca/meta-xml.test.ts
  - supabase/snippets/12_dwc_assertions.sql
autonomous: false
requirements: [ATTR-01, ATTR-02, ATTR-03]
must_haves:
  truths:
    - "dwc.occurrences emits exactly 26 columns"
    - "Every exported row has institutionCode = 'SalishSea'"
    - "Every exported row has rightsHolder = 'SalishSea.io' (no contributor/org names)"
    - "Every datasetName is prefixed 'SalishSea.io — ' and is non-NULL"
    - "Maplify branch exports trusted rows only; iNat/HappyWhale excluded by 2-branch UNION construction"
    - "Maplify recordedBy is the headline observer name or NULL (never a multi-name list / ID credit / direction)"
    - "OCCURRENCE_FIELDS.length === 26 and assertFieldAlignment confirms view↔array parity"
    - "meta.xml declares 26 occurrence fields in correct ordinal order; institutionCode at index 19"
    - "npm test passes green"
  artifacts:
    - path: "supabase/migrations/20260621000000_dwc_view_rebuild.sql"
      provides: "DROP+CREATE of the three dwc occurrence views (26 cols) + dwc.datasets v1.3 bump"
      contains: "CREATE VIEW dwc._native_occurrences"
    - path: "scripts/dwca/fields.ts"
      provides: "26-entry OCCURRENCE_FIELDS with institutionCode at index 19"
      contains: "institutionCode"
    - path: "scripts/dwca/fields.test.ts"
      provides: "Parity assertions bumped to 26; dcterms pair moved to {20, 23}"
      contains: "toBe(26)"
    - path: "scripts/dwca/meta-xml.test.ts"
      provides: "Total field count 32 (26 occ + 6 mm); institutionCode index 19; dcterms {20,23}"
      contains: "32"
    - path: "supabase/snippets/12_dwc_assertions.sql"
      provides: "SC#1-SC#6 SQL assertions over the rebuilt views"
      contains: "institutionCode"
  key_links:
    - from: "scripts/dwca/fields.ts (OCCURRENCE_FIELDS)"
      to: "dwc._native_occurrences / dwc._maplify_occurrences column order"
      via: "assertFieldAlignment position-by-position parity"
      pattern: "institutionCode"
    - from: "dwc._maplify_occurrences"
      to: "public.collections (c.name) via s.collection_id"
      via: "LEFT JOIN + COALESCE fallback to 'Whale Alert (Global)'"
      pattern: "LEFT JOIN public.collections"
    - from: "dwc.occurrences"
      to: "dwc._native_occurrences + dwc._maplify_occurrences"
      via: "SELECT * UNION ALL (SRC-01 by construction, 26-col parity enforced at CREATE)"
      pattern: "UNION ALL"
---

<objective>
The coordinated 26-column core of Phase 12, landing as a single coherent change
so `npm test` parity is never transiently broken (D-10 single-PR lockstep).
DROP and recreate the three `dwc` occurrence views to emit 26 columns: add
`institutionCode` at ordinal 19; flip `rightsHolder` from per-person/per-org to
the constant `'SalishSea.io'`; replace the opaque `datasetName` bucket with a
per-collection `'SalishSea.io — ' || collection.name` FK join; add `AND s.trusted`
to the Maplify WHERE; derive Maplify `recordedBy` from the Wave-1-validated
view-time regex over `s.comments`; and bump `dwc.datasets` title v1.2 → v1.3.
In lockstep, add the `institutionCode` entry to `OCCURRENCE_FIELDS` (25→26),
update `fields.test.ts` and `meta-xml.test.ts` for the new ordinal + shifted
dcterms pair, and add the SC#1-SC#6 SQL assertion snippet. Then run the
`[BLOCKING]` schema push so the DROP+CREATE chain actually compiles against the
DB (UNION ALL enforces 26-column/type parity at CREATE VIEW time).

Purpose: This is the attribution flip itself — ATTR-01 (institutionCode /
rightsHolder / recordedBy), ATTR-02 (per-collection datasetName), and ATTR-03
(SRC-01 by construction). It is the highest-risk phase: reordering any field
without the matching migration edit silently corrupts the archive; the test gate
and the UNION-time parity check are the guardrails.
Output: New migration, 26-entry field array, updated tests, SC assertion snippet,
and the migration applied to the DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dwc-view-rebuild/12-CONTEXT.md
@.planning/phases/12-dwc-view-rebuild/12-RESEARCH.md
@.planning/phases/12-dwc-view-rebuild/12-PATTERNS.md
@.planning/phases/12-dwc-view-rebuild/12-VALIDATION.md
@.planning/phases/12-dwc-view-rebuild/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add institutionCode to the field contract (fields.ts + fields.test.ts + meta-xml.test.ts)</name>
  <files>scripts/dwca/fields.ts, scripts/dwca/fields.test.ts, scripts/dwca/meta-xml.test.ts</files>
  <read_first>
    - scripts/dwca/fields.ts (the 25-entry OCCURRENCE_FIELDS, header comment at lines 40-47, the dcterms-namespace divergence note)
    - scripts/dwca/fields.test.ts (EXPECTED_OCCURRENCE_NAMES, length assertion, dcterms position tests, non-dcterms URI invariant)
    - scripts/dwca/meta-xml.test.ts (total-count assertion = 31, dcterms position checks at pairs[19]/pairs[22], the dynamic ordinal-alignment test)
    - scripts/dwca/meta-xml.ts (buildMetaXml — pure function; no change needed, but read to confirm it derives from OCCURRENCE_FIELDS)
    - .planning/phases/12-dwc-view-rebuild/12-RESEARCH.md (§Pattern 3 institutionCode ordinal = index 19, termUri http://rs.tdwg.org/dwc/terms/institutionCode; the dcterms-shift impact on tests)
    - .planning/phases/12-dwc-view-rebuild/12-PATTERNS.md (the exact fields.ts / fields.test.ts / meta-xml.test.ts edit blocks)
    - .planning/phases/12-dwc-view-rebuild/12-CONTEXT.md (D-01 institutionCode is the new 26th column; D-10 lockstep)
  </read_first>
  <behavior>
    - OCCURRENCE_FIELDS.length === 26
    - OCCURRENCE_FIELDS[19] === { name: 'institutionCode', termUri: 'http://rs.tdwg.org/dwc/terms/institutionCode' }
    - The dcterms pair (rightsHolder, license) is at indices 20 and 23 (shifted from 19 and 22)
    - Every index ∉ {20, 23} carries a dwc/terms URI
    - buildMetaXml emits 32 total field elements (26 occurrence + 6 multimedia); core index 19 is institutionCode dwc/terms URI; core indices 20 and 23 are the dcterms pair
  </behavior>
  <action>
    Insert the `institutionCode` entry into `OCCURRENCE_FIELDS` at index 19,
    between `recordedBy` (18) and `rightsHolder` (now 20), with
    `termUri: 'http://rs.tdwg.org/dwc/terms/institutionCode'` (dwc/terms, NOT
    dcterms — RESEARCH §Pattern 3 / CONTEXT §Specifics). Update the file header
    comment (lines 40-47) from "25-entry" / "index 19 (rightsHolder) and 22
    (license)" to "26-entry" / "index 20 (rightsHolder) and 23 (license)".

    In `fields.test.ts`: insert `'institutionCode'` into `EXPECTED_OCCURRENCE_NAMES`
    at index 19; change the length assertion from `toBe(25)` to `toBe(26)`; move
    the dcterms position test from {19, 22} to {20, 23}; change the non-dcterms
    URI invariant exclusion set from `{19, 22}` to `{20, 23}`; add a new test
    asserting `OCCURRENCE_FIELDS[19]` is `institutionCode` with the dwc/terms URI.

    In `meta-xml.test.ts`: change the total-count assertion from `31` to `32`;
    move the dcterms `pairs[19]`/`pairs[22]` checks to `pairs[20]`/`pairs[23]`;
    add a new test asserting core `pairs[19]` is `['19', 'http://rs.tdwg.org/dwc/terms/institutionCode']`.
    The dynamic ordinal-alignment test (mapping OCCURRENCE_FIELDS to expected
    pairs) needs NO change — it derives from the array.

    `meta-xml.ts` itself needs NO change (pure function over OCCURRENCE_FIELDS).
  </action>
  <verify>
    <automated>npm test -- --run scripts/dwca/fields.test.ts scripts/dwca/meta-xml.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/dwca/fields.ts` contains `{ name: 'institutionCode', termUri: 'http://rs.tdwg.org/dwc/terms/institutionCode' }` and `OCCURRENCE_FIELDS.length === 26`
    - `npm test -- --run scripts/dwca/fields.test.ts` exits 0; the length test asserts `toBe(26)`; dcterms pair tests check indices 20 and 23
    - `npm test -- --run scripts/dwca/meta-xml.test.ts` exits 0; total-count test asserts 32; a test asserts core index 19 is the institutionCode dwc/terms URI
  </acceptance_criteria>
  <done>The TS field contract declares 26 occurrence fields with institutionCode at index 19; both affected test files pass green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 26-column view-rebuild migration + dwc.datasets v1.3 bump</name>
  <files>supabase/migrations/20260621000000_dwc_view_rebuild.sql</files>
  <read_first>
    - supabase/migrations/20260617203900_dwc_schema.sql (the CURRENT 25-col dwc._native_occurrences / dwc._maplify_occurrences / dwc.occurrences definitions, exact casts, the Maplify CROSS JOIN LATERAL CASE this replaces, the WHERE clause, and dwc.datasets VALUES at lines 568-580)
    - supabase/migrations/20260619184037_reference_tables.sql (public.collections name/slug, public.organizations — the JOIN targets)
    - supabase/migrations/20260620000100_resolution_backfill.sql (Phase 11 collection_id backfill — confirms maplify.sightings.contributor_id stays NULL, so recordedBy is a string not an FK)
    - .planning/phases/12-dwc-view-rebuild/maplify_trusted_comments_census.tsv (Wave 1 census output — the recordedBy regex is grounded here)
    - .planning/phases/12-dwc-view-rebuild/12-RESEARCH.md (§Pattern 1 DROP order, §Pattern 2 UNION cast parity, §Pattern 3 institutionCode ordinal, §Pattern 4 recordedBy regex, §Pattern 5 datasetName JOIN, §Pattern 7 v1.3 bump, §Pattern 8 trusted filter; Pitfalls 1-8)
    - .planning/phases/12-dwc-view-rebuild/12-PATTERNS.md (the exact native cols 19-25, Maplify cols 19-22, DROP order, dwc.datasets v1.3 block)
    - .planning/phases/12-dwc-view-rebuild/12-CONTEXT.md (D-01..D-06, D-10, D-11)
    - .agents/skills/supabase-postgres-best-practices/SKILL.md (SQL/view discipline)
  </read_first>
  <action>
    Create `supabase/migrations/20260621000000_dwc_view_rebuild.sql`. DROP the
    three views in reverse-dependency order: `DROP VIEW IF EXISTS dwc.occurrences;`
    then `dwc._maplify_occurrences;` then `dwc._native_occurrences;` (occurrences
    first — it depends on the branches; no CASCADE — RESEARCH Pitfall 8). Do NOT
    touch `dwc.multimedia` (it references public.observation_photos, not the
    occurrence views).

    Recreate `dwc._native_occurrences` (26 cols): keep cols 0-18 exactly as today.
    Insert col 19 `'SalishSea'::text AS "institutionCode"`. Set col 20
    `'SalishSea.io'::text AS "rightsHolder"` (was `c.name`). Set col 21
    `('SalishSea.io — ' || c_coll.name)::text AS "datasetName"` (was the v1.2
    constant; per-collection datasetName per D-04) via `JOIN public.collections c_coll ON c_coll.id = o.collection_id`
    (plain JOIN — observations.collection_id has a NOT NULL DEFAULT to
    'salishsea-direct' → 'SalishSea.io — SalishSea.io Direct' per D-04). Cols 22-25 (datasetID, license, dynamicProperties,
    informationWithheld) unchanged but shifted by one ordinal. Every scalar carries
    an explicit `::text`/type cast (UNION discipline).

    Recreate `dwc._maplify_occurrences` (26 cols, MUST match the native column
    names/types exactly). Add `AND s.trusted` to the WHERE (D-05). Replace the
    `s.source` 3-way CROSS JOIN LATERAL CASE with `LEFT JOIN public.collections
    c_coll ON c_coll.id = s.collection_id` (LEFT JOIN — collection_id can be NULL).
    Col 19 `recordedBy` = the Wave-1-validated regex:
    `NULLIF((CASE WHEN (regexp_match(split_part(s.comments, '<br>', 1), '^\[[^\]]+\]\s+.+?\(([^()]+)\)'))[1] ~ '[,]' OR (...)[1] ~ '^IDs?\s' THEN NULL ELSE (...)[1] END)::text, NULL)` — the comma guard and `^IDs?\s` guard NULL out multi-name lists and ID credits (Pitfall 3). Col 20 `'SalishSea'::text AS "institutionCode"`. Col 21 `'SalishSea.io'::text AS "rightsHolder"`. Col 22 `('SalishSea.io — ' || COALESCE(c_coll.name, 'Whale Alert (Global)'))::text AS "datasetName"` (D-06 fallback). datasetID/license/dynamicProperties/informationWithheld follow the same column list as the native branch, in the same order.

    NOTE: column ORDER in both branches must be identical and match
    OCCURRENCE_FIELDS exactly — recordedBy(18), institutionCode(19),
    rightsHolder(20), datasetName(21), datasetID(22), license(23),
    dynamicProperties(24), informationWithheld(25). (The native branch keeps
    recordedBy at 18 as `c.name`; only the Maplify recordedBy expression differs.)

    Recreate `dwc.occurrences AS SELECT * FROM dwc._native_occurrences UNION ALL
    SELECT * FROM dwc._maplify_occurrences;` (SRC-01 by construction — exactly two
    branches, no WHERE filter; 26-col/type parity enforced at CREATE — D-11).

    Bump `dwc.datasets`: `CREATE OR REPLACE VIEW dwc.datasets` (column count
    unchanged, so REPLACE works) with the `title` VALUES literal changed from
    `'SalishSea.io Cetacean Occurrences (v1.2)'` to `(v1.3)`. Leave `datasetID`
    (`/occurrences-v1` slug) and all other VALUES columns unchanged (Pitfall 4 /
    RESEARCH §Pattern 7 — only the human-readable title carries the version).

    Do NOT add any GRANT to the `dwc` views (security — the dwc schema is NOT
    PostgREST-exposed; no new GRANT may broaden access).
  </action>
  <verify>
    <automated>grep -q "DROP VIEW IF EXISTS dwc.occurrences" supabase/migrations/20260621000000_dwc_view_rebuild.sql &amp;&amp; grep -q "'SalishSea'::text" supabase/migrations/20260621000000_dwc_view_rebuild.sql &amp;&amp; grep -q "'SalishSea.io'::text" supabase/migrations/20260621000000_dwc_view_rebuild.sql &amp;&amp; grep -q "LEFT JOIN public.collections" supabase/migrations/20260621000000_dwc_view_rebuild.sql &amp;&amp; grep -q "AND s.trusted" supabase/migrations/20260621000000_dwc_view_rebuild.sql &amp;&amp; grep -q "v1.3" supabase/migrations/20260621000000_dwc_view_rebuild.sql &amp;&amp; ! grep -iqE "GRANT .*dwc\\." supabase/migrations/20260621000000_dwc_view_rebuild.sql &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The migration DROPs `dwc.occurrences` before `dwc._maplify_occurrences` before `dwc._native_occurrences` (no CASCADE)
    - Both branch views emit 26 columns in identical order: institutionCode at ordinal 19; rightsHolder='SalishSea.io'::text; datasetName via collection FK join
    - Native branch uses `JOIN public.collections`; Maplify branch uses `LEFT JOIN public.collections` + `COALESCE(c_coll.name, 'Whale Alert (Global)')`
    - Maplify WHERE includes `AND s.trusted`; recordedBy uses the comma + `^IDs?\s` NULL guards
    - `dwc.occurrences` is `SELECT * ... UNION ALL SELECT * ...` (exactly two branches; no WHERE)
    - `dwc.datasets` title VALUES literal is `(v1.3)`; `datasetID` slug unchanged
    - No `GRANT` on any `dwc.` object (no new exposure)
  </acceptance_criteria>
  <done>The migration file defines the three 26-col views (DROP+CREATE), the trusted filter, the FK-joined datasetName, the regex recordedBy, and the v1.3 datasets bump, with UNION cast discipline and no new GRANT.</done>
</task>

<task type="auto">
  <name>Task 3: Author the SC#1-SC#6 SQL assertion snippet</name>
  <files>supabase/snippets/12_dwc_assertions.sql</files>
  <read_first>
    - supabase/snippets/11_resolution_assertions.sql (header, `\set ON_ERROR_STOP on`, `DO $$ ... RAISE EXCEPTION` SC-block pattern, PROD-ONLY comment-out pattern, footer)
    - supabase/snippets/05_dwc_assertions.sql (Phase 5 ALIGN-shape assertions over dwc views)
    - .planning/phases/12-dwc-view-rebuild/12-PATTERNS.md (the exact SC#1-SC#6 blocks for 12_dwc_attribution_assertions.sql — reuse, but name the file 12_dwc_assertions.sql per VALIDATION)
    - .planning/phases/12-dwc-view-rebuild/12-RESEARCH.md (§Validation Architecture — req→test map; Wave 0 gaps)
    - .planning/ROADMAP.md (Phase 12 SC#1-SC#5 — the exact assertions to encode)
  </read_first>
  <action>
    Create `supabase/snippets/12_dwc_assertions.sql` following the
    `11_resolution_assertions.sql` shape: `\set ON_ERROR_STOP on`, a `\echo`
    banner, a run-comment block (local reset vs prod-pooler note — RESEARCH D-07:
    local reset is a no-op for prod-only Maplify rows, so the data-distinct
    assertions are meaningful only against prod), then one `DO $$ ... RAISE
    EXCEPTION 'SC#N FAIL: …' ... END $$;` block per criterion:

    SC#1 (ATTR-01): `COUNT(*) FROM dwc.occurrences WHERE "institutionCode" IS DISTINCT FROM 'SalishSea'` must be 0.
    SC#2 (ATTR-01): `... WHERE "rightsHolder" IS DISTINCT FROM 'SalishSea.io'` must be 0.
    SC#3 (ATTR-02): `... WHERE "datasetName" IS NULL OR "datasetName" NOT LIKE 'SalishSea.io — %'` must be 0.
    SC#4 (ATTR-03 / field contract): `COUNT(*) FROM information_schema.columns WHERE table_schema='dwc' AND table_name='occurrences'` must equal 26.
    SC#5 (ATTR-03 row-count): local/smoke — `COUNT(*) FROM dwc.occurrences` > 0; plus a PROD-ONLY commented block asserting the count does not exceed `(SELECT COUNT(*) FROM public.observations) + (SELECT COUNT(*) FROM maplify.sightings WHERE NOT is_test AND number_sighted BETWEEN 1 AND 1000 AND source != 'rwsas' AND trusted)` — the SRC-01 trusted-only ceiling.
    SC#6: `dwc.datasets` title `LIKE '%v1.3%'`.

    Mark the prod-data-dependent distinct/ceiling assertions with a `-- PROD-ONLY:`
    header (do not uncomment against `db reset`). Close with the
    `\echo === All Phase 12 local assertions passed ===` footer.
  </action>
  <verify>
    <automated>test -f supabase/snippets/12_dwc_assertions.sql &amp;&amp; grep -q "SC#1" supabase/snippets/12_dwc_assertions.sql &amp;&amp; grep -q "institutionCode" supabase/snippets/12_dwc_assertions.sql &amp;&amp; grep -q "SalishSea.io — " supabase/snippets/12_dwc_assertions.sql &amp;&amp; grep -q "= 26" supabase/snippets/12_dwc_assertions.sql &amp;&amp; grep -q "v1.3" supabase/snippets/12_dwc_assertions.sql &amp;&amp; grep -q "PROD-ONLY" supabase/snippets/12_dwc_assertions.sql &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/snippets/12_dwc_assertions.sql` exists with SC#1-SC#6 `DO $$ … RAISE EXCEPTION` blocks
    - SC#1 asserts `institutionCode` distinct = {'SalishSea'}; SC#2 asserts `rightsHolder` = {'SalishSea.io'}; SC#3 asserts every `datasetName LIKE 'SalishSea.io — %'` and non-NULL
    - SC#4 asserts `dwc.occurrences` has exactly 26 columns; SC#6 asserts datasets title contains v1.3
    - The PROD-ONLY row-count ceiling block references `observations + trusted maplify` (the SRC-01 ceiling) and is commented out for local
  </acceptance_criteria>
  <done>The assertion snippet encodes SC#1-SC#6 with the prod-only ceiling, ready to run against the rebuilt views.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] Apply the migration (supabase db push)</name>
  <files>supabase/migrations/20260621000000_dwc_view_rebuild.sql</files>
  <what-built>
    The view-rebuild migration must apply to the DB so the DROP+CREATE chain
    actually compiles — the UNION ALL enforces 26-column/type parity at CREATE
    VIEW time. Build/type checks pass WITHOUT the push, creating a false-positive
    verification state, so the push is mandatory before verification.
  </what-built>
  <action>
    Apply the new migration to the database after Tasks 1-3 are complete. Locally,
    `supabase db reset` re-applies all migrations (re-running the DROP+CREATE+UNION
    chain — the column-parity compile check fires here). For prod, the existing
    `main`-push deploy flow runs `supabase db push` (see project memory). Confirm
    the chain compiles cleanly, then run the local SC#4/SC#6 assertions. Set
    `SUPABASE_ACCESS_TOKEN` and `supabase link` for non-TTY application.
  </action>
  <how-to-verify>
    1. Apply the migration. Locally: `supabase db reset` re-applies all migrations
       (re-running the DROP+CREATE chain — the column-parity compile check fires
       here, but data-dependent SC#1/2/3/5 are no-ops locally per RESEARCH D-07).
       Non-TTY: ensure `SUPABASE_ACCESS_TOKEN` is set and `supabase link` is done.
    2. Confirm the migration applied with no error (no "cannot change number of
       columns", no "cannot drop … because other objects depend on it", no UNION
       type-mismatch). If the column count or a cast drifts between branches, the
       CREATE VIEW step fails loudly here — fix the branch parity and re-apply.
    3. PROD application: pushing `main` runs `supabase db push` in
       `.github/workflows/deploy.yml`, which applies this migration to prod (where
       the ~6,800 Maplify rows live). The data-dependent SC assertions (SC#1/2/3/5)
       are only meaningful against prod — run `supabase/snippets/12_dwc_assertions.sql`
       (uncommenting the PROD-ONLY blocks) against the prod IPv4 session pooler
       after the prod deploy, OR defer that prod verification to Phase 13.
    4. Local smoke: after `supabase db reset`, run `psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -v ON_ERROR_STOP=1 -f supabase/snippets/12_dwc_assertions.sql` — SC#4 (26 columns) and SC#6 (v1.3) and the local smoke SC#5 (non-empty if local has any rows) must pass; the PROD-ONLY distinct blocks stay commented.
  </how-to-verify>
  <verify>
    <automated>MISSING — operator-run schema push; see how-to-verify (local `supabase db reset` compile check + SC#4/SC#6 via psql). Data assertions SC#1/2/3/5 are prod-only per RESEARCH D-07.</automated>
  </verify>
  <acceptance_criteria>
    - The migration applies with no error (the DROP+CREATE+UNION chain compiles; 26-col/type parity holds across both branches)
    - Local: `psql … -f supabase/snippets/12_dwc_assertions.sql` passes SC#4 (=26 columns) and SC#6 (v1.3)
    - Prod application is via the existing `main`-push deploy flow (not a separate manual prod push); PROD-ONLY data assertions deferred to post-deploy / Phase 13
  </acceptance_criteria>
  <done>The migration compiles cleanly against the DB (26-col/type parity holds) and the local SC#4/SC#6 assertions pass; prod application is staged for the main-push deploy.</done>
  <resume-signal>Type "applied" once the migration compiles cleanly and the local SC#4/SC#6 assertions pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maplify.sightings.comments → dwc views | User-supplied free text parsed by view-time regex (read-only) |
| dwc views → offline archive | The public GBIF archive; must not widen exposure or leak excluded sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-EXPO | Information Disclosure | new dwc view definitions | mitigate | No `GRANT` added to any `dwc.` object; the dwc schema is not in PostgREST `api.schemas` (unchanged). Automated verify greps for absence of `GRANT … dwc.`. |
| T-12-02-SRC | Information Disclosure | dwc.occurrences UNION | mitigate | SRC-01 (iNat/HappyWhale exclusion) preserved by construction — `dwc.occurrences` is the UNION of exactly the two branches (native + Maplify), no third source can leak (D-11). SC#5 prod ceiling is the runtime guard. |
| T-12-02-PII | Information Disclosure | Maplify recordedBy regex | mitigate | Regex validated by the Wave-1 prod census (12-01); comma + `^IDs?\s` guards NULL out multi-name lists and ID credits so only intended observer-name attribution (D-02) reaches the public archive — no incidental non-name PII in parens. |
| T-12-02-DOS | Denial of Service | regexp_match over comments | accept | Postgres regex engine has linear guarantees on this pattern; `comments` is capped at varchar(2000); runs once per nightly offline build, not on a request path. |
| T-12-02-SC | Tampering | npm/supabase installs | mitigate | No new packages installed this phase (RESEARCH §Standard Stack — zero new deps); no Package Legitimacy Gate needed. |
</threat_model>

<verification>
- `npm test` passes green (fields + meta-xml + full suite).
- The migration applies cleanly (DROP+CREATE+UNION compiles; 26-col/type parity).
- `supabase/snippets/12_dwc_assertions.sql` SC#4 (26 cols) + SC#6 (v1.3) pass locally; SC#1/2/3/5 ready for prod.
- No new GRANT on any dwc object; SRC-01 preserved by 2-branch UNION.
</verification>

<success_criteria>
- `SELECT DISTINCT "institutionCode" FROM dwc.occurrences` = {'SalishSea'} (SC#1, prod).
- `SELECT DISTINCT "rightsHolder" FROM dwc.occurrences` = {'SalishSea.io'} (SC#2, prod).
- `SELECT DISTINCT "datasetName"` all prefixed 'SalishSea.io — ', non-NULL (SC#3, prod).
- `OCCURRENCE_FIELDS.length === 26`; assertFieldAlignment view↔array parity; meta.xml 26 fields in order (SC#4).
- `COUNT(*) FROM dwc.occurrences` ≤ observations + trusted Maplify; iNat/HappyWhale absent by construction (SC#5).
</success_criteria>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (exclude from drift verification):
- `supabase/migrations/20260621000000_dwc_view_rebuild.sql` — the new migration filename
- `institutionCode` column on `dwc._native_occurrences`, `dwc._maplify_occurrences`, `dwc.occurrences` (ordinal 19)
- The `{ name: 'institutionCode', termUri: 'http://rs.tdwg.org/dwc/terms/institutionCode' }` entry in `OCCURRENCE_FIELDS`
- The Maplify `recordedBy` view-time extraction expression (NULLIF + regexp_match over headline)
- The per-collection `datasetName` expression `('SalishSea.io — ' || [COALESCE] c_coll.name)`
- The rebuilt 26-column `dwc._native_occurrences`, `dwc._maplify_occurrences`, `dwc.occurrences` view bodies
- The v1.3 `dwc.datasets` title VALUES literal
- `supabase/snippets/12_dwc_assertions.sql` — SC#1-SC#6 assertion snippet
- New test assertions in `fields.test.ts` (index-19 institutionCode test; dcterms {20,23}) and `meta-xml.test.ts` (count=32; core index-19 institutionCode)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-dwc-view-rebuild/12-02-SUMMARY.md` when done
</output>
