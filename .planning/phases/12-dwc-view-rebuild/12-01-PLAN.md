---
phase: 12-dwc-view-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/snippets/12_comments_census.sql
  - .planning/phases/12-dwc-view-rebuild/maplify_trusted_comments_census.tsv
autonomous: false
requirements: [ATTR-01]
must_haves:
  truths:
    - "A read-only census of trusted maplify.sightings.comments parenthetical patterns exists as a committed artifact"
    - "The recordedBy regex is grounded in real prod data, not the 169-row sample"
    - "Multi-name, ID-credit, and directional parentheticals are confirmed to NULL out"
  artifacts:
    - path: "supabase/snippets/12_comments_census.sql"
      provides: "Read-only census query for parenthetical patterns in trusted Maplify comments"
      contains: "regexp_match"
    - path: ".planning/phases/12-dwc-view-rebuild/maplify_trusted_comments_census.tsv"
      provides: "Committed census output from prod (extracted name + count per distinct pattern)"
  key_links:
    - from: "supabase/snippets/12_comments_census.sql"
      to: "maplify.sightings.comments (WHERE trusted = TRUE)"
      via: "regexp_match over split_part(comments, '<br>', 1)"
      pattern: "regexp_match\\(split_part\\(comments"
---

<objective>
Census the parenthetical contents of prod `maplify.sightings.comments` (read-only)
BEFORE the recordedBy extraction regex is finalized in the Wave 2 view rebuild.
This is the mandatory D-03 research task — the same discipline as Phase 11's
bracket-tag census. The 169-row sample in `occurrence-bodies.tsv` is not enough;
the full ~6,800-row prod corpus must confirm the regex extracts real observer
names and NULLs out multi-name lists, `(ID …)` identification credits, and
directional/count parentheticals (per D-03, RESEARCH §Pattern 4).

Purpose: De-risk the highest-uncertainty piece of Phase 12 (RESEARCH confidence
LOW on the regex). Writing the regex against assumptions instead of prod data
risks writing garbage into the public GBIF `recordedBy` field (ATTR-01).
Output: A committed census snippet + its prod output TSV that grounds the
Wave 2 regex (which contributes to ATTR-01: `recordedBy` from the contributor).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dwc-view-rebuild/12-CONTEXT.md
@.planning/phases/12-dwc-view-rebuild/12-RESEARCH.md
@.planning/phases/12-dwc-view-rebuild/12-PATTERNS.md
@.planning/phases/12-dwc-view-rebuild/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the read-only comments census snippet</name>
  <files>supabase/snippets/12_comments_census.sql</files>
  <read_first>
    - supabase/snippets/12_comments_census.sql (file being created — read if present)
    - supabase/snippets/11_resolution_assertions.sql (header/PROD-ONLY comment-out pattern analog)
    - .planning/phases/12-dwc-view-rebuild/12-RESEARCH.md (§Pattern 4 recordedBy regex; §Open Questions Q1 — the census query shape)
    - .planning/phases/12-dwc-view-rebuild/12-PATTERNS.md (the `supabase/snippets/12_dwc_attribution_assertions.sql` "D-03 census snippet" block — the exact census SELECT)
    - .planning/phases/12-dwc-view-rebuild/12-CONTEXT.md (D-02, D-03 — string extraction, not FK; NULL-out non-names)
    - occurrence-bodies.tsv (the 169-row sample — the shapes the regex must handle)
  </read_first>
  <action>
    Create `supabase/snippets/12_comments_census.sql` as a READ-ONLY census (no
    DDL, no UPDATE — comments are immutable per Phase 11 D-12). Follow the
    `11_resolution_assertions.sql` header convention (a `\echo` banner + a run
    comment block documenting that this runs against PROD via the IPv4 session
    pooler, never against `supabase db reset` which has no Maplify data).

    Emit at minimum two census queries:
    (a) The candidate-extraction census: `SELECT (regexp_match(split_part(comments, '<br>', 1), '^\[[^\]]+\]\s+.+?\(([^()]+)\)'))[1] AS extracted, COUNT(*) AS n FROM maplify.sightings WHERE trusted = TRUE GROUP BY 1 ORDER BY n DESC` — the exact regex the Wave 2 migration will embed, so the census output IS the regex validation.
    (b) A NULL-out audit: among non-NULL `extracted` values, flag rows where `extracted ~ ','` (multi-name lists) or `extracted ~ '^IDs?\s'` (identification credits) — these MUST become NULL in the view per Pitfall 3.

    Use the FIRST-parenthetical-after-bracket-tag regex `^\[[^\]]+\]\s+.+?\(([^()]+)\)`
    against the headline segment only (`split_part(comments, '<br>', 1)`), per
    RESEARCH §Pattern 4 — never the full comments string (the attribution line
    below `<br>` can contain spurious parens). Do NOT use `usernm` (UI client, not
    observer — RESEARCH Anti-Patterns).

    Do NOT embed prod credentials in the file. The snippet is run by the operator
    against prod; the file carries only SQL.
  </action>
  <verify>
    <automated>test -f supabase/snippets/12_comments_census.sql &amp;&amp; grep -q "regexp_match(split_part" supabase/snippets/12_comments_census.sql &amp;&amp; grep -q "trusted = TRUE" supabase/snippets/12_comments_census.sql &amp;&amp; ! grep -iqE "UPDATE |DROP |CREATE |DELETE " supabase/snippets/12_comments_census.sql &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/snippets/12_comments_census.sql` exists and contains `regexp_match(split_part(comments, '<br>', 1)` and `'^\[[^\]]+\]\s+.+?\(([^()]+)\)'`
    - The file contains `WHERE trusted = TRUE` (census scoped to exported rows)
    - The file contains NO `UPDATE`/`DROP`/`CREATE`/`DELETE` statements (read-only — comments immutable)
    - The file does NOT reference `usernm` for name extraction
    - A PROD-ONLY run comment documents that `supabase db reset` is a no-op here (RESEARCH D-07 precedent)
  </acceptance_criteria>
  <done>The census snippet exists, is read-only, embeds the exact Wave-2 regex, and is scoped to trusted rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the census against prod and confirm the regex before Wave 2</name>
  <files>.planning/phases/12-dwc-view-rebuild/maplify_trusted_comments_census.tsv</files>
  <what-built>
    A read-only SQL census snippet that extracts the first-parenthetical observer
    name from the headline of every trusted Maplify comment, and audits which
    extracted values are multi-name / ID-credit / directional (must NULL out).
  </what-built>
  <action>
    Operator runs `supabase/snippets/12_comments_census.sql` against PROD via the
    IPv4 session pooler, captures the output to
    `.planning/phases/12-dwc-view-rebuild/maplify_trusted_comments_census.tsv`,
    inspects the kept-extraction set, and confirms (or extends) the regex guards
    before Wave 2 embeds the regex. This is operator-run because the meaningful
    corpus lives in prod (local `db reset` has no Maplify data — RESEARCH D-07).
  </action>
  <how-to-verify>
    1. Run the census against PROD via the IPv4 session pooler (per project memory:
       `aws-1-us-west-1.pooler.supabase.com:5432`, user `postgres.<ref>`, password
       from DB_PASSWORD). Example:
       `psql "<prod-pooler-dsn>" -f supabase/snippets/12_comments_census.sql > .planning/phases/12-dwc-view-rebuild/maplify_trusted_comments_census.tsv`
    2. Inspect the output TSV:
       - The dominant rows should be single human names (e.g. `Michelle Goll`, `Fu Lu Amy`).
       - Rows with a comma (`Howard Garrett, Alisa Schulman-Janiger`) must be flagged by audit query (b) as NULL-targets.
       - Rows starting `ID `/`IDs ` (`ID Rachel Haight`) must be flagged as NULL-targets.
       - Directional/count parentheticals (`heading northwest, …`) carry a comma and are caught by the comma guard.
    3. Confirm NO extracted value that the regex would KEEP (no comma, not `ID `-prefixed) is obviously non-name. If a new non-name shape appears at material volume, note it so the Wave 2 regex guard is extended (e.g. add another `~` exclusion).
    4. Commit the output as `.planning/phases/12-dwc-view-rebuild/maplify_trusted_comments_census.tsv`.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/12-dwc-view-rebuild/maplify_trusted_comments_census.tsv &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/12-dwc-view-rebuild/maplify_trusted_comments_census.tsv` exists and is committed
    - The kept-extraction set (no comma, not ID-prefixed) is confirmed to be observer names, not garbage
    - Any new non-name shape at material volume is documented for the Wave 2 regex guard
  </acceptance_criteria>
  <done>The census has been run against prod, the output committed, and the kept-name set confirmed clean (or the additional regex guard documented).</done>
  <resume-signal>Type "approved" if the kept names are clean (or describe the additional regex guard needed before Wave 2 proceeds).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prod DB → census output TSV | Read-only `SELECT` extracts personal names from `comments` into a committed artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-PII | Information Disclosure | `recordedBy` regex over `comments` | mitigate | Census confirms only observer names (after bracket tag) are extracted; multi-name lists and ID credits NULL out. Name attribution is intended per D-02; the census is the guard that no incidental non-name PII (counts, directions, locations in parens) is kept. |
| T-12-01-RO | Tampering | `12_comments_census.sql` | mitigate | File is read-only by construction (no DDL/DML); automated verify greps for absence of `UPDATE`/`DROP`/`CREATE`/`DELETE`. `comments` stays immutable (Phase 11 D-12). |
| T-12-01-CRED | Information Disclosure | prod DSN | accept | Operator supplies the pooler DSN at run time; the committed `.sql` file carries no credentials. |
</threat_model>

<verification>
- `supabase/snippets/12_comments_census.sql` is read-only and embeds the exact Wave-2 regex.
- The census has been run against prod and the kept-name set confirmed clean (human checkpoint).
- `maplify_trusted_comments_census.tsv` committed.
</verification>

<success_criteria>
- The recordedBy extraction regex is validated against the full prod trusted-row corpus, not the 169-row sample, before any migration embeds it.
- No multi-name / ID-credit / directional parenthetical survives into the kept set.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (exclude from drift verification):
- `supabase/snippets/12_comments_census.sql` — read-only D-03 census snippet
- `.planning/phases/12-dwc-view-rebuild/maplify_trusted_comments_census.tsv` — committed census output
- The recordedBy extraction regex pattern `^\[[^\]]+\]\s+.+?\(([^()]+)\)` (validated here, embedded in the Wave-2 view)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-dwc-view-rebuild/12-01-SUMMARY.md` when done
</output>
