---
phase: 07-nightly-workflow-hosting
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01-PLAN
  - 07-02-PLAN
files_modified:
  - .github/workflows/dwca-nightly.yml
autonomous: false
requirements:
  - EXPORT-01
  - EXPORT-02
  - EXPORT-03
  - EXPORT-04
  - EXPORT-05
tags: [phase-07, dwca, nightly, github-actions, oidc, s3, cloudfront, secret-gate, sha-pin]
user_setup:
  - service: github-actions
    why: "The workflow reads SUPABASE_DB_URL from the GH `production` environment to drive Phase 6's DuckDB ATTACH (direct port-5432 connection, not the 6543 pooler)."
    env_vars:
      - name: SUPABASE_DB_URL
        source: "Supabase Dashboard → Project Settings → Database → Connection string → URI (Direct connection, port 5432) — substitute the service-role password from Supabase Dashboard → Project Settings → API → service_role secret"
        format: "postgres://postgres.<project_ref>:<service_role_password>@db.<project_ref>.supabase.co:5432/postgres"
        environment: production
    dashboard_config:
      - task: "Set repository production environment secret `SUPABASE_DB_URL`"
        location: "GitHub → Settings → Environments → production → Add secret"
        notes: "MUST be port 5432 (direct), NOT 6543 (transaction pooler). DuckDB's postgres extension requires direct connection."

must_haves:
  truths:
    - "A scheduled `.github/workflows/dwca-nightly.yml` runs at `cron: '0 9 * * *'` UTC (= 02:00 PT / 01:00 PST) AND supports `workflow_dispatch` for manual re-runs (per S-01, S-02)."
    - "After a successful run, `https://salishsea.io/dwca/salishsea-occurrences-v1.zip` returns HTTP 200 with `Content-Type: application/zip` (or `application/octet-stream`), AND `https://salishsea.io/dwca/salishsea-occurrences-v1.parquet` returns 200 with a non-text content type (per EXPORT-02, V-01)."
    - "The workflow uploads to S3 in the order `parquet`, `zip`, `parquet.sha256`, `zip.sha256` — checksums LAST (per P-02)."
    - "The workflow invalidates `/dwca/*` on CloudFront and waits for the invalidation to complete before the V-01 smoke check (per I-01, Pattern 4)."
    - "The workflow refuses to upload if `scripts/dwca/guard.ts` exits non-zero — failure is structured into `dist/dwca/guard-diff.txt` and surfaced via the failure issue (per G-01..G-04, O-01)."
    - "After a publish, `https://salishsea.io/dwca/<name>.sha256` returns a GNU coreutils-formatted sidecar that verifies the corresponding `<name>` via `sha256sum -c` (per V-01, EXPORT-04, EXPORT-05)."
    - "Workflow uses OIDC role assumption (`role-to-assume: arn:aws:iam::648183724555:role/salishsea-deploy-action`, `permissions.id-token: write`) — no long-lived AWS keys (per C-03)."
    - "`SUPABASE_DB_URL` is sourced from `secrets.SUPABASE_DB_URL` under `environment: production` — NEVER echoed to logs (per C-01, T-7-01)."
    - "Every third-party Action is SHA-pinned (not tag-pinned) — `actions/checkout@de0fac2e...`, `actions/setup-node@48b55a01...`, `aws-actions/configure-aws-credentials@acca2b1b...`, `peter-evans/create-issue-from-file@fca9117c27cdc29c6c4db3b86c48e4115a786710` — matching `deploy.yml`'s convention (per T-7-03)."
    - "`concurrency: dwca-nightly` with `cancel-in-progress: false` queues a manual dispatch behind an in-flight cron run rather than cancelling either (per W-03)."
    - "On failure, the workflow opens (or comments on) an open GitHub issue labelled `dwca-nightly-failed` (deduped) via `peter-evans/create-issue-from-file` + `actions/github-script` (per O-01 + Specifics dedupe note)."
    - "Workflow lives at `.github/workflows/dwca-nightly.yml` as a NEW file SEPARATE from `.github/workflows/deploy.yml` — no coupling to push-to-`main` (per W-01)."
    - "Publication is a single-file overwrite at stable S3 keys — no staging prefix, no per-run dated dir, no manifest pointer (per P-01)."
    - "Successful runs are silent beyond the smoke-check log lines and the green GHA badge — no email, no issue, no Slack ping (per O-02)."
  artifacts:
    - path: ".github/workflows/dwca-nightly.yml"
      provides: "Scheduled + manually-dispatchable GitHub Actions workflow that wraps `npm run build:dwca`, runs the G-01..G-04 guard, publishes both artifacts + sha256 sidecars to S3 in the documented order, invalidates CloudFront `/dwca/*`, smoke-verifies via V-01, and opens/comments a dedup'd failure issue on non-zero exit"
      contains: "uses: peter-evans/create-issue-from-file@fca9117c27cdc29c6c4db3b86c48e4115a786710"
      min_lines: 100
  key_links:
    - from: ".github/workflows/dwca-nightly.yml"
      to: "scripts/dwca/guard.ts (Plan 07-01)"
      via: "`run: npx tsx scripts/dwca/guard.ts` step BETWEEN `npm run build:dwca` and the `aws s3 cp` steps"
      pattern: "npx tsx scripts/dwca/guard\\.ts"
    - from: ".github/workflows/dwca-nightly.yml"
      to: "scripts/dwca/verify-publish.ts (Plan 07-01)"
      via: "`run: npx tsx scripts/dwca/verify-publish.ts` step AFTER `aws cloudfront wait invalidation-completed`"
      pattern: "npx tsx scripts/dwca/verify-publish\\.ts"
    - from: ".github/workflows/dwca-nightly.yml"
      to: "production CloudFront distribution"
      via: "`aws cloudfront create-invalidation --distribution-id $DISTRIBUTION_ID --paths /dwca/*` then `aws cloudfront wait invalidation-completed`"
      pattern: "create-invalidation.*?/dwca/\\*"
    - from: ".github/workflows/dwca-nightly.yml"
      to: "S3 bucket `salishsea-io` under key prefix `site/dwca/`"
      via: "`aws s3 cp <file> s3://${S3_BUCKET}/site/dwca/<file>` × 4 in documented order"
      pattern: "s3://\\$\\{? ?\\{? ?vars\\.S3_BUCKET ?\\}? ?\\}?/site/dwca"
---

<objective>
Land `.github/workflows/dwca-nightly.yml` — the scheduled GitHub Actions workflow that nightly wraps `npm run build:dwca` (Phase 6, frozen), runs the G-01..G-04 hard-floor guard (Plan 07-01), publishes both artifacts + GNU coreutils sha256 sidecars to S3 in the documented checksum-LAST order (P-02), invalidates `/dwca/*` on CloudFront (I-01), waits for invalidation completion (Pattern 4), runs the V-01 smoke check (Plan 07-01), and on any failure opens or comments on a dedup'd GitHub issue labelled `dwca-nightly-failed` (O-01).

This is the only prod-touching, secret-requiring surface in Phase 7. Three blocking human-verify checkpoints gate it:
1. **Task 2 (peter-evans SHA verify):** confirm `peter-evans/create-issue-from-file@fca9117c27cdc29c6c4db3b86c48e4115a786710` resolves on github.com to the v6.0.0 tag — supply-chain mitigation per ASVS V14 and the project's SHA-pin convention.
2. **Task 3 (SUPABASE_DB_URL gate):** confirm the new GH `production` environment secret `SUPABASE_DB_URL` is set with the direct port-5432 DSN — per C-02 and the project memory rule "If a deploy requires a new env var, tell the user and wait for confirmation before pushing".
3. **Task 5 (first nightly run review):** trigger one `workflow_dispatch`, review the run, confirm both artifacts are reachable + verified, and confirm no `dwca-nightly-failed` issue was opened.

Purpose: ships EXPORT-01..05. Closes the Phase 7 goal — "A scheduled GitHub Actions workflow regenerates and publishes the archive nightly to the existing S3/CloudFront site, reusing the existing AWS OIDC role and bucket, with an atomic write-then-swap, an empty-result guard, a CloudFront invalidation, and a published checksum."

Output:
- `.github/workflows/dwca-nightly.yml` — single-job workflow, ~100-130 lines.
- One commit to `main` after Tasks 2 and 3 pass.
- One `workflow_dispatch` run completed successfully (Task 5).
- All 5 phase requirements satisfied per the per-task verification map in `07-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-nightly-workflow-hosting/07-CONTEXT.md
@.planning/phases/07-nightly-workflow-hosting/07-RESEARCH.md
@.planning/phases/07-nightly-workflow-hosting/07-VALIDATION.md
@.github/workflows/deploy.yml
@.github/workflows/smoke.yml
@.planning/phases/07-nightly-workflow-hosting/07-01-PLAN.md
@.planning/phases/07-nightly-workflow-hosting/07-02-PLAN.md
</context>

<artifacts_this_phase_produces>
- `.github/workflows/dwca-nightly.yml` — new workflow file.
- New GitHub Actions cron schedule: `'0 9 * * *'` UTC.
- New GitHub Actions environment secret consumption: `SUPABASE_DB_URL` (the secret itself is set by the user in Task 3; the workflow only references it).
- New GitHub label `dwca-nightly-failed` (created automatically by `peter-evans/create-issue-from-file` on first failure).
- New S3 key prefix in production: `site/dwca/` (was unused; this workflow is the only writer).
- New CloudFront invalidation pattern: `/dwca/*` (existed unused; first use here).

(No new infrastructure, no new IAM, no new Lambda — strictly orchestration. Pre-existing artifacts consumed: the OIDC role `arn:aws:iam::648183724555:role/salishsea-deploy-action`, `vars.S3_BUCKET`, `vars.CLOUDFRONT_DISTRIBUTION_ID`, the L-01 Lambda@Edge change from Plan 07-02, the `scripts/dwca/guard.ts` and `scripts/dwca/verify-publish.ts` modules from Plan 07-01.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Draft `.github/workflows/dwca-nightly.yml`</name>
  <files>.github/workflows/dwca-nightly.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (whole file) — the template for OIDC role assumption (L62-66), env-and-secret pattern (L29-34), SHA-pinning convention (every `uses:` line), `aws s3 sync` + `create-invalidation` (L80-81), `actions/checkout@de0fac2e...`, `actions/setup-node@48b55a01...`, `aws-actions/configure-aws-credentials@acca2b1b...` pins.
    - .github/workflows/smoke.yml — confirms the project's existing cron-workflow conventions (if any).
    - .planning/phases/07-nightly-workflow-hosting/07-CONTEXT.md §"Schedule & trigger", §"Atomic publish (EXPORT-03)", §"Empty/under-threshold guard (EXPORT-03)", §"DB connection & AWS auth", §"CloudFront invalidation & smoke verification", §"Failure surfacing (observability)", §"Workflow file shape" (all locked decisions S-01..W-03).
    - .planning/phases/07-nightly-workflow-hosting/07-RESEARCH.md §"Code Examples / Workflow skeleton" (lines ~615-725) — the literal YAML reference; copy the structure verbatim with field-level placeholders that match this plan's locked decisions.
    - .planning/phases/07-nightly-workflow-hosting/07-RESEARCH.md §"Pattern 5: Failure-issue dedupe via github-script" (lines ~399-435) — the two-step dedupe pattern.
    - .planning/phases/07-nightly-workflow-hosting/07-RESEARCH.md §"Package Legitimacy Audit" — confirms the four required SHA pins.
    - .planning/phases/07-nightly-workflow-hosting/07-RESEARCH.md §"Common Pitfalls" — at minimum Pitfall 3 (CloudFront caching) and Pitfall 6 (`aws cloudfront wait` timeout — the wait step needs `continue-on-error: true` so V-01 can still attempt verify with its own retry budget).
    - .planning/phases/07-nightly-workflow-hosting/07-01-PLAN.md — the contract of `scripts/dwca/guard.ts` (CLI, env vars `ZIP_FLOOR_BYTES`/`PARQUET_FLOOR_BYTES`/`ROW_FLOOR`, exit 1 on trip, writes `dist/dwca/guard-diff.txt`) and `scripts/dwca/verify-publish.ts` (CLI, fetches both `.zip` + `.parquet`, exits 1 on mismatch).
  </read_first>
  <action>
    Create `.github/workflows/dwca-nightly.yml` matching the RESEARCH.md §"Workflow skeleton" structure with the following concrete bindings:

    **Top-level metadata:**
    - `name: DwC-A Nightly`
    - `on.schedule: [{ cron: '0 9 * * *' }]` (per S-01 — UTC literal, do NOT write `0 2 * * *`).
    - `on.workflow_dispatch:` (no inputs — per CONTEXT §"Anti-Patterns to Avoid" + Claude's Discretion: no `skip-guard` input).
    - `concurrency: { group: dwca-nightly, cancel-in-progress: false }` (per W-03).

    **Job `publish`:**
    - `runs-on: ubuntu-latest`
    - `environment: production` (per W-02 — gates `secrets.SUPABASE_DB_URL`, `vars.S3_BUCKET`, `vars.CLOUDFRONT_DISTRIBUTION_ID`).
    - `permissions:` block exactly:
      - `id-token: write` (for OIDC)
      - `contents: read`
      - `issues: write` (for O-01 failure issue)
      (Per ASVS V14 / RESEARCH §"Security Domain" — minimum required; no `actions: write`, no `packages: *`.)
    - `env:` job-level — `ZIP_FLOOR_BYTES: '51200'`, `PARQUET_FLOOR_BYTES: '10240'`, `ROW_FLOOR: '1000'` (per CONTEXT G-02 + Plan 07-01 default constants).

    **Steps (in order):**
    1. `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd` with `persist-credentials: false` (mirrors `deploy.yml:15-17`).
    2. `actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e` with `node-version-file: package.json` (mirrors `deploy.yml:20-22`).
    3. `name: Install dependencies` `run: npm ci`.
    4. `aws-actions/configure-aws-credentials@acca2b1b2070338fb9fd1ca27ecee81d687e58e5` with `role-to-assume: arn:aws:iam::648183724555:role/salishsea-deploy-action` and `aws-region: us-west-2` (mirrors `deploy.yml:62-66`, per C-03).
    5. `name: Build DwC-A` with `env: SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}` and `run: npm run build:dwca`. NEVER echo `$SUPABASE_DB_URL`; do NOT use `set -x`; pass via `env:` so it reaches `build.ts` but is not interpolated into the shell command.
    6. `name: Compute checksums` `working-directory: dist/dwca` `run: |` block with TWO `sha256sum <name> > <name>.sha256` invocations (order doesn't matter here — these are local files; the S3 upload order is what matters per P-02).
    7. `name: Guard (G-01..G-04)` with `env: SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}` and `run: npx tsx scripts/dwca/guard.ts` (per G-03 — between build and S3; per Plan 07-01 contract).
    8. `name: Publish to S3 (checksum-LAST order, P-02)` with `env: DEST: s3://${{ vars.S3_BUCKET }}/site/dwca`, `working-directory: dist/dwca`, and a `run: |` block containing exactly four `aws s3 cp` invocations in this exact order:
       a. `aws s3 cp salishsea-occurrences-v1.parquet        "${DEST}/salishsea-occurrences-v1.parquet"`
       b. `aws s3 cp salishsea-occurrences-v1.zip            "${DEST}/salishsea-occurrences-v1.zip"`
       c. `aws s3 cp salishsea-occurrences-v1.parquet.sha256 "${DEST}/salishsea-occurrences-v1.parquet.sha256"`
       d. `aws s3 cp salishsea-occurrences-v1.zip.sha256     "${DEST}/salishsea-occurrences-v1.zip.sha256"`
       (Per P-02 + RESEARCH §"Anti-Patterns to Avoid" — `aws s3 sync` is forbidden here; it would unspecified-order the uploads.)
    9. `name: Invalidate CloudFront /dwca/*` with `id: invalidate`, `env: DISTRIBUTION_ID: ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }}`, `run:` block that captures the invalidation Id via `--query 'Invalidation.Id' --output text` and writes it to `$GITHUB_OUTPUT` as `id=<ID>` (per I-01 + Pattern 4).
    10. `name: Wait for invalidation` with `continue-on-error: true` (per RESEARCH Pitfall 6 — waiter timeout is non-fatal because V-01 has its own implicit retry via HTTP), `env: DISTRIBUTION_ID: ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }}`, `run: aws cloudfront wait invalidation-completed --distribution-id "${DISTRIBUTION_ID}" --id "${{ steps.invalidate.outputs.id }}"`.
    11. `name: Smoke verify (V-01)` `run: npx tsx scripts/dwca/verify-publish.ts` (per Plan 07-01 contract; no `DWCA_BASE_URL` env override needed — defaults to `https://salishsea.io/dwca`).
    12. `name: Find existing failure issue` with `if: failure()`, `id: find-issue`, `uses: actions/github-script@<SHA>` — pick the current v8 SHA at plan time and pin it. Inline `script:` per RESEARCH Pattern 5 (lines ~412-423): `github.rest.issues.listForRepo({state: 'open', labels: 'dwca-nightly-failed', per_page: 1})` then `core.setOutput('issue-number', issues.data[0]?.number ?? '')`.
    13. `name: Open or update failure issue` with `if: failure()`, `uses: peter-evans/create-issue-from-file@fca9117c27cdc29c6c4db3b86c48e4115a786710` (v6.0.0 SHA — verified at Task 2), with:
        - `title: "DwC-A nightly publish failed (run ${{ github.run_id }})"`
        - `content-filepath: ./dist/dwca/guard-diff.txt` (the guard writes this; if a non-guard failure path runs and no guard-diff.txt exists, the action will still open an issue but with empty body — accepted, the run URL in the title is the link)
        - `labels: dwca-nightly-failed`
        - `issue-number: ${{ steps.find-issue.outputs.issue-number }}` (empty string → creates new issue per peter-evans v6 docs; non-empty → comments on existing)

    **General style:**
    - 4-space indent (matches `deploy.yml`).
    - Each `uses:` line has an inline `# v<N>` comment after the SHA (mirrors `deploy.yml` style).
    - NEVER write `echo "$SUPABASE_DB_URL"` or `set -x`. NEVER pass `SUPABASE_DB_URL` via `run: ... ${SUPABASE_DB_URL}` interpolation — always via `env:` block on the step that needs it (steps 5 and 7 only).
    - NEVER write `aws s3 sync` — only `aws s3 cp` per the four-step ordered list.
    - NEVER set `cancel-in-progress: true` on the concurrency group.

    Implements EXPORT-01 (cron + dispatch), EXPORT-02 (S3 + CloudFront publish), EXPORT-03 (atomic write-then-swap via P-02 + guard + invalidation), EXPORT-04 (sha256 sidecars uploaded + verified), EXPORT-05 (parquet treated symmetrically). T-7-01 mitigation (DSN never echoed); T-7-02 mitigation (OIDC reused, no new IAM); T-7-03 mitigation (all SHA pins).
  </action>
  <verify>
    <automated>node -e "const yaml=require('yaml'); const fs=require('fs'); const doc=yaml.parse(fs.readFileSync('.github/workflows/dwca-nightly.yml','utf8')); if(doc.on.schedule[0].cron!=='0 9 * * *')throw new Error('cron mismatch'); if(doc.concurrency.group!=='dwca-nightly')throw new Error('concurrency'); if(doc.concurrency['cancel-in-progress']!==false)throw new Error('cancel-in-progress'); if(doc.jobs.publish.environment!=='production')throw new Error('env'); if(doc.jobs.publish.permissions['id-token']!=='write')throw new Error('id-token'); if(doc.jobs.publish.permissions.issues!=='write')throw new Error('issues'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.github/workflows/dwca-nightly.yml`.
    - File is valid YAML — `node -e "require('yaml').parse(require('fs').readFileSync('.github/workflows/dwca-nightly.yml','utf8'))"` exits 0. (Use the `yaml` package if present; otherwise the executor may `npm install --no-save yaml` for this validation step.)
    - Cron schedule is exactly `'0 9 * * *'` — assertion: `grep -F "cron: '0 9 * * *'" .github/workflows/dwca-nightly.yml` exits 0.
    - `workflow_dispatch` trigger present — assertion: `grep -c "workflow_dispatch:" .github/workflows/dwca-nightly.yml` ≥ 1.
    - `concurrency.cancel-in-progress: false` — assertion: `grep -c "cancel-in-progress: false" .github/workflows/dwca-nightly.yml` ≥ 1 (per W-03).
    - `environment: production` declared on the job — assertion: `grep -c "environment: production" .github/workflows/dwca-nightly.yml` ≥ 1.
    - OIDC role ARN present — assertion: `grep -c "arn:aws:iam::648183724555:role/salishsea-deploy-action" .github/workflows/dwca-nightly.yml` ≥ 1 (per C-03).
    - All required SHA pins present:
      - `grep -c "actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd" .github/workflows/dwca-nightly.yml` ≥ 1
      - `grep -c "actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e" .github/workflows/dwca-nightly.yml` ≥ 1
      - `grep -c "aws-actions/configure-aws-credentials@acca2b1b2070338fb9fd1ca27ecee81d687e58e5" .github/workflows/dwca-nightly.yml` ≥ 1
      - `grep -c "peter-evans/create-issue-from-file@fca9117c27cdc29c6c4db3b86c48e4115a786710" .github/workflows/dwca-nightly.yml` ≥ 1
    - NO tag pins (every external `uses:` line ends with a 40-char hex SHA) — assertion: `grep -E "uses: [^ ]+@v?[0-9]+($|\\s)" .github/workflows/dwca-nightly.yml` returns no matches (i.e., exit 1 from grep).
    - Upload order is parquet → zip → parquet.sha256 → zip.sha256 — assertion: `grep -n "aws s3 cp salishsea-occurrences-v1" .github/workflows/dwca-nightly.yml` outputs lines whose numeric line numbers are strictly increasing AND the order of filename suffixes is `.parquet`, `.zip`, `.parquet.sha256`, `.zip.sha256` (per P-02). The executor verifies with: `awk '/aws s3 cp salishsea-occurrences-v1/ {sub(/.*salishsea-occurrences-v1/,""); sub(/ .*/,""); print}' .github/workflows/dwca-nightly.yml | head -4 | tr '\n' ',' | grep -F ".parquet,.zip,.parquet.sha256,.zip.sha256,"` exits 0.
    - `aws cloudfront create-invalidation --distribution-id` present with `--paths '/dwca/*'` — assertion: `grep -F "create-invalidation" .github/workflows/dwca-nightly.yml | grep -F "/dwca/*"` exits 0.
    - `aws cloudfront wait invalidation-completed` present — assertion: `grep -c "wait invalidation-completed" .github/workflows/dwca-nightly.yml` ≥ 1.
    - Both Plan 07-01 modules invoked — assertions: `grep -c "npx tsx scripts/dwca/guard.ts" .github/workflows/dwca-nightly.yml` ≥ 1 AND `grep -c "npx tsx scripts/dwca/verify-publish.ts" .github/workflows/dwca-nightly.yml` ≥ 1.
    - Workflow does NOT use `aws s3 sync` — assertion: `grep -c "aws s3 sync" .github/workflows/dwca-nightly.yml` returns `0`.
    - Workflow does NOT echo the DSN — assertion: `grep -E '(echo|printf|set -x).*SUPABASE_DB_URL' .github/workflows/dwca-nightly.yml` returns empty.
    - `permissions:` block contains `id-token: write`, `contents: read`, `issues: write` (and nothing else) — assertions: each `grep -c` returns 1.
    - Failure-issue dedupe step present — assertion: `grep -c "labels: 'dwca-nightly-failed'" .github/workflows/dwca-nightly.yml` ≥ 1 AND `grep -c "actions/github-script@" .github/workflows/dwca-nightly.yml` ≥ 1.
    - Failure issue `content-filepath: ./dist/dwca/guard-diff.txt` — assertion: `grep -c "guard-diff.txt" .github/workflows/dwca-nightly.yml` ≥ 1.
  </acceptance_criteria>
  <done>
    `.github/workflows/dwca-nightly.yml` exists, parses as valid YAML, mirrors `deploy.yml`'s conventions exactly, encodes every CONTEXT locked decision (S-01..W-03), invokes both Plan 07-01 helpers, and is ready for the Task 2 + Task 3 + Task 4 gates before pushing to `main`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify `peter-evans/create-issue-from-file@fca9117c27cdc29c6c4db3b86c48e4115a786710` resolves to v6.0.0</name>
  <what-built>
    The workflow pins the `peter-evans/create-issue-from-file` action at SHA `fca9117c27cdc29c6c4db3b86c48e4115a786710` per RESEARCH.md §"Package Legitimacy Audit" (the SHA was found in the v6.0.0 GitHub release page). slopcheck was unavailable at research time so the package is `[ASSUMED]` legitimate, not `[VERIFIED]` — this is the mandatory human-verify gate before merging the workflow.
  </what-built>
  <action>Pause for human verification. Open the github.com URLs in <how-to-verify> step 1 (and optionally step 2), confirm the SHA `fca9117c27cdc29c6c4db3b86c48e4115a786710` resolves to the v6.0.0 tag of `peter-evans/create-issue-from-file`, and only return "approved" if the SHA matches and the maintainer is the expected one. If the SHA does not resolve or resolves to a different version, STOP — describe what was found verbatim and do not advance to Task 4. This gate is non-bypassable; `workflow.auto_advance` does not apply to package legitimacy checkpoints per the planner gate.</action>
  <how-to-verify>
    1. Open https://github.com/peter-evans/create-issue-from-file/releases/tag/v6.0.0 (or https://github.com/peter-evans/create-issue-from-file/tree/fca9117c27cdc29c6c4db3b86c48e4115a786710) and confirm:
       - The release page exists and shows the version tag `v6.0.0`.
       - The commit SHA matches `fca9117c27cdc29c6c4db3b86c48e4115a786710` (full 40 characters).
       - The author is `peter-evans` (Peter Evans), who also maintains `create-pull-request` and `create-or-update-comment` — both ubiquitous in the GitHub ecosystem.
       - The release page or README documents the same inputs we use: `title`, `content-filepath`, `labels`, `issue-number`.
    2. Optionally cross-check on https://www.npmjs.com/~peter-evans — no npm package needed for this action, but the same author should be visible.
    3. If the SHA does not resolve, or resolves to a different version, STOP — do not merge. Investigate (perhaps the v6.0.0 release was rebased; pick the new v6.0.x SHA and update the workflow before proceeding).

    This gate addresses T-7-03 (Supply-chain compromise of third-party action) — supply-chain compromises of GH Actions are real (tj-actions/changed-files, March 2025); a SHA pin only helps if a human verifies the SHA at install time.
  </how-to-verify>
  <resume-signal>Type "approved" (or "SHA verified — v6.0.0") to proceed. If the SHA does not resolve or is unexpected, describe what you found.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Confirm `SUPABASE_DB_URL` is set in GH Actions `production` environment</name>
  <what-built>
    The workflow `dwca-nightly.yml` reads `secrets.SUPABASE_DB_URL` under `environment: production` to drive Phase 6's DuckDB ATTACH. This secret is NEW — no existing workflow uses it (`deploy.yml` uses `secrets.DB_PASSWORD` for `supabase db push`, which is a different credential path). Per the project memory rule ("If a deploy requires a new env var, tell the user and wait for confirmation before pushing"), this gate is MANDATORY before pushing the workflow.
  </what-built>
  <action>Pause for human verification. Open GitHub → Settings → Environments → `production` → Environment secrets. Confirm a secret named exactly `SUPABASE_DB_URL` exists with port-5432 (direct) format per <how-to-verify>. If absent or set to the wrong port (6543 pooler), set it now via the GH UI. Only return "approved" / "secret confirmed" after the secret is in place. Claude cannot read or set the secret — this is a human-only step per the project memory rule "If a deploy requires a new env var, tell the user and wait for confirmation before pushing".</action>
  <how-to-verify>
    1. In your browser, open the GitHub repo → Settings → Environments → `production`.
    2. Under "Environment secrets" confirm a secret named exactly `SUPABASE_DB_URL` exists (the secret value is masked — you cannot read it back from the UI, you can only confirm the name).
    3. Confirm the value (which you originally set or are about to set) matches this format:
       ```
       postgres://postgres.<project_ref>:<service_role_password>@db.<project_ref>.supabase.co:5432/postgres
       ```
       The `<project_ref>` for this project is `grztmjpzamcxlzecmqca` (from MEMORY.md).
    4. CRITICAL: the port MUST be `5432` (direct), NOT `6543` (the transaction pooler). DuckDB's postgres extension does not support the pooled connection per RESEARCH Pitfall 4. The service-role password comes from Supabase Dashboard → Settings → API → `service_role` secret.
    5. If the secret is missing or uses port 6543, set/update it now via "Add secret" or "Update" in the GH UI. The secret value is held only by GitHub — Claude cannot see it.
    6. Confirm you have set the secret correctly by replying "secret set" or "secret confirmed".

    DO NOT proceed to push (Task 4) until this gate passes — pushing the workflow with a missing or wrong `SUPABASE_DB_URL` would make the first cron run fail at the `Build DwC-A` step and open a `dwca-nightly-failed` issue with a Postgres connection error in the body, which is undesirable as a "first impression" of the new workflow.
  </how-to-verify>
  <resume-signal>Type "approved" (or "secret confirmed") to proceed. If the secret cannot be set right now, describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Commit and push `.github/workflows/dwca-nightly.yml` to `main`</name>
  <files>.github/workflows/dwca-nightly.yml</files>
  <read_first>
    - .planning/phases/07-nightly-workflow-hosting/07-03-PLAN.md (this plan) — Tasks 2 and 3 must be approved before this step runs.
    - The committed workflow file from Task 1.
  </read_first>
  <action>
    With Task 2 (peter-evans SHA verified) and Task 3 (SUPABASE_DB_URL set) both approved, commit the workflow file and push to `main`. The commit message should follow the repo's convention.

    Commit message (HEREDOC):
    ```
    feat(07): add dwca-nightly.yml — scheduled DwC-A publish to S3/CloudFront

    Wraps `npm run build:dwca` (Phase 6) in a nightly cron workflow that:
    - runs at `0 9 * * *` UTC + supports workflow_dispatch (S-01, S-02)
    - applies the G-01..G-04 hard-floor guard between build and publish (G-03)
    - uploads to S3 in checksum-LAST order: parquet, zip, parquet.sha256, zip.sha256 (P-02)
    - invalidates `/dwca/*` on CloudFront and waits for completion (I-01, Pattern 4)
    - V-01 smoke check round-trips both sha256 sidecars after invalidation
    - opens/comments-on a dedup'd `dwca-nightly-failed` issue on failure (O-01)

    Reuses the existing OIDC role `salishsea-deploy-action` (C-03) and bucket
    `salishsea-io/site/dwca/` (C-04). No new AWS infra. Secret `SUPABASE_DB_URL`
    (direct port 5432) is set in GH production environment per C-01/C-02.

    Covers EXPORT-01..05.
    ```

    Pushing to `main` will NOT trigger an immediate run — the workflow's only triggers are cron and `workflow_dispatch`. The first run happens at the next cron tick (09:00 UTC) OR when manually dispatched in Task 5.

    Plan 07-02 must have already completed (L-01 Lambda@Edge change live on production CloudFront) — Task 5's `workflow_dispatch` smoke check depends on it.

    Per project memory: pushes to `main` auto-deploy via `deploy.yml`. Adding `dwca-nightly.yml` does NOT trigger `deploy.yml` (deploy.yml triggers on `push` only — adding a sibling workflow file is not a no-op there, but the deploy.yml will run because the push touches `.github/workflows/`. This is fine; the deploy is idempotent and will not affect `dwca-nightly.yml`).
  </action>
  <verify>
    <automated>git log --oneline -1 -- .github/workflows/dwca-nightly.yml | grep -E "feat\(07\)" &amp;&amp; git ls-remote origin main | awk '{print $1}' &gt; /tmp/remote-main &amp;&amp; git rev-parse HEAD &gt; /tmp/local-head &amp;&amp; diff /tmp/local-head /tmp/remote-main</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/dwca-nightly.yml` is tracked in git — `git ls-files .github/workflows/dwca-nightly.yml` outputs the path.
    - Latest commit touching the file uses the `feat(07):` prefix — assertion: `git log --oneline -1 -- .github/workflows/dwca-nightly.yml | grep -c "feat(07)"` is `1`.
    - `main` is pushed — assertion: `git rev-parse HEAD` matches `git ls-remote origin main` first column.
    - The push also triggered `deploy.yml` (expected side-effect) — assertion: `gh run list --workflow=deploy.yml --limit 1 --json status,headSha --jq '.[0].headSha'` matches the HEAD SHA. (Acceptable — the deploy is idempotent and unrelated to dwca-nightly.yml.)
  </acceptance_criteria>
  <done>
    Workflow file is on `main`; the scheduled trigger is registered (per RESEARCH Pitfall 1, GHA may take 15-60 min to recognize a brand-new cron; this is acceptable since Task 5 uses `workflow_dispatch` for the first run, not the cron).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Trigger first `workflow_dispatch` run and verify success</name>
  <what-built>
    The `.github/workflows/dwca-nightly.yml` workflow is now on `main`. Tasks 2 (SHA), 3 (secret), and Plan 07-02 (L-01 Lambda@Edge) are all green. Time to prove end-to-end:
    - Build produces `dist/dwca/salishsea-occurrences-v1.{zip,parquet}` against the production DB.
    - Guard passes (the production DB has hundreds of thousands of `dwc.occurrences` rows per Phase 5's view definitions; well above the 1000-row floor).
    - All four files upload to S3 in the documented order.
    - CloudFront invalidation completes.
    - V-01 smoke check passes (the Lambda@Edge L-01 carve-out from Plan 07-02 lets the binary through).
    - No `dwca-nightly-failed` issue opens.
  </what-built>
  <action>Pause for human verification. Run the `gh workflow run dwca-nightly.yml --ref main` dispatch from <how-to-verify> step 1, watch the run via `gh run watch`, and verify every step finishes green per <how-to-verify> steps 2-4. Confirm all four `https://salishsea.io/dwca/salishsea-occurrences-v1.*` URLs return 200 and the sha256sum -c round-trip exits 0. Only return "approved" if the run is green AND no `dwca-nightly-failed` issue was opened. If any step failed, describe the failing step + observed output for triage — do not approve past failures.</action>
  <how-to-verify>
    1. Trigger the workflow:
       ```
       gh workflow run dwca-nightly.yml --ref main
       ```
       Then watch:
       ```
       gh run watch
       ```
       Or open the Actions tab in the browser and click into the new "DwC-A Nightly" run.

    2. Once the run completes (expect ~3-5 min build + ~1-3 min S3 upload + ~1-15 min invalidation + smoke), confirm:
       - Status: `completed success`.
       - "Build DwC-A" step finished without printing any line containing `postgres://` or the service-role password (DSN scrub verification — quickly scan the step logs).
       - "Guard (G-01..G-04)" step output starts with `guard ok:` and includes the row count, zip bytes, and parquet bytes.
       - "Publish to S3 (checksum-LAST order, P-02)" step has 4 successful `upload:` lines (one each for `.parquet`, `.zip`, `.parquet.sha256`, `.zip.sha256` in THAT order).
       - "Wait for invalidation" step has either `completed success` OR `continue-on-error: true` allowed it to proceed past a timeout (acceptable per Pitfall 6).
       - "Smoke verify (V-01)" step output has two `: ok (<hex>)` lines — one for the `.zip` and one for the `.parquet`.
       - No `dwca-nightly-failed` issue was opened: `gh issue list --label dwca-nightly-failed --state open` returns no rows.

    3. Verify externally with curl:
       ```
       curl -sI https://salishsea.io/dwca/salishsea-occurrences-v1.zip
       curl -sI https://salishsea.io/dwca/salishsea-occurrences-v1.parquet
       curl -sI https://salishsea.io/dwca/salishsea-occurrences-v1.zip.sha256
       curl -sI https://salishsea.io/dwca/salishsea-occurrences-v1.parquet.sha256
       ```
       Expected: every response is `HTTP/2 200`. The `.zip` Content-Type should be `application/zip` or `application/octet-stream` (NOT `text/html`). The `.sha256` Content-Type should be `text/plain` or `application/octet-stream`.

    4. Round-trip the sidecar manually as a final V-01 sanity check:
       ```
       cd /tmp
       curl -sSfO https://salishsea.io/dwca/salishsea-occurrences-v1.zip
       curl -sSfO https://salishsea.io/dwca/salishsea-occurrences-v1.zip.sha256
       sha256sum -c salishsea-occurrences-v1.zip.sha256
       ```
       Expected: `salishsea-occurrences-v1.zip: OK`.

    5. If everything is green, type "approved" — Phase 7 is complete pending Phase 8.

    6. If any step fails:
       - If `Build DwC-A` fails: typically a DSN issue (port 6543 instead of 5432, wrong password). Re-check Task 3.
       - If `Guard` fails: examine the structured diff — likely a real data issue (run a `select count(*) from dwc.occurrences` against the prod DB to verify ≥ 1000).
       - If `Publish to S3` fails: OIDC role / bucket policy. Check the role's permissions for `s3:PutObject` on `salishsea-io/site/dwca/*`.
       - If `Smoke verify` fails: Plan 07-02's L-01 may not be deployed (re-run Plan 07-02 Task 2 verify), OR CloudFront invalidation didn't propagate (wait 5 min and re-trigger).
       - In every case: a `dwca-nightly-failed` issue should auto-open with the guard-diff body — confirm that, then triage and trigger another run after fixing.
  </how-to-verify>
  <resume-signal>Type "approved" if the run was green and all 4 URLs return 200. If anything failed, describe what failed (step name, error output) and we will triage.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → AWS (OIDC) | Short-lived OIDC token assumes the existing `salishsea-deploy-action` IAM role. Trust scoped by AWS to this GitHub org/repo. |
| GitHub Actions runner → Supabase Postgres | DSN delivered via `secrets.SUPABASE_DB_URL` under `environment: production` (gated). Direct port-5432 connection; service-role credentials. |
| GitHub Actions runner → npm registry | `npm ci` against the project lockfile. Trust posture inherited from Phase 6 closeout. |
| GitHub Actions runner → CloudFront edge | `aws cloudfront create-invalidation` and `aws cloudfront wait` via the same OIDC role. |
| CloudFront edge → S3 origin | Existing OAC; no change. |
| Workflow runner → github.com REST API | `permissions.issues: write` for O-01 failure issue. Scoped to this repo's issues. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Information Disclosure | `SUPABASE_DB_URL` leak to workflow logs | mitigate | Secret is delivered via `env:` only on the two steps that need it (steps 5 and 7); never via `run:` string interpolation. GHA automatically masks secret values in logs (the `::add-mask::` semantics). No `set -x`, no `echo "$SUPABASE_DB_URL"`. Plan 07-01's `guard.ts` re-uses Phase 6's `maskDsn` for any error path. Task 5 verifies by scanning the "Build DwC-A" step logs for `postgres://` substring. |
| T-7-02 | Elevation of Privilege | Overprivilege of OIDC role | accept (documented) | `salishsea-deploy-action` permits `s3:PutObject` on the whole `salishsea-io` bucket including the SPA's `/site/*` keys (not just `/site/dwca/*`) and `cloudfront:CreateInvalidation` on the whole distribution. CONTEXT D-04 explicitly accepts this for v1.2; tighter `salishsea-dwca-publish` role is in §Deferred Ideas. The workflow only WRITES `site/dwca/*` per the four `aws s3 cp` invocations; the broader permission is unused in practice. |
| T-7-03 | Tampering / Supply Chain | Compromised `peter-evans/create-issue-from-file` action | mitigate | SHA-pinned to `fca9117c27cdc29c6c4db3b86c48e4115a786710` (v6.0.0). Task 2 is a MANDATORY `checkpoint:human-verify` gating the SHA at github.com BEFORE the push — `workflow.auto_advance` is ignored for this checkpoint per the planner gate. ASVS V14 (configuration: SHA-pin every external action) satisfied. |
| T-7-04 | Tampering / Stale archive | Consumer fetches yesterday's zip with today's sha sidecar (mid-publish window) | mitigate | P-02 checksum-LAST upload order: a `(sha-then-zip)` consumer always sees a consistent pair. Window is ~seconds. I-01 + Pattern 4 invalidation + V-01 smoke check are the final close-the-loop. RESEARCH §Pattern 1 documents the rationale; deferred `manifest.json` pointer is the future SLO upgrade. |
| T-7-05 | Tampering / Response substitution | Lambda@Edge intercepts `/dwca/*` for bot UAs | mitigate | Addressed by Plan 07-02's L-01 carve-out, deployed BEFORE this plan's Task 5 first-run via the gating dependency `depends_on: [07-02-PLAN]` + Plan 07-02's Task 2 deployment verify. Task 5's `curl -sI` confirms `Content-Type: application/zip` (not `text/html`) — runtime regression gate. |
| T-7-06 | Denial of Service | Workflow runs concurrently from cron + manual dispatch | mitigate | `concurrency: dwca-nightly` with `cancel-in-progress: false` queues the second; never two concurrent S3 writers to the same keys. (Per W-03.) |
| T-7-07 | Tampering | `workflow_dispatch` user input bypasses guard | mitigate | No `workflow_dispatch.inputs` defined — manual triggers run the full pipeline including guard. Per CONTEXT §"Anti-Patterns to Avoid", no `skip-guard` input. |
| T-7-08 | Information Disclosure | Guard-diff body in failure issue exposes row counts + byte sizes | accept | Aggregate row counts and file sizes are not secrets. The issue is opened in our own repo's issue tracker (private if the repo is private); operator-readable. |
| T-7-09 | Tampering | Lambda@Edge replication race — first cron run hits an old replicated version | mitigate by ordering | Plan 07-02 (Wave 1) must complete BEFORE this plan's Task 5. The `depends_on: [07-02-PLAN]` frontmatter declares this; Plan 07-02's Task 2 human-verify proves the replication is complete before Plan 07-03's Task 5 runs. |

ASVS L1 mappings:
- V2 Authentication: OIDC role assumption (no long-lived AWS keys).
- V4 Access Control: `environment: production` gates secret access; `permissions:` block minimal (id-token: write, contents: read, issues: write).
- V5 Input Validation: workflow consumes only `vars.*` and `secrets.*` from trusted GH environment; no untrusted user input.
- V6 Cryptography: sha256 (FIPS-acceptable); no MD5/SHA-1.
- V14 Configuration: every external action SHA-pinned; `environment: production` ties secret access to the protected environment.
</threat_model>

<verification>
- File `.github/workflows/dwca-nightly.yml` parses as valid YAML.
- All grep-based static assertions in Task 1 acceptance_criteria pass.
- Tasks 2, 3 approved.
- After Task 4 push: `gh run list --workflow=dwca-nightly.yml --limit 1` shows the workflow is registered (may have no runs yet — the file is on main).
- After Task 5 dispatch: `gh run list --workflow=dwca-nightly.yml --limit 1 --json status,conclusion --jq '.[0]'` reports `{"status":"completed","conclusion":"success"}`.
- Post-publish HEAD checks: all four `https://salishsea.io/dwca/salishsea-occurrences-v1.*` URLs return HTTP 200.
- Round-trip: `sha256sum -c salishsea-occurrences-v1.zip.sha256` on the downloaded zip + sidecar reports `OK`.
- `gh issue list --label dwca-nightly-failed --state open` returns no issues after the successful first run.
</verification>

<success_criteria>
- ✅ `.github/workflows/dwca-nightly.yml` lands on `main` matching every locked CONTEXT decision (S-01..W-03) and every RESEARCH-required SHA pin. (EXPORT-01)
- ✅ One `workflow_dispatch` run completed successfully against production (EXPORT-01 verified at runtime).
- ✅ `https://salishsea.io/dwca/salishsea-occurrences-v1.zip` + `.parquet` reachable with non-text/html Content-Type. (EXPORT-02)
- ✅ Upload order parquet→zip→parquet.sha256→zip.sha256 + guard tripped on under-threshold + invalidation `/dwca/*`. (EXPORT-03)
- ✅ `.sha256` sidecars published in GNU coreutils format; `sha256sum -c` round-trip green. (EXPORT-04)
- ✅ `.parquet` treated symmetrically with `.zip` for atomic publish + guard + invalidation + checksum. (EXPORT-05)
- ✅ No `dwca-nightly-failed` issue opened. T-7-01 through T-7-09 mitigations in place and verified.
- ✅ Phase 7 closes; Phase 8 (frontend download link) is unblocked.
</success_criteria>

<output>
Create `.planning/phases/07-nightly-workflow-hosting/07-03-SUMMARY.md` when done. Required keys in frontmatter: `phase`, `plan`, `requires: [07-01-PLAN, 07-02-PLAN]`, `provides: ["dwca-nightly.yml on main", "first successful workflow_dispatch run", "published https://salishsea.io/dwca/salishsea-occurrences-v1.{zip,parquet,zip.sha256,parquet.sha256}"]`, `affects: [Phase 8 (DOWNLOAD-01) — stable URL is now live]`, `tech-stack.added: ["peter-evans/create-issue-from-file@fca9117c27cdc29c6c4db3b86c48e4115a786710 (v6.0.0)", "actions/github-script@<pinned-SHA>"]`, `tech-stack.patterns: ["Checksum-LAST upload order for object-store atomicity", "CloudFront invalidation + waiter + smoke check post-publish", "Two-step failure-issue dedupe via github-script + create-issue-from-file"]`, `key-files.created: [.github/workflows/dwca-nightly.yml]`, `key-decisions` (include the actions/github-script SHA picked at plan time + any waiter timeout observation from Task 5), `patterns-established`, `requirements-completed: [EXPORT-01, EXPORT-02, EXPORT-03, EXPORT-04, EXPORT-05]`.

This SUMMARY closes Phase 7. The follow-up DWCA-05 (GBIF validator) deferred from Phase 6 remains independently tracked.
</output>
