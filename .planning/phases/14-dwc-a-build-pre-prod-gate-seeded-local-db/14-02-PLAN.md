---
phase: 14-dwc-a-build-pre-prod-gate-seeded-local-db
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - .github/workflows/build.yml
autonomous: false
requirements:
  - DWCA-GATE-03
  - DWCA-GATE-04
  - DWCA-GATE-05
  - DWCA-GATE-07
must_haves:
  truths:
    - "build.yml applies supabase/ci-seed.sql after `supabase db start` and before `npm test`"
    - "build.yml exports SUPABASE_DB_URL scoped to the `npm test` step only (not job-wide, not on the build/gen-types steps)"
    - "With SUPABASE_DB_URL exported in CI, the build.test.ts suite un-skips and the DWCA-01..04/06 assertions execute on every PR"
    - "A fresh checkout with NO SUPABASE_DB_URL still passes `npm test` with build.test.ts reported skipped (no-DSN skip path preserved)"
    - "guard.ts / ROW_FLOOR remain untouched — the gate runs build.test.ts only, not the G-01..G-04 floors"
    - "A deliberate bare-schema-ref regression (aad63dd-style) makes the seeded suite go red, proving the gate has teeth"
  artifacts:
    - path: ".github/workflows/build.yml"
      provides: "PR Build job extended with a CI-fixture apply step + a step-scoped SUPABASE_DB_URL on the test step"
      contains: "supabase db query --local --file supabase/ci-seed.sql"
  key_links:
    - from: ".github/workflows/build.yml (Apply CI seed fixture step)"
      to: "supabase/ci-seed.sql (Plan 01 artifact)"
      via: "supabase db query --local --file"
      pattern: "ci-seed\\.sql"
    - from: ".github/workflows/build.yml (Run tests step env)"
      to: "scripts/dwca/build.test.ts HAS_DSN gate"
      via: "step-scoped SUPABASE_DB_URL"
      pattern: "SUPABASE_DB_URL"
---

<objective>
Extend the existing `.github/workflows/build.yml` Build job (D-01) so the
DwC-A integration suite becomes a true pre-merge gate: apply the Plan 01 CI
fixture after migrations, then export `SUPABASE_DB_URL` scoped to the existing
`npm test` step so `build.test.ts` un-skips in place (D-02). Then prove the
gate's two correctness contracts: the no-DSN skip path is preserved
(cross-cutting constraint) and the gate goes RED on a deliberate
bare-schema-ref regression (draft SC#4, manual one-time red-test).

Purpose: Plan 01 produced the fixture and proved it makes the suite green
locally. This plan is the activation half — the two-line workflow edit that
turns the existing required "Build" check into the gate, plus the two
verifications that the gate is both non-disruptive (no-DSN green) and
load-bearing (regression goes red). No new workflow/job is created (D-01);
`guard.ts`/`ROW_FLOOR` stay nightly-only and untouched (D-03, D-04).

Output: a modified `.github/workflows/build.yml` (one new named step + one
`env:` block on the existing test step), a recorded no-DSN green run, and a
recorded manual red-test result.

Minted requirement IDs addressed by this plan (full set minted in 14-01-PLAN.md
`<objective>`; back-fill all of DWCA-GATE-01..07 into ROADMAP.md `### Phase 14`
Requirements line — currently "TBD"):
  DWCA-GATE-03 — `build.yml` applies the fixture + exports SUPABASE_DB_URL scoped to `npm test`; suite un-skips (D-01, D-02)
  DWCA-GATE-04 — `guard.ts`/`ROW_FLOOR` untouched, nightly-only (D-03, D-04) — satisfied by omission + asserted in verification
  DWCA-GATE-05 — no-DSN `npm test` still passes with build.test.ts skipped (cross-cutting constraint)
  DWCA-GATE-07 — gate fails on a deliberate bare-schema-ref regression (manual red-test, draft SC#4)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/14-dwc-a-build-pre-prod-gate-seeded-local-db/14-CONTEXT.md
@.planning/phases/14-dwc-a-build-pre-prod-gate-seeded-local-db/14-RESEARCH.md
@.planning/phases/14-dwc-a-build-pre-prod-gate-seeded-local-db/14-VALIDATION.md

# This plan's upstream artifact: the fixture the workflow applies
@.planning/phases/14-dwc-a-build-pre-prod-gate-seeded-local-db/14-01-PLAN.md

# The exact workflow file to edit (current step order)
@.github/workflows/build.yml
# The suite being activated (HAS_DSN gate) — DO NOT modify
@scripts/dwca/build.test.ts
# The pipeline under test; Step 15.5 is the aad63dd-prone query — read for the red-test
@scripts/dwca/build.ts
# OUT OF SCOPE — confirm this file is NOT touched by this plan (nightly-only)
@scripts/dwca/guard.ts
# The nightly workflow whose behavior must NOT change (the gate front-runs it)
@.github/workflows/dwca-nightly.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend build.yml — apply CI fixture + export step-scoped SUPABASE_DB_URL</name>
  <files>.github/workflows/build.yml</files>
  <read_first>
    - .github/workflows/build.yml — the current Build job. Step order today:
      `- uses: supabase/setup-cli@... (v2.53.6)` → `- run: supabase db start` →
      `- name: Verify generated types match Postgres schema` (runs gen-types) →
      `- run: npm run build` → `- run: npm test` → infra steps. The existing
      `supabase db start` step has NO `name:` (it is a bare `- run:`); leave it
      as-is to minimize the diff.
    - 14-RESEARCH.md §Q4 (the exact build.yml edit + placement rationale) and §Q5
      (the `supabase db query --local --file` invocation, available in the pinned
      CLI v2.53.6; psql fallback if the flag is unavailable).
    - 14-RESEARCH.md §Q1 — `supabase db start` does NOT run seed.sql (seeding is
      exclusive to `db reset`), so the live-fetch seed cannot reach CI; the static
      fixture is the only seed applied. No `--no-seed` flag or config change needed.
    - scripts/dwca/build.test.ts — `HAS_DSN = !!process.env['SUPABASE_DB_URL']`
      flips `describe.skip` → `describe`. The env var is read at module load, so it
      must be present in the `npm test` step's environment (a step-scoped `env:`
      block is sufficient — vitest's loadEnv supplements but does not override
      process.env).
  </read_first>
  <action>
    Edit `.github/workflows/build.yml` with exactly two additive changes inside
    the existing `build` job's `steps:` list. Do NOT add a new job or workflow
    (D-01). Do NOT change the infra steps, the checkout/setup-node/npm-ci steps,
    the `supabase/setup-cli` pin, or the `supabase db start` step.

    Change 1 — insert a new named step IMMEDIATELY AFTER the existing
    `- run: supabase db start` step and BEFORE the `Verify generated types match
    Postgres schema` step:
      name: `Apply CI seed fixture`
      run: `supabase db query --local --file supabase/ci-seed.sql`
    Placement rationale: the fixture must run after migrations are applied (so the
    reference rows from migration 20260619184037 and the dwc views exist) and
    before the test step. gen-types and `npm run build` do not need the seed, so
    placing the apply step right after `db start` keeps the fixture available for
    everything downstream while keeping the diff localized.

    Change 2 — attach a step-scoped `env:` block to the EXISTING `- run: npm test`
    step (the FIRST one — the root-project test step at the top level, NOT the
    `working-directory: infra` one). Convert the bare `- run: npm test` to a named
    step `Run tests` with:
      run: `npm test`
      env:
        SUPABASE_DB_URL: `postgresql://postgres:postgres@127.0.0.1:54322/postgres`
    Scope the env to THIS step only — do NOT add SUPABASE_DB_URL to a job-level
    `env:` block, and do NOT put it on the gen-types step or the `npm run build`
    step (avoids any accidental DSN leakage into unrelated step logs; the DSN is
    the well-known local-dev credential, not a secret, but step-scoping is the
    documented hygiene per RESEARCH §Q4). The `infra` `npm test` step (the one with
    `working-directory: infra`) must remain unchanged and must NOT receive the env.

    Do NOT touch scripts/dwca/guard.ts or the ROW_FLOOR logic — the gate runs
    build.test.ts only (D-03/D-04); guard.ts stays nightly-only. Do NOT touch
    .github/workflows/dwca-nightly.yml.
  </action>
  <acceptance_criteria>
    - `.github/workflows/build.yml` contains a step named `Apply CI seed fixture`
      whose `run` is exactly `supabase db query --local --file supabase/ci-seed.sql`,
      positioned after the `supabase db start` step and before the
      `Verify generated types match Postgres schema` step.
    - The root-project test step carries a step-scoped `env:` with
      `SUPABASE_DB_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres`.
    - `grep -v '^[[:space:]]*#' .github/workflows/build.yml | grep -c SUPABASE_DB_URL`
      returns exactly 1 (the env appears on the test step only, not job-wide).
    - The `working-directory: infra` test step is unchanged and has no
      SUPABASE_DB_URL env.
    - `git diff --stat` shows ONLY `.github/workflows/build.yml` modified (no edits
      to build.ts, build.test.ts, guard.ts, dwca-nightly.yml, or supabase/seed.sql).
    - The workflow YAML is valid: `python3 -c "import yaml;
      yaml.safe_load(open('.github/workflows/build.yml'))"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/build.yml'))" && grep -q 'supabase db query --local --file supabase/ci-seed.sql' .github/workflows/build.yml && test "$(grep -v '^[[:space:]]*#' .github/workflows/build.yml | grep -c SUPABASE_DB_URL)" = "1"</automated>
  </verify>
  <done>
    build.yml has the `Apply CI seed fixture` step in the correct position and a
    single step-scoped SUPABASE_DB_URL on the root `npm test` step; YAML parses;
    no other files changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the no-DSN skip path is preserved (cross-cutting constraint)</name>
  <files>None — verification only (no files modified; runs `npm test` with the DSN unset)</files>
  <read_first>
    - scripts/dwca/build.test.ts lines 51-67 — the gate: `const DSN =
      process.env['SUPABASE_DB_URL']; const HAS_DSN = !!DSN; const d = HAS_DSN ?
      describe : describe.skip;`. With no DSN, the whole describe block is
      `describe.skip` and every nested test reports as skipped; vitest exit code
      stays 0.
    - 14-VALIDATION.md §Per-Task Verification Map (NO-DSN proof) and §Sampling Rate
      ("After every task commit: Run `npm test` (no DSN)").
    - package.json — `"test": "vitest"`. Use `npm test -- run` locally to force a
      single non-watch run (CI auto-detects non-TTY; locally pass `run`).
  </read_first>
  <action>
    Confirm the existing fast unit-test contract still holds on a fresh checkout
    with NO database wired in. In a shell with `SUPABASE_DB_URL` UNSET (explicitly
    `unset SUPABASE_DB_URL` first, and ensure no `.env.local` defines it — if
    `.env.local` exists locally and sets the DSN, run from a clean env or
    temporarily move it for this check), run `npm test -- run`. Confirm the process
    exits 0 and the vitest reporter shows the `build:dwca integration` describe
    block as SKIPPED (not failed, not run). This proves DWCA-GATE-05: the no-DSN
    path is preserved, so contributors without a local stack still get a green
    `npm test`.

    This task writes NO code and modifies NO files — it is the cross-cutting
    constraint verification. Record the skipped-suite reporter line in the SUMMARY.
  </action>
  <acceptance_criteria>
    - With `SUPABASE_DB_URL` unset, `npm test -- run` exits 0.
    - The vitest reporter marks the `build:dwca integration (DWCA-01..04/06 ...)`
      describe block as skipped (its tests are not executed and not failed).
    - No source/test/workflow files are modified by this verification (`git status`
      clean except for the build.yml change from Task 1).
  </acceptance_criteria>
  <verify>
    <automated>env -u SUPABASE_DB_URL npm test -- run</automated>
  </verify>
  <done>
    `npm test -- run` with no DSN exits 0 and reports the build.test.ts integration
    block as skipped; no files changed by this task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual red-test — prove the gate fails on a bare-schema-ref regression (SC#4)</name>
  <files>scripts/dwca/build.ts — TEMPORARILY mutated on a throwaway scratch branch, then restored via `git checkout --`. NO committed change to this file.</files>
  <read_first>
    - 14-CONTEXT.md §"Claude's Discretion" (Regression-proof) and 14-VALIDATION.md
      §Manual-Only Verifications (RED-TEST) — the locked approach: a one-time manual
      red-test on a scratch branch, NOT a permanent committed negative test.
    - scripts/dwca/build.ts Step 15.5 (the associated-parties query) — every
      Postgres relation is `pgdb.`-qualified (`pgdb.maplify.sightings`,
      `pgdb.public.collections`, `pgdb.public.organizations`). The aad63dd
      regression was a bare un-`pgdb.`-qualified ref producing `Catalog Error:
      schema "maplify" does not exist`. This is the exact line to mutate for the
      red-test.
    - 14-RESEARCH.md Pitfall 6 — the bare-schema-ref failure mode and why the
      runtime gate is the complement to the static build-queries.test.ts guard.
  </read_first>
  <what-built>
    Task 1 wired the seeded DwC-A suite into the PR Build job; Plan 01 proved the
    suite is green against the fixture. This checkpoint proves the gate is
    LOAD-BEARING — that it actually catches the class of bug it exists to catch
    (the aad63dd bare-schema-ref regression that previously only surfaced in the
    nightly post-deploy). Per the locked decision this is a one-time manual
    red-test, deliberately NOT a permanent committed test (keeps the suite clean).
  </what-built>
  <action>
    Execute the manual red-test below (this checkpoint task makes NO permanent code
    change — the build.ts mutation is throwaway and is restored before resuming).
    See <how-to-verify> for the exact step-by-step. The executor performs the
    mutation, runs the seeded suite, observes the red, restores build.ts, and then
    pauses for the human to confirm the observed failure + clean working tree.
  </action>
  <how-to-verify>
    On a throwaway scratch branch (do NOT commit), with the seeded local stack
    standing (run `supabase db reset && supabase db query --local --file
    supabase/ci-seed.sql` first if needed):

    1. In `scripts/dwca/build.ts` Step 15.5, change one `pgdb.`-qualified relation
       in the associated-parties query to a bare reference — e.g. change
       `FROM pgdb.maplify.sightings s` to `FROM maplify.sightings s` (drop the
       `pgdb.` prefix). This reproduces the aad63dd regression.
    2. Run the seeded suite exactly as CI does:
       `SUPABASE_DB_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres npm
       test -- run`
    3. Confirm the suite goes RED: `build.test.ts`'s `beforeAll` (which runs
       `npm run build:dwca`) throws because the build pipeline exits non-zero with a
       DuckDB `Catalog Error: schema "maplify" does not exist` (or equivalent), and
       the DWCA-* tests therefore fail. Expected: non-zero exit code, visible
       Catalog Error in the build output.
    4. Restore the file: `git checkout -- scripts/dwca/build.ts` (or discard the
       scratch branch). Confirm `git status` is clean (no lingering edit to
       build.ts).
    5. Re-run the seeded suite once more to confirm it is GREEN again after restore
       (sanity check that the red was caused solely by the injected regression).

    Report: the exact mutation made, the observed failure (error text + non-zero
    exit), and confirmation the working tree is clean after restore.
  </how-to-verify>
  <verify>
    <human-check>The injected bare-schema-ref made `SUPABASE_DB_URL=... npm test -- run` exit non-zero with a DuckDB Catalog error naming the unqualified schema; after `git checkout -- scripts/dwca/build.ts` the working tree is clean and the seeded suite is green again; no permanent negative test was committed.</human-check>
  </verify>
  <acceptance_criteria>
    - With the injected bare-schema-ref, `SUPABASE_DB_URL=... npm test -- run`
      exits NON-ZERO and the output contains a DuckDB Catalog/binder error naming
      the unqualified schema (e.g. `schema "maplify" does not exist`).
    - After `git checkout -- scripts/dwca/build.ts`, `git status` shows no
      modification to scripts/dwca/build.ts, and the seeded suite is GREEN again.
    - No permanent negative/regression test is committed (the red-test is a
      throwaway mutation, per the locked decision — verify no new `*.fails`/negative
      test file exists under scripts/dwca/).
  </acceptance_criteria>
  <done>
    The red-test confirmed the gate goes red on a bare-schema-ref regression
    (non-zero exit + Catalog error), the working tree is clean after restore, the
    seeded suite is green again, and no permanent negative test was committed.
  </done>
  <resume-signal>Type "approved" (red-test confirmed the gate goes red and the working tree is clean after restore) or describe what you observed instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → local Supabase Postgres | The CI fixture-apply and test steps run against the ephemeral local stack (port 54322) on the runner; no production database, no real secrets cross this boundary. |
| `build.yml` step env → step logs | `SUPABASE_DB_URL` is injected as a step-scoped env on the `npm test` step. The value is the well-known Supabase local-dev DSN (`postgresql://postgres:postgres@127.0.0.1:54322/postgres`), NOT a secret (per RESEARCH §Security Domain) — no masking required. `build.ts`/`guard.ts` already `maskDsn` it from their own logs regardless. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information Disclosure | `SUPABASE_DB_URL` in build.yml | accept | The DSN is the documented Supabase local-dev default, not sensitive (RESEARCH §Security Domain). No production credential is introduced. Acceptance criteria assert it appears exactly once and is step-scoped (not job-wide), limiting log exposure to the test step. |
| T-14-05 | Tampering | scope creep into guard.ts / nightly workflow | mitigate | Acceptance criteria assert `git diff --stat` shows ONLY build.yml modified — guard.ts, ROW_FLOOR, and dwca-nightly.yml are explicitly out of scope (D-03/D-04) and protected by the diff assertion. |
| T-14-06 | Tampering | leftover red-test mutation in build.ts | mitigate | Task 3 acceptance criteria require `git status` clean after `git checkout -- scripts/dwca/build.ts`; the red-test is a throwaway scratch mutation, never committed. Prevents an accidental broken build.ts merge. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (configuration only; RESEARCH §Standard Stack confirms zero new packages — the existing `npm ci` installs unchanged dependencies). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `.github/workflows/build.yml` parses as valid YAML and contains the
  `Apply CI seed fixture` step (`supabase db query --local --file supabase/ci-seed.sql`)
  in the correct position (after `db start`, before gen-types).
- `SUPABASE_DB_URL` appears exactly once in build.yml (step-scoped on the root
  `npm test` step), not job-wide; the `infra` test step is unchanged.
- No-DSN `npm test -- run` exits 0 with build.test.ts reported skipped
  (cross-cutting constraint preserved).
- Manual red-test (Task 3 checkpoint): a bare-schema-ref regression makes the
  seeded suite exit non-zero with a Catalog Error; working tree clean after
  restore; suite green again.
- guard.ts, ROW_FLOOR, and .github/workflows/dwca-nightly.yml are unchanged
  (D-03/D-04 — gate runs build.test.ts only).
</verification>

<success_criteria>
- build.yml extends the EXISTING Build job (no new workflow/job) to apply the CI
  fixture after migrations and export a step-scoped SUPABASE_DB_URL so
  build.test.ts un-skips on every PR (D-01, D-02).
- The no-DSN skip path is preserved: a DB-less `npm test` is still green
  (DWCA-GATE-05).
- The gate is proven load-bearing: a deliberate aad63dd-style regression turns
  the seeded suite red, then the working tree is restored clean (DWCA-GATE-07 /
  draft SC#4).
- guard.ts / ROW_FLOOR and the nightly workflow are untouched (DWCA-GATE-04).
</success_criteria>

<output>
Create `.planning/phases/14-dwc-a-build-pre-prod-gate-seeded-local-db/14-02-SUMMARY.md` when done.
Record: the final build.yml diff (step name + placement + step-scoped env), the
no-DSN skipped-suite reporter line, the exact red-test mutation + observed
Catalog Error + non-zero exit, and confirmation the working tree was clean after
restore. Note that DWCA-GATE-01..07 should be back-filled into ROADMAP.md
`### Phase 14` Requirements (currently "TBD") and that the origin todo
`2026-06-21-seeded-local-db-gate-for-dwca-build.md` can be closed.
</output>
