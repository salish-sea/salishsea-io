---
phase: 14-dwc-a-build-pre-prod-gate-seeded-local-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/ci-seed.sql
autonomous: true
requirements:
  - DWCA-GATE-01
  - DWCA-GATE-02
  - DWCA-GATE-06
must_haves:
  truths:
    - "Applying supabase/ci-seed.sql to a freshly-migrated local DB exits 0 with no FK/constraint/PK errors"
    - "After the fixture is applied, dwc.occurrences has >= 1 row (>=1 native + >=2 trusted Maplify; the untrusted Maplify row is excluded)"
    - "After the fixture is applied, dwc.multimedia has >= 1 row (the native observation photo)"
    - "The Step 15.5 associated-parties query returns >= 1 org (Orca Network via the orca-network collection)"
    - "supabase db reset applies the full migration chain from scratch and exits 0 (RESEARCH A3 risk retired)"
    - "The fixture references existing provider/organization/collection IDs and does NOT re-insert reference rows"
  artifacts:
    - path: "supabase/ci-seed.sql"
      provides: "CI-only static fixture: source rows for maplify.sightings + public.observations + public.observation_photos referencing migration-seeded reference IDs"
      contains: "INSERT INTO maplify.sightings"
      min_lines: 40
  key_links:
    - from: "supabase/ci-seed.sql"
      to: "inaturalist.taxa(id) = 41521"
      via: "taxon_id FK on maplify.sightings + public.observations"
      pattern: "41521"
    - from: "supabase/ci-seed.sql"
      to: "public.collections(id) = 1 (orca-network, organization_id=1)"
      via: "collection_id on the bracket-tagged trusted Maplify row"
      pattern: "collection_id"
    - from: "supabase/ci-seed.sql"
      to: "public.contributors (via auth.users trigger or direct insert)"
      via: "contributor_id on the native observation (INNER JOIN in dwc._native_occurrences)"
      pattern: "contributor_id"
---

<objective>
Author `supabase/ci-seed.sql` — the CI-only static fixture that makes the
`dwc.occurrences` / `dwc.multimedia` view chain non-empty and deliberately
exercises the bug-prone query branches (trust filtering, recordedBy regex,
the Step 15.5 associated-parties join), AND verify that `supabase db reset`
applies the full migration chain cleanly from scratch.

Purpose: This fixture is the data half of the pre-prod gate. Plan 02 wires it
into CI; without it the gate has nothing to run against. It must reference the
reference rows already seeded by migration `20260619184037_reference_tables.sql`
(D-06) and insert ONLY source rows (D-05), branch-covering minimal (D-07).

Output: `supabase/ci-seed.sql` (new file), plus a confirmed-clean `supabase db
reset` run that retires RESEARCH risk A3.

Minted requirement IDs introduced by this phase (back-fill into ROADMAP.md
`### Phase 14` Requirements line — currently "TBD"):
  DWCA-GATE-01 — CI-only static fixture `supabase/ci-seed.sql` applied after migrations (D-05, D-06)
  DWCA-GATE-02 — fixture is branch-covering minimal: trusted+untrusted, tagged+untagged, with/without collection_id, >=1 multimedia (D-07)
  DWCA-GATE-03 — `build.yml` applies fixture + exports SUPABASE_DB_URL scoped to `npm test`; suite un-skips (D-01, D-02, D-03) [Plan 02]
  DWCA-GATE-04 — `guard.ts`/`ROW_FLOOR` untouched, nightly-only (D-03, D-04) [Plan 02, by omission]
  DWCA-GATE-05 — no-DSN `npm test` still passes (skip path preserved) [Plan 02]
  DWCA-GATE-06 — `supabase db reset` applies the full migration chain cleanly from scratch (RESEARCH A3)
  DWCA-GATE-07 — gate fails on a deliberate bare-schema-ref regression (manual red-test, SC#4) [Plan 02]
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/14-dwc-a-build-pre-prod-gate-seeded-local-db/14-CONTEXT.md
@.planning/phases/14-dwc-a-build-pre-prod-gate-seeded-local-db/14-RESEARCH.md
@.planning/phases/14-dwc-a-build-pre-prod-gate-seeded-local-db/14-VALIDATION.md

# The view chain the fixture must satisfy (exact JOINs + WHERE clauses + 26 columns)
@supabase/migrations/20260621000000_dwc_view_rebuild.sql
# dwc.multimedia view (native-only photo join; license_code CASE)
@supabase/migrations/20260617203900_dwc_schema.sql
# Reference rows the fixture references but must NOT re-insert
@supabase/migrations/20260619184037_reference_tables.sql
# FK defaults: provider_id, collection_id defaults on source tables
@supabase/migrations/20260619203013_source_table_fk_columns.sql
# contributor + user_contributor + the AFTER INSERT ON auth.users trigger
@supabase/migrations/20260203234153_individuals.sql
# Existing live-fetch seed — LEAVE UNCHANGED (do not edit this file)
@supabase/seed.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author supabase/ci-seed.sql (branch-covering minimal source-row fixture)</name>
  <files>supabase/ci-seed.sql</files>
  <read_first>
    - supabase/migrations/20260621000000_dwc_view_rebuild.sql — the EXACT view chain. Note: `dwc._maplify_occurrences` WHERE is `NOT s.is_test AND s.number_sighted BETWEEN 1 AND 1000 AND s.source != 'rwsas' AND s.trusted`, with `JOIN dwc.taxa_classification tc ON tc.taxon_id = s.taxon_id` (INNER — NULL taxon_id excluded) and `LEFT JOIN public.collections c_coll`. `dwc._native_occurrences` uses `JOIN public.contributors c ON c.id = o.contributor_id` and `JOIN public.collections c_coll ON c_coll.id = o.collection_id` (both INNER — NULL excludes the row). The native individualCount column is `o.count::integer` (the source column is `public.observations.count`, NOT `number`). recordedBy regex is `^\[[^\]]+\]\s+.+?\(([^()]+)\)` over `split_part(s.comments,'<br>',1)`, NULLed if the captured name contains a comma or starts with `IDs? `.
    - supabase/migrations/20260617203900_dwc_schema.sql (the `dwc.multimedia` view, ~lines 620-697) — JOINs `public.observation_photos op` → `public.observations o` → `public.contributors c`, coreId is `'salishsea:' || op.observation_id::text`, WHERE `op.license_code IS NOT NULL AND op.license_code <> 'none'`. license_code `'cc-by'` is included.
    - supabase/migrations/20260619184037_reference_tables.sql — confirm the exact seeded IDs: providers id 1 (direct) / id 2 (maplify); collections id 1 (orca-network, organization_id=1) / id 10 (salishsea-direct, no org); organizations id 1 (orca-network). The fixture REFERENCES these — never re-INSERTs them.
    - supabase/migrations/20260619203013_source_table_fk_columns.sql — `maplify.sightings.provider_id` DEFAULT 2; `public.observations.provider_id` DEFAULT 1, `public.observations.collection_id` DEFAULT 10. These defaults mean the native observation needs no explicit provider_id/collection_id.
    - supabase/migrations/20260203234153_individuals.sql (lines 6-54) — `public.contributors` (id IDENTITY, name NOT NULL), `public.user_contributor (user_uuid uuid PK, contributor_id NOT NULL)`, and the `create_contributor_on_sign_in` trigger that fires `AFTER INSERT ON auth.users FOR EACH ROW` and synchronously inserts BOTH a contributors row and a user_contributor mapping row.
    - 14-RESEARCH.md §Q2 (column lists, NOT NULL constraints, FK targets) and §"Pattern: Fixture Design for Minimal DwC Branch Coverage" (the reference fixture skeleton). NOTE: `public.observations` requires a NOT-NULL `user_uuid` REFERENCES auth.users(id) (re-added in migration 20260207000253); the auth.users row must be inserted first.
  </read_first>
  <action>
    Create `supabase/ci-seed.sql` as a single static SQL file (no live API calls,
    no network). Header comment must state: CI-only fixture, applied via
    `supabase db query --local --file supabase/ci-seed.sql` AFTER migrations; not
    run by `db start`/`db reset` (it is not in config.toml `[db.seed].sql_paths`);
    reference rows come from migration 20260619184037; safe to re-run only on a
    freshly-reset DB.

    Section 1 — native branch (feeds dwc._native_occurrences + dwc.multimedia):
    Insert one `auth.users` row with a fixed UUID
    `00000000-0000-0000-0000-000000000001`, email `ci-gate@example.com`,
    `created_at`/`updated_at` = NOW(), and `raw_user_meta_data` =
    `'{"name":"CI Gate Test"}'::jsonb`. The `create_contributor_on_sign_in` trigger
    fires synchronously on this INSERT, creating a `public.contributors` row named
    "CI Gate Test" and a `public.user_contributor` mapping. In a `DO $$ ... $$`
    block, read the contributor id via `SELECT contributor_id INTO v_contrib_id FROM
    public.user_contributor WHERE user_uuid = '00000000-0000-0000-0000-000000000001'`,
    then INSERT one `public.observations` row: explicit `id = gen_random_uuid()` (or a
    fixed UUID), `observed_at = NOW() - INTERVAL '1 day'`,
    `subject_location = gis.ST_Point(-123.3, 48.4)::gis.geography`, `taxon_id = 41521`
    (Orcinus orca, confirmed in inaturalist.taxa), the native count column
    `count = 2` (the source column is `public.observations.count`),
    explicit `contributor_id = v_contrib_id` (REQUIRED — the native view INNER-JOINs
    contributors), `user_uuid = '00000000-0000-0000-0000-000000000001'` (NOT NULL FK
    to auth.users), `created_at = NOW()`, `updated_at = NOW()`. Omit `provider_id`
    (DEFAULT 1) and `collection_id` (DEFAULT 10 → salishsea-direct, satisfies the
    native collection JOIN). Then INSERT one `public.observation_photos` row
    referencing that observation: explicit `id = 1`, `observation_id` = the
    observation's id, `seq = 1`, `href = 'https://example.com/ci-gate-test-photo.jpg'`,
    `license_code = 'cc-by'` (NOT 'none'/NULL → included in dwc.multimedia).

    If the `auth.users` trigger approach proves brittle in execution (e.g. an
    auth-schema NOT NULL column the fixture cannot satisfy), the documented fallback
    (RESEARCH A1) is to INSERT directly into `public.contributors (name) VALUES
    ('CI Gate Test') RETURNING id`, INSERT a matching `auth.users` row + a
    `public.user_contributor (user_uuid, contributor_id)` mapping so the NOT-NULL
    `observations.user_uuid` FK still resolves, then use that contributor_id on the
    observation. Prefer the trigger path; record which path was used in the SUMMARY.

    Section 2 — Maplify branch (feeds dwc._maplify_occurrences + Step 15.5). Use
    `INSERT INTO maplify.sightings (...) VALUES (...) ON CONFLICT (id) DO NOTHING`
    for each row. Required NOT-NULL columns per RESEARCH §Q2: `id, project_id,
    trip_id, scientific_name, location, number_sighted, created_at, in_ocean,
    moderated, trusted, is_test, source`. Omit `provider_id` (DEFAULT 2).
      - Row A (id=1): `trusted = TRUE`, `is_test = FALSE`, `number_sighted = 3`,
        `source = 'whale_alert'`, `taxon_id = 41521`, `collection_id = 1`
        (orca-network, organization_id=1), `location = gis.ST_Point(-122.9,
        48.5)::gis.geography`, and a BRACKET-TAGGED comment whose headline matches
        the recordedBy regex with a single non-comma parenthetical name, e.g.
        `'[Orca Network] 3 orcas heading north (Jane Smith)<br>All adults.'`. This
        row appears in dwc.occurrences with recordedBy='Jane Smith' AND surfaces in
        the Step 15.5 associated-parties result (collection 1 → org 1 Orca Network).
      - Row B (id=2): `trusted = FALSE` (all other NOT-NULLs valid, taxon_id=41521).
        Exercises the trust-filter branch: present in the fixture but EXCLUDED from
        dwc.occurrences by `AND s.trusted`.
      - Row C (id=3): `trusted = TRUE`, `is_test = FALSE`, `number_sighted = 2`,
        `source = 'whale_alert'`, `taxon_id = 41521`, NO `collection_id` (NULL → the
        LEFT JOIN falls through to the COALESCE datasetName fallback), and an
        UNTAGGED comment with no bracket prefix, e.g. `'Three orcas spotted near the
        rocks.'` → recordedBy regex returns NULL. Appears in dwc.occurrences but NOT
        in Step 15.5.

    Use `gis.ST_Point(lng, lat)::gis.geography` for all geometry. Do NOT touch
    `supabase/seed.sql`. Do NOT INSERT into providers/organizations/collections.
    Confirm the file contains no real/PII data and no production credentials — the
    only credential-shaped string is the well-known local DSN, which does NOT belong
    in this fixture (it lives in build.yml, Plan 02).
  </action>
  <acceptance_criteria>
    - `supabase/ci-seed.sql` exists and contains `INSERT INTO maplify.sightings`,
      `INSERT INTO public.observations`, and `INSERT INTO public.observation_photos`,
      and contains NO `INSERT INTO public.providers`, `public.organizations`, or
      `public.collections`.
    - On a freshly-reset local stack: `supabase db reset` exits 0 (full migration
      chain applies cleanly — DWCA-GATE-06 / RESEARCH A3). Then `supabase db query
      --local --file supabase/ci-seed.sql` exits 0 with no FK/constraint/PK error.
    - After applying the fixture, `supabase db query --local "SELECT count(*) FROM
      dwc.occurrences"` returns >= 3 (1 native + 2 trusted Maplify).
    - `supabase db query --local "SELECT count(*) FROM dwc.occurrences WHERE
      \"occurrenceID\" = 'maplify:2'"` returns 0 (untrusted Row B excluded).
    - `supabase db query --local "SELECT count(*) FROM dwc.multimedia"` returns >= 1.
    - `supabase db query --local "SELECT \"recordedBy\" FROM dwc.occurrences WHERE
      \"occurrenceID\" = 'maplify:1'"` returns `Jane Smith`.
    - The Step 15.5 org query (the SELECT DISTINCT org.name ... block from build.ts,
      run against the local DB with the maplify/public schema prefixes) returns a row
      containing `Orca Network`.
    - File contains no real PII and no production credentials (no prod DSN, no real
      Supabase project ref, no live secret).
  </acceptance_criteria>
  <verify>
    <automated>supabase db reset && supabase db query --local --file supabase/ci-seed.sql && supabase db query --local "SELECT (SELECT count(*) FROM dwc.occurrences) AS occ, (SELECT count(*) FROM dwc.multimedia) AS mm, (SELECT count(*) FROM dwc.occurrences WHERE \"occurrenceID\"='maplify:2') AS untrusted"</automated>
  </verify>
  <done>
    `supabase/ci-seed.sql` exists; `supabase db reset` (full chain) + fixture apply
    both exit 0; dwc.occurrences >= 3, dwc.multimedia >= 1, untrusted Row B count = 0;
    recordedBy for maplify:1 is "Jane Smith"; Step 15.5 returns Orca Network.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the seeded suite runs green end-to-end (full build:dwca + build.test.ts)</name>
  <files>None — verification only (runs `SUPABASE_DB_URL=... npm test` against the seeded DB; produces only gitignored dist/dwca/ build artifacts)</files>
  <read_first>
    - scripts/dwca/build.test.ts — the DB-gated integration suite. `HAS_DSN =
      !!process.env['SUPABASE_DB_URL']` flips `describe.skip` → `describe`. `beforeAll`
      runs `npm run build:dwca`. Assertions use `OCCURRENCE_FIELDS.length` (26 after
      Phase 12), not a hardcoded 25 — the in-comment "25" is stale but the code is
      dynamic (lines 186, 201, and DWCA-06 uses `OCCURRENCE_FIELDS.length + 1`).
    - scripts/dwca/build.ts — the pipeline. Step 8 `assertNonZeroRows(pgdb.dwc.occurrences)`
      is the hard gate the fixture must satisfy; Step 15.5 is the associated-parties
      query (all relations pgdb-qualified). build:dwca = `tsx scripts/dwca/build.ts`.
    - 14-RESEARCH.md §Q3 (per-test fixture requirements) and §Q5 (the `supabase db
      query --local --file` invocation).
    - package.json — `"test": "vitest"`. Use `npm test -- run` locally to force a
      single non-watch run.
  </read_first>
  <action>
    With the fixture applied (Task 1's seeded DB still standing), run the full
    integration suite locally with the DSN exported, exactly as CI will:
    `SUPABASE_DB_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres npm test -- run`.
    Confirm `build.test.ts` ACTIVATES (its describe block runs — not skipped) and
    every DWCA-01..04/06 test passes, including the DWCA-06 parquet row-parity check
    (parquet row count == dwc.occurrences row count) and the DWCA-02 round-trip
    (first occurrence row has `OCCURRENCE_FIELDS.length` = 26 fields). The
    `npm run build:dwca` child process inside `beforeAll` must exit 0 (Step 8
    assertNonZeroRows passes because the fixture made dwc.occurrences non-empty,
    and the Step 15.5 pgdb-qualified query succeeds).

    This task writes NO code and modifies NO files — it is a verification gate that
    proves the fixture from Task 1 is sufficient for the suite BEFORE Plan 02 wires
    it into CI. If any DWCA test fails, the fix belongs in Task 1's fixture (e.g. an
    additional/adjusted row), not in the test or the build pipeline (both are locked
    out of scope). Record the passing test summary in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `SUPABASE_DB_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres npm
      test -- run` exits 0.
    - The vitest reporter shows the `build:dwca integration (DWCA-01..04/06 ...)`
      describe block as RUN (its tests are present and passing), NOT skipped.
    - DWCA-06 row-parity passes (parquet count == view count) and DWCA-02 round-trip
      asserts 26 fields on the first row.
    - No changes to scripts/dwca/build.ts, scripts/dwca/build.test.ts, scripts/dwca/guard.ts,
      or any fields file (`git status` shows only supabase/ci-seed.sql modified, plus
      build artifacts under dist/dwca which are gitignored).
  </acceptance_criteria>
  <verify>
    <automated>SUPABASE_DB_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres npm test -- run</automated>
  </verify>
  <done>
    The seeded full suite exits 0 with `build.test.ts` activated and all
    DWCA-01..04/06 tests green; no source/test files changed (only
    supabase/ci-seed.sql added).
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

This phase (both plans) creates exactly the following artifacts. Nothing else is
created, renamed, or deleted.

| Artifact | Plan | Type | Notes |
|----------|------|------|-------|
| `supabase/ci-seed.sql` | 14-01 | new file | CI-only static fixture: source rows for `maplify.sightings` (3 rows: trusted+tagged, untrusted, trusted+untagged) + `public.observations` (1 native row) + `public.observation_photos` (1 photo). References migration-seeded provider/org/collection IDs; does NOT re-insert reference rows. |
| `.github/workflows/build.yml` (edit) | 14-02 | modified file | Adds one named step `Apply CI seed fixture` (`supabase db query --local --file supabase/ci-seed.sql`) after `supabase db start`, and a step-scoped `SUPABASE_DB_URL` env on the existing root `npm test` step. No new job/workflow. |
| `14-01-SUMMARY.md` | 14-01 | new file | Records contributor path used, observed row counts, and the clean `supabase db reset` (A3) result. |
| `14-02-SUMMARY.md` | 14-02 | new file | Records the build.yml diff, the no-DSN skipped-suite line, and the red-test result. |
| Minted requirement IDs DWCA-GATE-01..07 | 14-01/14-02 | doc IDs | Back-fill into ROADMAP.md `### Phase 14` Requirements (currently "TBD") and REQUIREMENTS.md traceability table. |

**Explicitly NOT produced / NOT changed (locked out of scope):**
- `supabase/seed.sql` — unchanged (D-05; keeps its live-API fetches for local dev).
- `scripts/dwca/build.ts`, `build.test.ts`, `fields.ts` — unchanged (the suite is
  *activated*, not authored; the only build.ts touch is the throwaway red-test
  mutation in 14-02 Task 3, which is restored and never committed).
- `scripts/dwca/guard.ts` / `ROW_FLOOR` — unchanged (D-03/D-04; nightly-only).
- `.github/workflows/dwca-nightly.yml` — unchanged (the gate front-runs it).
- No new npm packages (RESEARCH §Standard Stack: zero new dependencies).
- No permanent negative/regression test (D-deferred; the SC#4 proof is a manual
  one-time red-test).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static SQL fixture → local CI Postgres | Fixture is committed, static, reviewed; runs only against the ephemeral local stack (port 54322), never prod. No untrusted input crosses at runtime. |
| Local Supabase stack → developer/CI runner | Local DSN `postgresql://postgres:postgres@127.0.0.1:54322/postgres` is the well-known Supabase local-dev credential (per RESEARCH §SUPABASE_DB_URL / Security Domain), NOT a secret. It does NOT appear in this fixture file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | `supabase/ci-seed.sql` | mitigate | Fixture contains only synthetic data (fixed UUID, "Jane Smith", "CI Gate Test", example.com URLs). Acceptance criteria explicitly assert no real PII and no production credentials. |
| T-14-02 | Information Disclosure | local DSN | accept | The local DSN is the documented Supabase local-dev default, not sensitive; it is deliberately NOT placed in the fixture (it belongs in build.yml, Plan 02). No masking required. |
| T-14-03 | Tampering | reference rows (providers/orgs/collections) | mitigate | Fixture references existing migration-seeded IDs and must NOT re-insert reference rows (D-06); acceptance criteria assert absence of provider/org/collection INSERTs, preventing accidental reference-table drift. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (configuration + static SQL only; RESEARCH §Standard Stack confirms zero new packages). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `supabase db reset` applies the full migration chain from scratch and exits 0
  (DWCA-GATE-06 / RESEARCH A3 risk retired).
- `supabase db query --local --file supabase/ci-seed.sql` exits 0 against the
  freshly-reset DB.
- dwc.occurrences >= 3 rows; dwc.multimedia >= 1 row; untrusted maplify:2 excluded;
  recordedBy for maplify:1 = "Jane Smith" (single parenthetical name extracted);
  Step 15.5 returns Orca Network.
- `SUPABASE_DB_URL=... npm test -- run` runs the un-skipped build.test.ts suite green.
- `supabase/seed.sql` is byte-for-byte unchanged (D-05).
</verification>

<success_criteria>
- `supabase/ci-seed.sql` exists, is static (no network calls), references existing
  reference IDs, and inserts only source rows (D-05, D-06, D-07).
- The fixture makes dwc.occurrences and dwc.multimedia non-empty and exercises
  trusted/untrusted, tagged/untagged, with/without collection_id branches (D-07).
- `supabase db reset` applies the full migration chain cleanly (A3 retired).
- The seeded full test suite passes with build.test.ts activated.
</success_criteria>

<output>
Create `.planning/phases/14-dwc-a-build-pre-prod-gate-seeded-local-db/14-01-SUMMARY.md` when done.
Record: which contributor path was used (auth.users trigger vs direct insert),
the observed dwc.occurrences / dwc.multimedia row counts, and confirmation that
`supabase db reset` applied the full chain cleanly (A3 result).
</output>
