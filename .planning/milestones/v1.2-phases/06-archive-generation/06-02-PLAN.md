---
phase: 06-archive-generation
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - scripts/dwca/fields.ts
  - scripts/dwca/fields.test.ts
autonomous: true
requirements:
  - DWCA-02
user_setup: []

must_haves:
  truths:
    - "`OCCURRENCE_FIELDS` is a frozen ordered list of exactly 25 entries; the column order matches the 25 columns emitted by `dwc.occurrences` in `supabase/migrations/20260617203900_dwc_schema.sql` (the UNION ALL view inherits column order from `dwc._native_occurrences`)"
    - "`MULTIMEDIA_FIELDS` is a frozen ordered list of exactly 6 entries; the column order matches `dwc.multimedia` in the same migration (coreId, type, identifier, license, rightsHolder, creator)"
    - "Position 19 (rightsHolder) and position 22 (license) of `OCCURRENCE_FIELDS` carry `purl.org/dc/terms/` URIs (Dublin Core); the remaining 23 entries carry `rs.tdwg.org/dwc/terms/` URIs (Darwin Core) — per F-03"
    - "Multimedia entry 0 (`coreId`) carries the GBIF extension URI `http://rs.gbif.org/terms/1.0/coreid`; entries 1..5 (`type`, `identifier`, `license`, `rightsHolder`, `creator`) carry `purl.org/dc/terms/` URIs"
    - "No duplicate `name` values exist within either array"
    - "`OCCURRENCE_FIELDS` and `MULTIMEDIA_FIELDS` are typed `as const satisfies readonly OccurrenceField[]` / `readonly MultimediaField[]` so every entry is a `readonly` tuple-shape; downstream code receives narrowed name literal types"
  artifacts:
    - path: "scripts/dwca/fields.ts"
      provides: "Canonical ordered field arrays — single source of truth for COPY column list, meta.xml `<field>` ordinals, and information_schema runtime assertion"
      exports: ["OCCURRENCE_FIELDS", "MULTIMEDIA_FIELDS", "OccurrenceField", "MultimediaField"]
    - path: "scripts/dwca/fields.test.ts"
      provides: "Pure unit tests enforcing array length, dcterms URI placement, no duplicates, and known positional sentinels (index 0 = occurrenceID; index 19 = rightsHolder; index 22 = license)"
  key_links:
    - from: "scripts/dwca/fields.ts OCCURRENCE_FIELDS"
      to: "supabase/migrations/20260617203900_dwc_schema.sql dwc._native_occurrences"
      via: "column order alignment"
      pattern: "column ordinal must match view ordinal"
    - from: "scripts/dwca/fields.ts MULTIMEDIA_FIELDS"
      to: "supabase/migrations/20260617203900_dwc_schema.sql dwc.multimedia"
      via: "column order alignment"
      pattern: "column ordinal must match view ordinal"
---

<objective>
Populate the canonical ordered field arrays in `scripts/dwca/fields.ts` with the 25 occurrence entries and 6 multimedia entries — each entry carrying its DwC term URI explicitly (with the two Dublin Core exceptions at occurrence positions 19 and 22, and the five DC + one GBIF coreid URIs across the multimedia array). Unskip and complete the pure unit tests in `scripts/dwca/fields.test.ts` so DWCA-02 is enforced at build-time before any downstream module consumes the arrays.

Purpose: F-01 / F-03 single source of truth. Every downstream module (`meta-xml.ts`, `assertions.ts`, the `build.ts` COPY) reads from these arrays; any drift from the Phase 5 view contract becomes a Vitest failure here AND a runtime assertion failure in Plan 03's `assertions.ts`. DWCA-02 = descriptor / data-file column-order parity is enforced before any executor reaches Plan 03+.

Output: Two readonly tuples — `OCCURRENCE_FIELDS` (25 entries) and `MULTIMEDIA_FIELDS` (6 entries) — plus the corresponding tests, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-archive-generation/06-CONTEXT.md
@.planning/phases/06-archive-generation/06-RESEARCH.md
@.planning/phases/06-archive-generation/06-01-SUMMARY.md
@supabase/migrations/20260617203900_dwc_schema.sql
@scripts/dwca/fields.ts
@scripts/dwca/fields.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate OCCURRENCE_FIELDS (25 entries) and MULTIMEDIA_FIELDS (6 entries)</name>
  <files>scripts/dwca/fields.ts</files>
  <read_first>
    - supabase/migrations/20260617203900_dwc_schema.sql — `CREATE VIEW dwc._native_occurrences` (lines 219..305) for the canonical 25-column order with exact quoted identifiers; `CREATE VIEW dwc.multimedia` (lines 663..697) for the 6-column order
    - 06-RESEARCH.md §T4 "Term URIs for the 25 occurrence columns" table — the authoritative URI per ordinal; in particular ordinals 19 (rightsHolder) and 22 (license) carry `http://purl.org/dc/terms/...` URIs while the other 23 carry `http://rs.tdwg.org/dwc/terms/...` URIs
    - 06-RESEARCH.md §T4 "Multimedia term URIs" table — coreId uses `http://rs.gbif.org/terms/1.0/coreid` (NOT dwc/terms); the other 5 (type, identifier, license, rightsHolder, creator) use `http://purl.org/dc/terms/...`
    - 06-CONTEXT.md F-01 (entry shape `{ name: string; termUri: string; }`), F-03 (per-field URI is data, not derived — no `name → URI` convention function; carry both literally)
    - scripts/dwca/fields.ts (current placeholder — preserve the file-level JSDoc; expand the arrays only)
  </read_first>
  <action>
    Populate `OCCURRENCE_FIELDS` with exactly 25 entries in this order, each `{ name: '<column>', termUri: '<uri>' } as const`:

    0  occurrenceID → http://rs.tdwg.org/dwc/terms/occurrenceID
    1  basisOfRecord → http://rs.tdwg.org/dwc/terms/basisOfRecord
    2  eventDate → http://rs.tdwg.org/dwc/terms/eventDate
    3  scientificName → http://rs.tdwg.org/dwc/terms/scientificName
    4  taxonRank → http://rs.tdwg.org/dwc/terms/taxonRank
    5  kingdom → http://rs.tdwg.org/dwc/terms/kingdom
    6  phylum → http://rs.tdwg.org/dwc/terms/phylum
    7  class → http://rs.tdwg.org/dwc/terms/class
    8  order → http://rs.tdwg.org/dwc/terms/order
    9  family → http://rs.tdwg.org/dwc/terms/family
    10 genus → http://rs.tdwg.org/dwc/terms/genus
    11 decimalLatitude → http://rs.tdwg.org/dwc/terms/decimalLatitude
    12 decimalLongitude → http://rs.tdwg.org/dwc/terms/decimalLongitude
    13 geodeticDatum → http://rs.tdwg.org/dwc/terms/geodeticDatum
    14 coordinateUncertaintyInMeters → http://rs.tdwg.org/dwc/terms/coordinateUncertaintyInMeters
    15 individualCount → http://rs.tdwg.org/dwc/terms/individualCount
    16 occurrenceStatus → http://rs.tdwg.org/dwc/terms/occurrenceStatus
    17 occurrenceRemarks → http://rs.tdwg.org/dwc/terms/occurrenceRemarks
    18 recordedBy → http://rs.tdwg.org/dwc/terms/recordedBy
    19 rightsHolder → http://purl.org/dc/terms/rightsHolder            **[dcterms]**
    20 datasetName → http://rs.tdwg.org/dwc/terms/datasetName
    21 datasetID → http://rs.tdwg.org/dwc/terms/datasetID
    22 license → http://purl.org/dc/terms/license                       **[dcterms]**
    23 dynamicProperties → http://rs.tdwg.org/dwc/terms/dynamicProperties
    24 informationWithheld → http://rs.tdwg.org/dwc/terms/informationWithheld

    Populate `MULTIMEDIA_FIELDS` with exactly 6 entries:

    0  coreId → http://rs.gbif.org/terms/1.0/coreid                     **[gbif extension]**
    1  type → http://purl.org/dc/terms/type
    2  identifier → http://purl.org/dc/terms/identifier
    3  license → http://purl.org/dc/terms/license
    4  rightsHolder → http://purl.org/dc/terms/rightsHolder
    5  creator → http://purl.org/dc/terms/creator

    Each array remains `as const satisfies readonly OccurrenceField[]` / `readonly MultimediaField[]` so name strings keep literal types.

    Cross-validate every `name` against the SQL migration's quoted identifier (e.g., the migration uses `"occurrenceID"`, `"basisOfRecord"`, `"order"` (the reserved word is intentionally re-quoted as `"order"` in the SELECT alias even though the helper view exposes it as `order_`)). Match the migration's CASE for each name verbatim — case-sensitive.

    Add an inline comment above each dcterms / GBIF entry stating the namespace divergence so a future reader doesn't "fix" them. Example: `// dcterms — NOT dwc/terms; per F-03 the URI is carried literally per entry`.

    Do NOT add any helper function that derives a URI from a name (F-03 forbids it).
  </action>
  <verify>
    <automated>
      npx tsc -p . --noEmit && node --input-type=module -e "import { OCCURRENCE_FIELDS, MULTIMEDIA_FIELDS } from './scripts/dwca/fields.ts'; if(OCCURRENCE_FIELDS.length!==25)throw new Error('expected 25 OCCURRENCE_FIELDS, got '+OCCURRENCE_FIELDS.length); if(MULTIMEDIA_FIELDS.length!==6)throw new Error('expected 6 MULTIMEDIA_FIELDS, got '+MULTIMEDIA_FIELDS.length); if(OCCURRENCE_FIELDS[0].name!=='occurrenceID')throw new Error('idx 0 wrong'); if(OCCURRENCE_FIELDS[19].name!=='rightsHolder'||!OCCURRENCE_FIELDS[19].termUri.startsWith('http://purl.org/dc/terms/'))throw new Error('idx 19 dcterms wrong'); if(OCCURRENCE_FIELDS[22].name!=='license'||!OCCURRENCE_FIELDS[22].termUri.startsWith('http://purl.org/dc/terms/'))throw new Error('idx 22 dcterms wrong'); if(MULTIMEDIA_FIELDS[0].termUri!=='http://rs.gbif.org/terms/1.0/coreid')throw new Error('multimedia coreid uri wrong');" 2>&1
    </automated>
  </verify>
  <done>
    `OCCURRENCE_FIELDS.length === 25` with the documented name/URI mapping and dcterms exceptions at indices 19 and 22; `MULTIMEDIA_FIELDS.length === 6` with `coreId` at index 0 using the GBIF coreid extension URI; `tsc --noEmit` exits 0.
  </done>
  <acceptance_criteria>
    - `OCCURRENCE_FIELDS.length === 25`.
    - `OCCURRENCE_FIELDS[0]` = `{ name: 'occurrenceID', termUri: 'http://rs.tdwg.org/dwc/terms/occurrenceID' }`.
    - `OCCURRENCE_FIELDS[19].name === 'rightsHolder'` AND `OCCURRENCE_FIELDS[19].termUri === 'http://purl.org/dc/terms/rightsHolder'`.
    - `OCCURRENCE_FIELDS[22].name === 'license'` AND `OCCURRENCE_FIELDS[22].termUri === 'http://purl.org/dc/terms/license'`.
    - For every index `i ∈ {0..24} \ {19, 22}`: `OCCURRENCE_FIELDS[i].termUri.startsWith('http://rs.tdwg.org/dwc/terms/')`.
    - `MULTIMEDIA_FIELDS.length === 6`.
    - `MULTIMEDIA_FIELDS[0]` = `{ name: 'coreId', termUri: 'http://rs.gbif.org/terms/1.0/coreid' }`.
    - For every index `i ∈ {1..5}`: `MULTIMEDIA_FIELDS[i].termUri.startsWith('http://purl.org/dc/terms/')`.
    - No two entries in either array share a `name`.
    - For every entry in OCCURRENCE_FIELDS, the `name` appears as a quoted-identifier alias in `supabase/migrations/20260617203900_dwc_schema.sql` within the `dwc._native_occurrences` view body (grep-verifiable). Likewise every MULTIMEDIA_FIELDS entry name appears as a quoted-identifier alias in the `dwc.multimedia` view body.
    - `npx tsc -p . --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Unskip and complete fields.test.ts assertions</name>
  <files>scripts/dwca/fields.test.ts</files>
  <read_first>
    - 06-RESEARCH.md Validation Architecture "DWCA-02 unit surface" table — the six required checks: length 25, length 6, index-0 sentinel, dcterms URI for index 19 and 22, no duplicate names
    - scripts/dwca/fields.test.ts (current scaffold with skipped tests)
    - scripts/dwca/fields.ts (populated by Task 1) — for the actual entries to assert against
    - 06-CONTEXT.md F-03 (per-field URI is data — tests assert the dcterms exceptions explicitly so the F-03 invariant is encoded in CI)
  </read_first>
  <action>
    Convert every `test.skip(...)` in `scripts/dwca/fields.test.ts` to a real `test(...)`. Required assertions:

    Occurrence array shape:
    - `OCCURRENCE_FIELDS.length === 25`.
    - `OCCURRENCE_FIELDS[0].name === 'occurrenceID'`.
    - `OCCURRENCE_FIELDS[0].termUri === 'http://rs.tdwg.org/dwc/terms/occurrenceID'`.
    - `OCCURRENCE_FIELDS[19].name === 'rightsHolder' && OCCURRENCE_FIELDS[19].termUri === 'http://purl.org/dc/terms/rightsHolder'`.
    - `OCCURRENCE_FIELDS[22].name === 'license' && OCCURRENCE_FIELDS[22].termUri === 'http://purl.org/dc/terms/license'`.
    - For every index `i` other than 19 and 22: `OCCURRENCE_FIELDS[i].termUri.startsWith('http://rs.tdwg.org/dwc/terms/')` — table-driven test iterating the array.
    - `new Set(OCCURRENCE_FIELDS.map(f => f.name)).size === OCCURRENCE_FIELDS.length` (no duplicates).

    Multimedia array shape:
    - `MULTIMEDIA_FIELDS.length === 6`.
    - `MULTIMEDIA_FIELDS[0].name === 'coreId' && MULTIMEDIA_FIELDS[0].termUri === 'http://rs.gbif.org/terms/1.0/coreid'`.
    - For every index `i ∈ {1..5}`: `MULTIMEDIA_FIELDS[i].termUri.startsWith('http://purl.org/dc/terms/')`.
    - `new Set(MULTIMEDIA_FIELDS.map(f => f.name)).size === MULTIMEDIA_FIELDS.length`.

    Forward-compat ordering invariant:
    - One additional test asserts that the multimedia name set is exactly `['coreId','type','identifier','license','rightsHolder','creator']` in that order (table comparison via `toEqual`). This prevents an editor from rearranging the array on autoformat.
    - Likewise one additional test asserts the occurrence name set in order matches the 25-name list from the action above (assert via `toEqual` on `OCCURRENCE_FIELDS.map(f => f.name)`). This is the primary DWCA-02 unit-level guardrail.

    Use the project's existing Vitest patterns (`describe` + `test`) per .planning/codebase/TESTING.md. Keep the placeholder smoke test from Plan 01 — it remains valid post-populate.

    Do NOT add any test that requires a database connection — those live in Plan 06's `build.test.ts`.
  </action>
  <verify>
    <automated>
      npx vitest run scripts/dwca/fields.test.ts 2>&1 | tee /tmp/p6-02-vitest.log; node -e "const log=require('fs').readFileSync('/tmp/p6-02-vitest.log','utf8'); const failed=/(\\d+) failed/.exec(log); if(failed)throw new Error('failures: '+failed[0]); const skipped=/(\\d+) skipped/.exec(log); if(skipped)throw new Error('skipped tests still present: '+skipped[0]); const passed=/(\\d+) passed/.exec(log); if(!passed||Number(passed[1])<10)throw new Error('expected at least 10 passing tests, got: '+(passed?passed[1]:'none'));"
    </automated>
  </verify>
  <done>
    `npx vitest run scripts/dwca/fields.test.ts` exits 0 with at least 10 passed tests and zero skipped tests.
  </done>
  <acceptance_criteria>
    - `npx vitest run scripts/dwca/fields.test.ts` exits 0.
    - Test output reports ≥10 passing tests and 0 skipped tests (grep on Vitest reporter output).
    - The two ordering tests (one per array) compare via `toEqual` against the literal expected name list — any reordering of `fields.ts` will fail the test.
    - No test imports anything other than `vitest` and `./fields.ts`.
    - The `noUnusedLocals` / `noUnusedParameters` strict checks pass (no leftover commented-out imports from Plan 01 scaffold).
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
This phase produces (Plan 02 contribution):
- `scripts/dwca/fields.ts` populated: `OCCURRENCE_FIELDS` (25 entries, F-03 dcterms pair at indices 19 and 22), `MULTIMEDIA_FIELDS` (6 entries, GBIF coreid URI at index 0)
- `scripts/dwca/fields.test.ts` with all DWCA-02 unit assertions green (≥10 passing tests, 0 skipped)
- `OccurrenceField` and `MultimediaField` types unchanged from Plan 01 scaffold but now exported with populated data
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `fields.ts` array values → downstream consumers | A bad URI here is silently emitted to every meta.xml, breaking DWCA-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-DRIFT | Tampering | F-01 array — accidental rename or reorder by future editor | mitigate | Plan 02 tests assert ordered name list via `toEqual`; any reorder fails CI. Plan 03 (assertions.ts) adds the second guardrail: runtime DESCRIBE against the live Postgres view. Defense in depth. |
| T-06-02-URI | Tampering | dcterms / GBIF URI typo at index 19, 22, or multimedia 0 | mitigate | Plan 02 tests assert the literal URI string at the three sentinel indices. The `.startsWith` test catches accidental drift to dwc/terms base on the dcterms pair. |
| T-06-02-XML | Injection | URI strings flow unescaped into meta.xml | accept (constrained input) | URIs come from constant TS source code (not user input); XML escaping in Plan 03's `meta-xml.ts` is still applied for defense in depth. The URIs themselves contain no `&`, `<`, `>` or quote characters. |
</threat_model>

<verification>
- `npx tsc -p . --noEmit` exits 0.
- `npx vitest run scripts/dwca/fields.test.ts` exits 0 with ≥10 passing tests, 0 skipped.
- `grep -c '"occurrenceID"' supabase/migrations/20260617203900_dwc_schema.sql` returns ≥1 (proves the cross-reference name actually exists in the migration source of truth).
</verification>

<success_criteria>
DWCA-02's first guardrail (TS-side ordered descriptor) is in place. Plan 03 can consume `OCCURRENCE_FIELDS` / `MULTIMEDIA_FIELDS` directly to generate `meta.xml` and the COPY column list with confidence that the arrays match the Phase 5 view contract — and that any drift in either direction will surface as a test failure here or a runtime assertion failure in Plan 03's `assertions.ts`.
</success_criteria>

<output>
Create `.planning/phases/06-archive-generation/06-02-SUMMARY.md` when done — record the final 25-entry and 6-entry tables with their URIs, and any anomaly noted while cross-validating against the migration file.
</output>
