---
phase: 05-db-projection-dwc-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260617203900_dwc_schema.sql
autonomous: true
requirements:
  - ALIGN-03
must_haves:
  truths:
    - "Querying `dwc.taxa_classification` returns exactly one row per `inaturalist.taxa.id`."
    - "For a taxon whose own rank is `family` or higher, `dwc.taxa_classification.scientific_name` carries the taxon's own scientific_name and `genus` is NULL — no fabricated binomial (M-05)."
    - "For a taxon whose own rank is `genus` or below, `dwc.taxa_classification.genus` is populated by walking ancestors."
    - "`anon` and `authenticated` roles hold `USAGE` on schema `dwc` (Pitfall 5)."
  artifacts:
    - path: supabase/migrations/20260617203900_dwc_schema.sql
      provides: "Initial migration file containing CREATE SCHEMA dwc, GRANT USAGE, and CREATE VIEW dwc.taxa_classification (recursive CTE)."
      contains: "CREATE SCHEMA dwc"
  key_links:
    - from: dwc.taxa_classification
      to: inaturalist.taxa
      via: "WITH RECURSIVE walking parent_id"
      pattern: "WITH RECURSIVE"
---

<objective>
Create the foundation of the Phase 5 migration file: the `dwc` schema, its USAGE grants, and the helper view `dwc.taxa_classification` that walks `inaturalist.taxa.parent_id` recursively per M-05. This is the leaf dependency for the branch views in plans 05-02 and 05-03.

Purpose: One inspectable place to centralize the Linnaean-rank pivot — both branch views JOIN this helper on `taxon_id`, so the higher-rank-only contract (ALIGN-03) has a single test surface.

Output: A new migration file at `supabase/migrations/20260617203900_dwc_schema.sql` containing (so far) the schema, grants preamble, and `dwc.taxa_classification` view. Subsequent plans append to this same file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-db-projection-dwc-schema/05-CONTEXT.md
@.planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md
@.planning/phases/05-db-projection-dwc-schema/05-VALIDATION.md
@.planning/phases/04-rights-data-model-policy-gate/04-POLICY.md
@supabase/migrations/20250903172708_initial_schema.sql
@supabase/migrations/20260610001507_occurrences_observed_at_indexes.sql
@supabase/config.toml
@.claude/skills/supabase-postgres-best-practices/SKILL.md
</context>

<critical_notes>
**Discrepancy 1 — Table renames (RESEARCH §"CRITICAL Schema Discrepancies"):** CONTEXT.md and POLICY §3.1/§3.3 refer to `public.sightings` / `public.sighting_photos` / `sighting_id`. The on-disk schema uses `public.observations` / `public.observation_photos` / `observation_id` since the 2025-09-15 rename migration. All Phase 5 SQL — including any cross-references in COMMENTs — must use the new identifiers. This plan does not reference those tables yet, but a comment in the file header explicitly documents the rename so reviewers do not flag the later plans as wrong.

**Discrepancy 2 — D-19 NULL branch unreachable (RESEARCH §"CRITICAL Schema Discrepancies"):** Plan 05-04's `dwc.multimedia` will encode a NULL CASE arm that is currently unreachable on `public.observation_photos.license_code` (still `varchar(20) NOT NULL`). Header comment in this file flags it so the executor knows the upcoming NULL arm is intentional forward-compat, not a misunderstanding.

**`dwc` is NOT a PostgREST API surface.** Do not modify `supabase/config.toml`. Do not add `dwc` to `api.schemas` or `extra_search_path` (Pitfall 5).
</critical_notes>

<tasks>

<task type="auto">
  <name>Task 1: Author migration header, CREATE SCHEMA, and GRANT USAGE</name>
  <files>supabase/migrations/20260617203900_dwc_schema.sql</files>
  <read_first>
    - supabase/migrations/20260610001507_occurrences_observed_at_indexes.sql — the most recent migration; mirror its file-comment-then-DDL style.
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"Recommended File Layout" (sequence 1–10) and §"Schema Push Command for Local Dev" → "Considerations for adding a new schema" (RLS, grants, search_path, api.schemas).
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"CRITICAL Schema Discrepancies" — both Discrepancy 1 and 2.
    - .planning/phases/04-rights-data-model-policy-gate/04-POLICY.md §6.7 (Phase 5 ownership split).
    - supabase/config.toml (confirm `dwc` is not in `api.schemas` or `extra_search_path` — do NOT modify).
  </read_first>
  <action>
    Create the file `supabase/migrations/20260617203900_dwc_schema.sql`.

    Begin with a multi-line `--` header comment that records:
    (1) the phase title "Phase 5 — DwC projection schema (encodes 04-POLICY §3.1 and §3.2)";
    (2) the artifact inventory the full migration will produce by the time all four plans finish — schema `dwc`, view `dwc.taxa_classification`, view `dwc._native_occurrences`, view `dwc._maplify_occurrences`, view `dwc.occurrences` (UNION ALL), view `dwc.multimedia`, view `dwc.datasets`;
    (3) **Discrepancy 1 callout** — quote: "Source tables are `public.observations` and `public.observation_photos` (renamed from `public.sightings` / `public.sighting_photos` on 2025-09-15, migration `20250915171505_sighting_policies.sql`). CONTEXT.md and 04-POLICY.md §3.1/§3.3 still use the old names; this migration uses the current names.";
    (4) **Discrepancy 2 callout** — quote: "POLICY §1.2/§1.4 specify a two-branch CASE for `license_code = 'none'` vs `license_code IS NULL`. The `DROP NOT NULL` migration was applied to `inaturalist.observation_photos.license`, NOT to `public.observation_photos.license_code` (which remains `varchar(20) NOT NULL`). The NULL arm in `dwc.multimedia` is encoded for forward-compat with a future `DROP NOT NULL`; today it matches zero rows.";
    (5) the verification harness path: "Local verification: `supabase db reset && psql … -f supabase/snippets/05_dwc_assertions.sql`."

    Then emit `CREATE SCHEMA dwc;` followed by `GRANT USAGE ON SCHEMA dwc TO anon, authenticated;` (RESEARCH Pitfall 5). The service-role inherits USAGE via Supabase defaults — do not add it explicitly.

    Do NOT add `dwc` to `extra_search_path` or `api.schemas` in `supabase/config.toml`; consumers must fully-qualify (`dwc.occurrences`) per RESEARCH §"Schema Push Command" / "Note on Supabase API exposure". This task does not modify `config.toml`.

    Leave a single `-- (continued: dwc.taxa_classification, etc.)` placeholder comment so subsequent plans know where to append.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q '^CREATE SCHEMA dwc;' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q 'GRANT USAGE ON SCHEMA dwc TO anon, authenticated' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q -i 'Discrepancy 1' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q -i 'Discrepancy 2' supabase/migrations/20260617203900_dwc_schema.sql</automated>
  </verify>
  <done>
    Migration file exists with header comment that names both discrepancies. `CREATE SCHEMA dwc;` and `GRANT USAGE ON SCHEMA dwc TO anon, authenticated;` are present. No `ALTER` to `supabase/config.toml`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append CREATE VIEW dwc.taxa_classification (recursive CTE + Linnaean pivot)</name>
  <files>supabase/migrations/20260617203900_dwc_schema.sql</files>
  <read_first>
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"Higher-Rank-Only Recursive Walk" (the full production-ready skeleton, lines defining `ancestors` CTE, `pivoted` CTE, and the outer SELECT with explicit-rank-list CASE).
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"`inaturalist.rank` → DwC `taxonRank` Vocabulary Mapping" (recommendation: cast enum to text directly; no remapping CASE).
    - .planning/phases/05-db-projection-dwc-schema/05-CONTEXT.md §"Taxonomy hierarchy walk (M-05)" — the higher-rank-only contract.
    - .planning/phases/05-db-projection-dwc-schema/05-RESEARCH.md §"Assumptions Log" A2 (cycle-safety of recursive walk).
    - supabase/migrations/20250903172708_initial_schema.sql — `inaturalist.taxa` columns (`id INTEGER`, `parent_id INTEGER REFERENCES inaturalist.taxa(id) DEFERRABLE INITIALLY DEFERRED`, `scientific_name varchar`, `rank inaturalist.rank`) and the `inaturalist.rank` enum declaration.
  </read_first>
  <action>
    Append to `supabase/migrations/20260617203900_dwc_schema.sql` a single statement: `CREATE VIEW dwc.taxa_classification AS …` matching the skeleton in RESEARCH §"Higher-Rank-Only Recursive Walk".

    Concrete requirements:
    - Use `WITH RECURSIVE ancestors AS (…) , pivoted AS (…) SELECT … FROM inaturalist.taxa t JOIN pivoted p ON p.taxon_id = t.id;`. Fully qualify `inaturalist.taxa` in every reference (Pitfall: search_path hijack — never bare `taxa`).
    - The seed selects every row of `inaturalist.taxa` with `depth = 0` and the leaf carrying its own `id` as both `leaf_id` and `ancestor_id`.
    - The recursive step joins `inaturalist.taxa p ON p.id = a.parent_id` and increments depth. Per RESEARCH Assumption A2 (defense in depth against accidental cycles), include `WHERE a.depth &lt; 50` on the recursive arm so any cycle bottoms out instead of looping forever.
    - The `pivoted` CTE emits one row per `leaf_id` with `MAX(CASE WHEN rank = 'kingdom'::inaturalist.rank THEN scientific_name END) AS kingdom`, repeated for `phylum`, `class`, `order_` (trailing underscore — `order` is a SQL reserved word), `family`, `genus`. The trailing underscore on `order_` is deliberate; the branch views alias it to the quoted DwC term `"order"`.
    - Output columns from the outer SELECT in order: `taxon_id` (= `t.id`), `taxon_rank` (= `t.rank::text` — direct enum cast per RESEARCH "the values match by construction"), `scientific_name` (= `t.scientific_name`, the leaf's own name — never fabricated), `kingdom`, `phylum`, `class`, `order_`, `family`, `genus`. The `genus` column uses the explicit `CASE WHEN t.rank IN ('genus','genushybrid','subgenus','species','complex','section','subsection','hybrid','subspecies','variety','form','infrahybrid') THEN p.genus ELSE NULL END` per RESEARCH §"Why the genus CASE is enumerated explicitly" — this enforces the M-05 higher-rank-only contract.
    - End with `COMMENT ON VIEW dwc.taxa_classification IS 'M-05 helper. Recursive walk over inaturalist.taxa.parent_id; one row per taxon. Higher-rank taxa have scientific_name = own name, genus = NULL (no fabricated binomial). See 05-RESEARCH §"Higher-Rank-Only Recursive Walk".';`

    Do NOT add `GRANT SELECT` on this view yet — the final grant statement `GRANT SELECT ON ALL TABLES IN SCHEMA dwc TO anon, authenticated;` lives at the end of the migration (plan 05-04) so it covers every view at once.
  </action>
  <verify>
    <automated>grep -q 'CREATE VIEW dwc.taxa_classification' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q 'WITH RECURSIVE' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q 'inaturalist.taxa' supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "depth &lt; 50" supabase/migrations/20260617203900_dwc_schema.sql &amp;&amp; grep -q "COMMENT ON VIEW dwc.taxa_classification" supabase/migrations/20260617203900_dwc_schema.sql</automated>
  </verify>
  <done>
    The view exists in the migration with: a recursive CTE that walks `inaturalist.taxa.parent_id` with a `depth &lt; 50` cycle-guard; a pivoted CTE producing one row per leaf with Linnaean-rank columns including `order_` (not `order`); an outer SELECT emitting `taxon_id`, `taxon_rank` (text-cast of the enum), `scientific_name` (always the leaf's own), `kingdom`, `phylum`, `class`, `order_`, `family`, and `genus` gated by the explicit rank-list CASE. A `COMMENT ON VIEW` documents the M-05 contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST (anon/authenticated) → DB | Anonymous SQL access reads only schemas in `api.schemas`. `dwc` is intentionally excluded from `api.schemas` and from `extra_search_path` (Pitfall 5). The risk surface for this plan is therefore the local DB only. |
| Postgres role → search_path | A non-superuser role creating a table named `taxa` in a higher-priority schema could shadow `inaturalist.taxa` for a recursive CTE referenced as bare `taxa`. Mitigation: fully-qualify every reference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | `dwc.*` views inheriting RLS through view-creator | mitigate | `dwc` is NOT in `supabase/config.toml:api.schemas`; PostgREST cannot expose it. Local-DB only surface. Verify via `grep` that no later plan modifies `config.toml`. |
| T-05-04 | Tampering | Recursive CTE in `dwc.taxa_classification` resolving `taxa` from wrong schema | mitigate | Every reference fully-qualified as `inaturalist.taxa`. Verification grep confirms no bare `JOIN taxa` or `FROM taxa`. |
</threat_model>

<verification>
Plan-level checks (run after both tasks committed):
- `grep -c 'CREATE SCHEMA dwc' supabase/migrations/20260617203900_dwc_schema.sql` returns exactly 1.
- `grep -c 'CREATE VIEW dwc.taxa_classification' supabase/migrations/20260617203900_dwc_schema.sql` returns exactly 1.
- `grep -v '^--' supabase/migrations/20260617203900_dwc_schema.sql | grep -c -E ' (FROM|JOIN) taxa[[:space:]]'` returns 0 (no bare `taxa` reference, only `inaturalist.taxa`).
- `git diff supabase/config.toml` is empty (no API-schema or search-path change).
- The schema-push and assertion suite live in plan 05-04; this plan stops at file authorship.
</verification>

<success_criteria>
- File `supabase/migrations/20260617203900_dwc_schema.sql` exists.
- Header comment names Discrepancy 1 (table rename) and Discrepancy 2 (NULL branch unreachable).
- `CREATE SCHEMA dwc;` + `GRANT USAGE ON SCHEMA dwc TO anon, authenticated;` are present.
- `CREATE VIEW dwc.taxa_classification AS WITH RECURSIVE …` is present, with `depth &lt; 50` cycle guard and explicit-rank-list genus CASE.
- No reference to bare `taxa` (only fully-qualified `inaturalist.taxa`).
- `supabase/config.toml` is untouched.
</success_criteria>

<output>
Create `.planning/phases/05-db-projection-dwc-schema/05-01-SUMMARY.md` when done. Capture: file path, the view's column list, the rank-list used in the genus CASE, and a one-line note that the file is the seed for plans 05-02..05-04 (which append to it).
</output>
