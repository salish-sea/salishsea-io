---
phase: 04-rights-data-model-policy-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-rights-data-model-policy-gate/04-POLICY.md
autonomous: true
requirements: [GAP-01, GAP-02, GAP-03, GAP-04]

must_haves:
  truths:
    - "A reader can find an explicit resolution OR a framed conferral question for every audited data/datatype gap — none is silently defaulted (success criterion 1)"
    - "A reader can find the occurrence-record license recorded as CC-BY-NC 4.0 as the exact resolvable URI https://creativecommons.org/licenses/by-nc/4.0/legalcode, plus the native contributor-consent stance (D-08) (success criterion 2)"
    - "A reader can find which fields carry recordedBy, rightsHolder, and dataset/record provenance for native (D-09) and Whale Alert + nested Orca Network / Cascadia sources (D-10/D-11) (success criterion 3)"
    - "A reader can find a recorded decision on Whale Alert / Maplify redistribution — the include-and-attribute holding rule (D-02), the hosted-but-unlinked hold (D-05/D-06), and a per-organization conferral question — so generation cannot proceed on an unresolved rights question (success criterion 4)"
  artifacts:
    - path: ".planning/phases/04-rights-data-model-policy-gate/04-POLICY.md"
      provides: "Single authoritative rights & data-model-gap policy that Phases 5/6/7/8 encode"
      contains: "https://creativecommons.org/licenses/by-nc/4.0/legalcode"
      min_lines: 150
  key_links:
    - from: ".planning/phases/04-rights-data-model-policy-gate/04-POLICY.md"
      to: "Phase 5 dwc schema (SQL encoding of D-01..D-03, D-09..D-14)"
      via: "named decision sections with stable anchors"
      pattern: "occurrenceStatus.*present"
    - from: ".planning/phases/04-rights-data-model-policy-gate/04-POLICY.md"
      to: "Phase 7/8 hosting + download link (hold rule D-05/D-06, open D-07 variant question)"
      via: "Redistribution Status section with holding rule and flagged open question"
      pattern: "hosted.but.unlinked"
---

<objective>
Author the single authoritative **Rights & Data-Model Policy** document for the v1.2
DarwinCore Archive milestone at
`.planning/phases/04-rights-data-model-policy-gate/04-POLICY.md`. The document resolves (or
explicitly frames for conferral) every rights/licensing, attribution, and data-model gap so the
downstream `dwc` schema (Phase 5), archive generator (Phase 6), nightly hosting (Phase 7), and
download link (Phase 8) have one place to encode and nothing left to silently fudge.

This phase **writes decisions, not code.** No SQL, no app code, no schema changes, no migrations.
The migration files and `database.types.ts` are READ-ONLY grounding references. Phases 5 and 6
will ENCODE this policy later — do not pre-empt their implementation.

Purpose: Close GAP-01..04 as explicit findings (success criterion 1: no gap silently defaulted)
and gate downstream generation on an actually-recorded rights stance rather than a ToS guess.

Output: One Markdown file, `04-POLICY.md`, with named/anchored decision sections referenced by
Phases 5-8. The document is the deliverable; the three tasks below author its sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase grounding — read before authoring
@.planning/phases/04-rights-data-model-policy-gate/04-CONTEXT.md
@.planning/phases/04-rights-data-model-policy-gate/04-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
This phase produces exactly one artifact and the named decision sections within it that
Phases 5-8 reference. No code, schema, or migration artifacts.

**File:** `.planning/phases/04-rights-data-model-policy-gate/04-POLICY.md`

**Named sections / anchors (downstream consumers cite these):**
- `## 1. License & Rights` — occurrence license URI, per-photo CC converter table, native
  consent basis (D-08), license-less photo exclusion. Encoded by Phase 5 (license columns,
  converter) and Phase 6 (serialized verbatim).
- `## 2. Attribution & Provenance Model` — recordedBy / rightsHolder / datasetName field
  mappings (D-09/D-10/D-11), the `dynamicProperties` key/value schema, unvalidated-identifier
  handling. Encoded by Phase 5 (SQL-computed columns + dynamicProperties JSON assembly).
- `## 3. Data-Model Gaps & Resolutions` — per-term gap table (eventDate precision,
  omit-unknown coordinateUncertainty, per-source basisOfRecord, occurrenceStatus, individualCount,
  D-14 no-op correction). Encoded by Phase 5 (`dwc.occurrences` columns/predicates).
- `## 4. Third-Party Redistribution Status` — per-organization conferral questions, the
  include-and-attribute holding rule (D-02), per-`maplify.source` drop granularity (D-03),
  hosted-but-unlinked hold (D-05), native-only public eligibility (D-06), and the flagged open
  D-07 native-only-variant question. Consumed by Phase 7 (publish-unlinked) and Phase 8
  (suppress third-party link; resolve native-only variant).
- `## 5. Scope Clarifications` — HappyWhale/iNaturalist exclusion; D-14 min-count no-op for v1.2.

Phases 5-8 MUST be able to encode each decision from this document without re-deciding anything.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author License & Rights and Attribution & Provenance sections (GAP-02, GAP-03)</name>
  <files>.planning/phases/04-rights-data-model-policy-gate/04-POLICY.md</files>
  <read_first>
    - .planning/phases/04-rights-data-model-policy-gate/04-RESEARCH.md (Creative Commons License URI Reference; DarwinCore Term Semantics Reference — Attribution & Rights Terms; dynamicProperties Key/Value Schema Proposal; Data-Model Gap Audit — observation_photos)
    - .planning/phases/04-rights-data-model-policy-gate/04-CONTEXT.md (D-08, D-09, D-10, D-11; "Already locked upstream" license block; Claude's Discretion on dynamicProperties)
    - .planning/REQUIREMENTS.md (GAP-02, GAP-03 text; Policy decisions block)
  </read_first>
  <action>
    Create `04-POLICY.md` with a top title `# Rights & Data-Model Policy`, a one-line statement
    that this document is authored by Phase 4 and encoded by Phases 5-8, and the section skeleton
    (`## 1.`..`## 5.`). Then author Sections 1 and 2.

    Section 1 (License & Rights) per GAP-02:
    - 1.1 Record the occurrence-record license as the exact resolvable URI string
      `https://creativecommons.org/licenses/by-nc/4.0/legalcode` (the `/legalcode` form GBIF's
      parser expects, NOT the `/4.0/` deed URL). State that this is a constant `license` column on
      every in-scope record and is already-decided upstream (operationalized, not re-decided).
    - 1.2 Per-photo license converter: a table mapping each `observation_photos.license_code` enum
      value (`cc0`, `cc-by`, `cc-by-nc`, `cc-by-sa`, `cc-by-nd`, `cc-by-nc-sa`, `cc-by-nc-nd`,
      `none`) to its canonical `/legalcode` CC URI, pinned at version 4.0 for all variants except
      `cc0` (CC0 1.0). Note assumption A1 (no version stored → 4.0 default) explicitly.
    - 1.3 Native contributor consent basis per D-08: platform-policy assertion for existing
      records PLUS a submission-form license/consent notice going forward; note the submission-form
      notice is in-scope for v1.2 but touches app runtime and is flagged for a roadmap home
      (do not plan it here).
    - 1.4 License-less photo exclusion (one shared converter, GAP-04 intersection): photos with
      `license_code = 'none'` OR NULL license are excluded from the Multimedia extension;
      Maplify `photo_url` has no stored license → excluded (cross-reference Section 3).

    Section 2 (Attribution & Provenance Model) per GAP-03:
    - 2.1 Native records (D-09): `rightsHolder` = individual contributor (`contributors.name`,
      per-record), `recordedBy` = same contributor display name; explicitly note contributor
      identity exposure as rightsHolder is accepted.
    - 2.2 Third-party records (D-10/D-11): `recordedBy` = `maplify.usernm` original observer where
      present; `datasetName` = human-readable sub-source name from `maplify.source` (mapping table
      built in Phase 5 against `SELECT DISTINCT source FROM maplify.sightings` — note A2/A4);
      `rightsHolder` = originating sub-source (Orca Network, Cascadia, …) when known, falling back
      to "Whale Alert / Maplify" when not.
    - 2.3 Propose the concrete `dynamicProperties` key/value schema (Claude's Discretion): a JSON
      string with keys `travelDirection`, `aggregatorSource`, `aggregatorChain`, `countIsMinimum`,
      `unvalidatedIdentifiers`. Define each key's value, when it is present vs omitted, and note
      `dynamicProperties` is passed through verbatim (not indexed by GBIF). The aggregator chain
      carries the Whale Alert → sub-source provenance as structured text.
    - 2.4 Unvalidated whale identifiers (e.g. `T065S` from `extract_identifiers(body)`): never
      emitted as `organismID`/`catalogNumber`; at most listed in `dynamicProperties.unvalidatedIdentifiers`.

    Cite the relevant D-numbers inline (D-08, D-09, D-10, D-11) and the GAP IDs (GAP-02, GAP-03)
    in the section text. Do NOT inline SQL or paste full implementation — name terms, columns,
    URI strings, and field mappings only.
  </action>
  <verify>
    <automated>MISSING — documentation phase, no runtime. Use acceptance_criteria source assertions below.</automated>
    <human-check>Read Sections 1-2 of 04-POLICY.md against GAP-02/GAP-03 and D-08..D-11.</human-check>
  </verify>
  <acceptance_criteria>
    - `04-POLICY.md` exists and contains the literal title `# Rights & Data-Model Policy`.
    - `04-POLICY.md` contains the exact string `https://creativecommons.org/licenses/by-nc/4.0/legalcode`.
    - `04-POLICY.md` contains a per-photo converter mapping row for each of `cc0`, `cc-by`, `cc-by-nc`, and `none`, with `none` marked as excluded.
    - `04-POLICY.md` documents the native consent basis citing `D-08` (platform-policy assertion + submission-form notice going forward).
    - `04-POLICY.md` states native `rightsHolder` = individual contributor and `recordedBy` = contributor display name citing `D-09`.
    - `04-POLICY.md` states third-party `datasetName` = sub-source name, `recordedBy` = `usernm`, `rightsHolder` = sub-source-when-known-else-Whale-Alert/Maplify, citing `D-10` and `D-11`.
    - `04-POLICY.md` defines a `dynamicProperties` schema naming the keys `travelDirection`, `aggregatorChain`, and `unvalidatedIdentifiers`.
    - `04-POLICY.md` states regex-extracted identifiers are never emitted as `organismID` or `catalogNumber`.
  </acceptance_criteria>
  <done>Sections 1 and 2 of 04-POLICY.md are authored with all GAP-02/GAP-03 decisions and the D-08..D-11 citations; all acceptance_criteria source assertions hold.</done>
</task>

<task type="auto">
  <name>Task 2: Author Data-Model Gaps & Resolutions and Scope Clarifications sections (GAP-01, GAP-04)</name>
  <files>.planning/phases/04-rights-data-model-policy-gate/04-POLICY.md</files>
  <read_first>
    - .planning/phases/04-rights-data-model-policy-gate/04-RESEARCH.md (Data-Model Gap Audit — observations + maplify.sightings; DarwinCore Term Semantics Reference — Spatial/Temporal/Occurrence-Level Terms; the min_count / D-14 correction note; Common Pitfalls 3-5)
    - .planning/phases/04-rights-data-model-policy-gate/04-CONTEXT.md (D-12, D-13, D-14; "Already locked upstream" block — basisOfRecord, geodeticDatum, coordinateUncertainty, eventDate, occurrenceID)
    - .planning/REQUIREMENTS.md (GAP-01, GAP-04 text; ALIGN-05 eventDate precision; Out of Scope table)
    - supabase/migrations/20250915170256_fix-inat-photos.sql (count / number_sighted / min_count handling — grounds D-12..D-14)
    - supabase/migrations/20250903172708_initial_schema.sql (observations.count, source column, license / travel_direction enums)
  </read_first>
  <action>
    Author Section 3 (Data-Model Gaps & Resolutions) per GAP-01 and GAP-04. Produce a per-term gap
    table for each in-scope source (`public.observations`, `maplify.sightings`,
    `public.observation_photos`). Each row records: DwC term, source column (or "(none)" for a
    constant), the gap description, and the resolution rule. Every audited gap MUST have an explicit
    resolution — never a silent default (success criterion 1). Cover at minimum:
    - `eventDate` precision (ALIGN-05): native `observed_at` is honest full-timestamp; Maplify
      `created_at` is report-time → emit at **date precision only** (`created_at::date`), never a
      false second-level sighting time. State the reason.
    - `coordinateUncertaintyInMeters`: emit real meters from `observations.accuracy` when non-null;
      **omit when unknown — never 0**; Maplify stores no accuracy → omit.
    - `basisOfRecord`: constant `HumanObservation` per-source for all in-scope sources.
    - `occurrenceStatus`: constant `present` on every in-scope record (D-12) — a constant column.
    - `individualCount`: emit only when a real count exists (`observations.count`,
      `maplify.number_sighted`); sparse column (D-13).
    - `geodeticDatum` = `WGS84` constant; `occurrenceID` source-prefixed surrogate keys
      (`salishsea:{id}`, `maplify:{id}`), stable/deterministic across runs.
    - License-less photo exclusion and unvalidated-identifier exclusion (GAP-04) — cross-reference
      Sections 1.4 and 2.4 so GAP-04 is fully closed.
    - The **D-14 correction**: record explicitly that `min_count` lives on `happywhale.encounters`
      (out of v1.2 scope), Maplify uses exact `number_sighted`, so D-14's min-count flagging has
      NO v1.2-applicable records — D-14 is a no-op for v1.2; preserve the policy for when HappyWhale
      is added. Add one sentence noting that CONTEXT.md D-14's reference to "maplify `min_count`" is
      a documentation inaccuracy, corrected here per RESEARCH.md — so the policy document is
      self-contained and a reader does not need to reconcile the two sources.

    Author Section 5 (Scope Clarifications): HappyWhale and iNaturalist excluded (already published
    to GBIF by canonical sources); restate the D-14 no-op; note `rwsas` is excluded at ingest (A4).

    Cite GAP-01, GAP-04, and D-12/D-13/D-14 inline. Name terms, columns, and constants only — no
    SQL bodies, no migration edits.
  </action>
  <verify>
    <automated>MISSING — documentation phase, no runtime. Use acceptance_criteria source assertions below.</automated>
    <human-check>Read Sections 3 and 5 against the RESEARCH.md gap audit and D-12..D-14.</human-check>
  </verify>
  <acceptance_criteria>
    - `04-POLICY.md` records `occurrenceStatus = present` as a constant column for all in-scope records, citing `D-12`.
    - `04-POLICY.md` records `individualCount` as emitted only when a real count exists (sparse), citing `D-13`.
    - `04-POLICY.md` records `coordinateUncertaintyInMeters` as omitted when unknown and never 0.
    - `04-POLICY.md` records Maplify `eventDate` emitted at date precision only (report-time, not sighting-time), referencing ALIGN-05.
    - `04-POLICY.md` records `basisOfRecord = HumanObservation` and `geodeticDatum = WGS84` as constants.
    - `04-POLICY.md` notes `D-14` min-count flagging has no v1.2-applicable records (`min_count` is happywhale-only; Maplify uses exact `number_sighted`) and states D-14 is a no-op for v1.2, explicitly flagging CONTEXT.md D-14's "maplify `min_count`" wording as a corrected documentation inaccuracy.
    - `04-POLICY.md` states the license-less photo exclusion and the unvalidated-identifier exclusion as GAP-04 resolutions (may cross-reference Sections 1.4 / 2.4).
    - `04-POLICY.md` Section 5 records HappyWhale and iNaturalist as excluded from v1.2.
  </acceptance_criteria>
  <done>Sections 3 and 5 of 04-POLICY.md are authored; every audited gap has an explicit resolution; the D-14 no-op correction is recorded; all acceptance_criteria source assertions hold.</done>
</task>

<task type="auto">
  <name>Task 3: Author Third-Party Redistribution Status section with conferral questions and hold rule (GAP-04)</name>
  <files>.planning/phases/04-rights-data-model-policy-gate/04-POLICY.md</files>
  <read_first>
    - .planning/phases/04-rights-data-model-policy-gate/04-RESEARCH.md (Third-Party Source Redistribution Landscape — Maplify/Conserve.IO, Orca Network, Cascadia; "Note on Whale Alert nomenclature"; Common Pitfalls 1 and 6)
    - .planning/phases/04-rights-data-model-policy-gate/04-CONTEXT.md (D-01, D-02, D-03, D-04, D-05, D-06, D-07; "Specific Ideas"; "Deferred" — conferral is out-of-band)
    - .planning/REQUIREMENTS.md (GAP-04; Milestone Scope — native + Maplify/Whale Alert only)
    - .planning/STATE.md (Blockers/Concerns — redistribution as external legal question)
  </read_first>
  <action>
    Author Section 4 (Third-Party Redistribution Status) per GAP-04 and D-01..D-07. Frame open
    questions for conferral rather than asserting permission (D-04) — record that most sources have
    no publicly stated redistribution policy (absence confirmed by research, not assumed).
    Include:
    - The build/hold stance: build at full scope (D-01); technical default is include-and-attribute
      (D-02), retreating from a source only on explicit prohibition or a "no" during conferral;
      per-`maplify.source` drop granularity (D-03) so a single declining sub-source can be dropped
      without dropping the whole feed (the `dwc` projection must support filtering by `maplify.source`).
    - The hosted-but-unlinked hold (D-05): the nightly job publishes to the stable `/dwca/` URL as
      designed; only the frontend download link is suppressed (state = "unlisted," not private; no
      new access-control infra).
    - Native-only public eligibility (D-06): native records may be publicly linked independently of
      third-party conferral (subject to the D-08 consent notice); the third-party-inclusive public
      link waits until conferral clears.
    - A per-organization subsection for **Whale Alert / Conserve.IO** (contact `info@whalealert.org`
      — explicitly the Conserve.IO marine-mammal app, NOT the `whale-alert.io` crypto tracker),
      **Orca Network**, and **Cascadia Research**. Each records: what is publicly stated, what is
      NOT stated, contact surface, and a precisely framed conferral question (use RESEARCH's drafted
      questions). Note Cascadia's own datasets already use CC-BY-NC 4.0 (positive signal).
    - The flagged open question **D-07**: D-06 implies a possible native-only public archive variant
      distinct from the held full archive; record this as an OPEN implementation question for the
      planner of Phase 7/8 (one held archive + one native-only variant, a build-time flag, or other)
      — flagged here, not resolved.
    - State that organizational conferral is an out-of-band, non-engineering task tracked as the
      gate that un-hides third-party records (do not plan it as a code task).

    Cite GAP-04 and D-01..D-07 inline. Do NOT assert "no prohibition = permission" (Pitfall 6).
  </action>
  <verify>
    <automated>MISSING — documentation phase, no runtime. Use acceptance_criteria source assertions below.</automated>
    <human-check>Read Section 4 against GAP-04, D-01..D-07, and the RESEARCH redistribution landscape.</human-check>
  </verify>
  <acceptance_criteria>
    - `04-POLICY.md` lists a distinct conferral question for each of Whale Alert (Conserve.IO), Orca Network, and Cascadia Research.
    - `04-POLICY.md` names `info@whalealert.org` / Conserve.IO and explicitly distinguishes it from the `whale-alert.io` cryptocurrency tracker.
    - `04-POLICY.md` records the hosted-but-unlinked hold citing `D-05` (publish to `/dwca/`; suppress only the frontend link).
    - `04-POLICY.md` records the include-and-attribute default citing `D-02` and per-`maplify.source` drop granularity citing `D-03`.
    - `04-POLICY.md` records native-only public eligibility citing `D-06`.
    - `04-POLICY.md` flags the native-only-variant question as OPEN for Phase 7/8 citing `D-07`.
    - `04-POLICY.md` does not assert redistribution permission where none was found (frames conferral questions per `D-04`).
  </acceptance_criteria>
  <done>Section 4 of 04-POLICY.md is authored with per-organization conferral questions, the include-and-attribute + hosted-but-unlinked hold rule, native-only eligibility, and the flagged D-07 open question; the document is complete across Sections 1-5; all acceptance_criteria source assertions hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation-only phase. The deliverable is a Markdown file in the planning tree.
There is no runtime, no new attack surface, no data exposure, and no code execution. The only
boundary that matters is the **policy boundary**: this document decides what downstream phases are
permitted to publicly expose.

| Boundary | Description |
|----------|-------------|
| policy → Phase 5-8 encoding | Decisions written here become the SQL/serializer/hosting behavior that determines what third-party data is publicly exposed. A wrong policy here propagates silently into a public archive. |

## STRIDE Threat Register

The genuine risk this phase governs is *policy* risk — a policy that green-lights publicly
exposing un-conferred third-party records, or that asserts redistribution permission that does not
exist. No code-level threats apply.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Redistribution Status section | mitigate | The policy MUST encode the hosted-but-unlinked hold (D-05/D-06) and frame conferral questions (D-04) rather than asserting permission — so Phases 7/8 cannot silently expose third-party data publicly. Acceptance criteria on Task 3 assert the hold rule and per-org conferral questions are present. |
| T-04-02 | Repudiation | Redistribution Status section | mitigate | The policy MUST NOT assert "no prohibition found = permission" (RESEARCH Pitfall 6). Conferral is recorded as an out-of-band gate that un-hides third-party records; permission is never claimed without it. |
| T-04-03 | Tampering (provenance integrity) | Attribution & Provenance + Gaps sections | mitigate | License-less photos and unvalidated identifiers are excluded by documented rule (GAP-04), and Maplify report-time is emitted at date precision only — so the published archive cannot misrepresent licensing, identity, or sighting-time precision. Asserted in Task 1/2 acceptance criteria. |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). No supply-chain surface. |

ASVS Level 1; block on high severity. No high-severity code-level threats exist for a
documentation deliverable; the policy-risk threats above are mitigated by the named acceptance
criteria, not by runtime controls.
</threat_model>

<verification>
This phase has no runtime, build, or automated test surface. Verification is human review of
`04-POLICY.md` against the four ROADMAP success criteria plus the per-task source assertions:

1. Every audited data/datatype gap (eventDate precision, omit-unknown coordinateUncertainty,
   per-source basisOfRecord, occurrenceStatus/individualCount, license-less photo exclusion,
   unvalidated-identifier exclusion) has an explicit resolution — none silently defaulted.
2. The occurrence license is the exact string
   `https://creativecommons.org/licenses/by-nc/4.0/legalcode`, with the native consent stance
   (D-08) documented.
3. The attribution/provenance model specifies `recordedBy` / `rightsHolder` / `datasetName` for
   native (D-09) and third-party (D-10/D-11), including the Whale Alert → sub-source chain.
4. A redistribution decision is recorded: include-and-attribute default, hosted-but-unlinked hold,
   and a per-organization conferral question for Whale Alert / Orca Network / Cascadia.

The D-14 min-count no-op correction is recorded (success criterion 1 — no silent default).
</verification>

<success_criteria>
- [ ] `04-POLICY.md` exists in the phase directory with all five named sections.
- [ ] Every audited gap from RESEARCH.md appears in Section 3 with an explicit resolution (no silent default) — closes GAP-01.
- [ ] Section 1 contains the exact CC-BY-NC 4.0 `/legalcode` URI, the per-photo converter table, and the D-08 consent basis — closes GAP-02.
- [ ] Section 2 specifies recordedBy / rightsHolder / datasetName for native and third-party sources plus the dynamicProperties schema — closes GAP-03.
- [ ] Section 4 records the include-and-attribute default, hosted-but-unlinked hold, native-only eligibility, the flagged D-07 open question, and a per-organization conferral question — closes GAP-04.
- [ ] The D-14 min-count no-op correction is recorded.
- [ ] No SQL, app code, schema change, or migration was produced or modified.
- [ ] Every D-NN (D-01..D-14) is cited somewhere in the document.
</success_criteria>

<output>
Create `.planning/phases/04-rights-data-model-policy-gate/04-01-SUMMARY.md` when done.
</output>
