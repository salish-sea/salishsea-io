---
phase: 02-rich-previews
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - public/preview.jpg
autonomous: false
requirements:
  - PREV-01
  - PREV-02

must_haves:
  truths:
    - "A 1200x630px branded fallback image exists that will appear when no licensed occurrence photo is available"
  artifacts:
    - path: "public/preview.jpg"
      provides: "Static branded OG fallback image (1200x630px, JPEG)"
  key_links:
    - from: "public/preview.jpg"
      to: "https://salishsea.io/preview.jpg"
      via: "Vite copies public/ to dist/ verbatim; S3 sync uploads dist/ to s3://salishsea-io/site/"
      pattern: "salishsea.io/preview.jpg"
---

<objective>
Create the static branded fallback image that appears in the og:image tag when an occurrence has no openly-licensed photo. This image lives in `public/` so Vite copies it to `dist/` verbatim (no content-hash renaming). The existing S3 deploy sync uploads `dist/` to `s3://salishsea-io/site/`, so `preview.jpg` lands at `/site/preview.jpg` and is served by CloudFront at `https://salishsea.io/preview.jpg` automatically on every deploy — no manual `aws s3 cp` needed.

Purpose: PREV-01 and PREV-02 — all shared links show an image; occurrences without licensed photos show the branded image.
Output: `public/preview.jpg` (1200x630px JPEG) committed to the repo; deployed via the normal Vite build + S3 sync.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-rich-previews/02-CONTEXT.md
@.planning/phases/02-rich-previews/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create branded fallback preview image</name>
  <files>public/preview.jpg</files>
  <action>
    The edge handler hardcodes `https://salishsea.io/preview.jpg` as the og:image fallback. This image needs to be created by the user — Claude cannot generate a branded JPEG.

    Place the file at `public/preview.jpg` (NOT `src/assets/`). Files in `src/assets/` are processed by Vite and renamed with content hashes, which would break the hardcoded URL. Files in `public/` are copied to `dist/` verbatim and then synced to S3 by the deploy workflow, making the image available at `https://salishsea.io/preview.jpg` automatically.

    Required specs:
    - Dimensions: 1200x630px (1.91:1 aspect ratio — optimal for Facebook, Bluesky, RCS, Twitter/X)
    - Format: JPEG or PNG (save as preview.jpg regardless)
    - Content: "SalishSea.io" wordmark or logo on a branded background (Salish Sea / orca theme)
    - Must look good as a preview card thumbnail — simple, readable at small sizes

    Steps for the user:
    1. Create the image using Figma, Canva, Photoshop, or any design tool
    2. Save it as `public/preview.jpg` in the repo
    3. Commit and push — the deploy workflow will sync it to S3 and it will be served at `https://salishsea.io/preview.jpg`
    4. Verify it's accessible at `https://salishsea.io/preview.jpg` in a browser
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://salishsea.io/preview.jpg</automated>
  </verify>
  <done>Image returns HTTP 200 at https://salishsea.io/preview.jpg AND public/preview.jpg exists in the repository</done>
</task>

</tasks>

<verification>
`curl -s -o /dev/null -w "%{http_code}" https://salishsea.io/preview.jpg` returns 200.
`public/preview.jpg` exists in the repository.
</verification>

<success_criteria>
- Image file committed at `public/preview.jpg`
- Image is 1200x630px (1.91:1 aspect ratio)
- Image is accessible at `https://salishsea.io/preview.jpg` via CloudFront (HTTP 200)
- Image looks appropriate as a branded preview card (simple, readable at small sizes)
</success_criteria>

<output>
After completion, create `.planning/phases/02-rich-previews/02-03-SUMMARY.md`
</output>
